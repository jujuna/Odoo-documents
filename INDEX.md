# Odoo Business Module Documentation

> Internal knowledge base — built from reading actual Odoo source code.
> Every fact in these files is traceable to a real source file path.
> Updated as we learn. Never written from assumptions. **Latest cross-module payroll/public-holiday audit updated by Codex on 2026-07-12.**
>
> **Version caveat:** unless a row says otherwise, a file was written against **Odoo 19** source. Files re-verified against **Odoo 20** so far:
> [`account_analytic_account.md`](account_analytic_account.md), [`account_asset.md`](account_asset.md), [`account_returns.md`](account_returns.md), [`accounting_coa.md`](accounting_coa.md), [`accounting_fixed_costs_guide.md`](accounting_fixed_costs_guide.md) (all 2026-09-22),
> [`accounting_migration.md`](accounting_migration.md), [`accounting_multicompany_branches.md`](accounting_multicompany_branches.md), [`accounting_reports.md`](accounting_reports.md) (2026-09-22),
> [`hr_payroll.md`](hr_payroll.md), [`hr_payroll_account.md`](hr_payroll_account.md), [`hr_timesheet.md`](hr_timesheet.md), [`hr_job_positions.md`](hr_job_positions.md), [`hr_recruitment_sourcing.md`](hr_recruitment_sourcing.md) (all 2026-09-22),
> [`stock_valuation.md`](stock_valuation.md), [`taxes.md`](taxes.md), [`timesheets.md`](timesheets.md), [`work_entries.md`](work_entries.md) (all 2026-09-22),
> [`rs_waybill_einvoice_fix_plan.md`](rs_waybill_einvoice_fix_plan.md) (2026-09-22, delta section at the top; the body below it stays Odoo 19).
> Two 20.0-wide changes affect nearly every other file: `ir.model.access` + `ir.rule` were merged into a single **`ir.access`** model
> (one CSV with `operation` and `domain` columns), and the `_read_group_*` / `Domain.custom` / `_field_to_sql` ORM hooks moved to a
> **`table`-first** signature.

---

## How to Use

- Each file covers one Odoo business module end-to-end.
- Code references are clickable links to the actual source.
- "Dependencies" sections tell you which modules must be installed and what integrations exist.
- Start with a module's doc before asking questions — the answer may already be there.

---

## Module Index

| Module | File | Status | Last Updated |
|---|---|---|---|
| `hr_payroll` (+ `hr_payroll_account`, `hr_work_entry`, holidays/attendance/planning/expense/commission/iso20022/contract_salary/documents bridges) | [`hr_payroll.md`](hr_payroll.md) | **Reviewed against Odoo 20 (2026-09-22)** — the deepest restructure in the set. **`hr.work.entry` is deleted**: work entries are now transient value dicts returned by `hr.version.generate_work_entries()` — no records, no 4-state machine, no conflict checks, no undefined-slot error, no generation cron, no regeneration wizard, no validation on confirm. **`work_entry_source` is gone** (replaced by `attendance_based` / `has_static_work_entries()`), **`hr.leave.type` merged into `hr.work.entry.type`**, **`hr.payslip.input.type` deleted** (inputs are defined by salary rules), and a new **`hr.time.rule`** engine with two daily crons replaces the old work-entry machinery. `hr_payroll_holidays`, `hr_work_entry_holidays(_enterprise)`, `hr_work_entry_attendance`/`_planning`, `hr_payroll_planning`, `hr_payroll_sale_commission`, `hr_contract_salary_payroll`/`_holidays` no longer exist. Pay run gains a `00_draft` state; counters renamed `payslips_with_warnings`/`payslips_with_errors` (sums of counts, not slip counts); **`compute_sheet` no longer blocks on `error_count`** (the v19 claim was materially misleading — the guard moved to done/paid/payment-wizard); hardcoded errors replaced by data-driven `hr.payroll.warning`; `correct_sheet` and the Edit-Payslip-Lines wizard gone (in-place line editing); `wage_with_holidays`/`wage_on_signature` gone; payment rewritten around the wizard's two exits with `mark_as_paid`. 458 links, all verified. **Pre-20 summary retained:** **Full audit (8-agent workflow, adversarially verified)** — rewritten end-to-end around statuses, blockers, and accounting. Adds: **Module Map** (community vs enterprise layering, dead stubs); **Work Entries** (4-state machine, the 4 conflict checks, undefined-slots UserError, generation cron + horizon gotcha, worked-days rollup, paid/unpaid amounts); **Time Off** (validated-leave→work-entry mapping, the "unapproved leave → conflict → can't generate payslip" chain, `hr_payroll_holidays` deferral: `payslip_state` normal/done/blocked, "Employee has time off to defer" danger error, Report-to-Next-Month guards, orphaned `_error_dependencies` reactivity caveat); **Statuses, Errors & Warnings** (`state` vs `state_display`, the `error_count` gate, full payslip/pay-run transition tables with every blocking condition, buttons-per-state, banner catalog, wrong-version/negative-net); **Refund/Correction/Recompute** (credit_note sub-lifecycle, `edited` flag); **Accounting deep mechanics** (draft-never-posted, gate conditions, (journal,month) batching, NET `not_computed_in_net`, storno, merge vs split, employee partner + per-bank split, analytic propagation, rounding adjustment line, accounting date, multi-company/currency, **move on cancel → `_unlink_or_reverse` delete/cancel/reverse**, set-to-draft keeps move); **Payment paths** (3 paid-state paths matrix, payment-report fields, SEPA/`batch_booking`, reconciliation residual gate, dead IBAN check); **Integrations** (attendance/planning/expense/commission/fleet — what each injects + blockers); **Contract Salary configurator → version handoff** (wage vs wage_with_holidays vs wage_on_signature, BE gotcha); **Crons/async PDF/`queued_for_pdf`/Documents bridge**; **Appendices**: A=full Blockers catalog, B=Status reference, C=Symptom→Cause. Retains prior salary-input/rule/period/Register-Payment-grouping detail; **+ 2026-07-11 internals append**: `hr.rule.parameter` date-versioned constants (lookup at payslip.date_to, no country filter, **edits clear whole registry cache**); **payroll_properties v19 input system** (Properties definitions on structure, `property_input` rules bypass amount_select, version↔payslip key-intersection sync, configurator benefit backend, copy() broken upstream); rule sections; headcount snapshots; declarations framework (dormant without l10n); configurable dashboard warnings (safe_eval records, one broken = whole panel dead); YTD mechanics (per-structure opt-in + company reset date); line math `amount×qty×rate/100` + styling; indexation wizard; **likely upstream bug: wage writes don't auto-refresh draft payslips** (`_get_fields_that_recompute_payslip` bound-method vs string keys); settings reference | 2026-09-22 |
| `hr_payroll_account` (+ `hr_payroll_account_iso20022`, `hr_payroll_expense`, `project_hr_payroll_account`) — Payslip to Journal Entry | [`hr_payroll_account.md`](hr_payroll_account.md) | **Reviewed against Odoo 20 (2026-09-22)** — **the Pay button and `action_register_payment` were removed from this module.** Paying is now `hr_payroll`'s payment-report wizard (`mark_as_paid()` sets `paid`); only `l10n_au_hr_payroll_account` reinstates a register-payment button, so the v19 reconciliation-driven flip to `paid` no longer describes generic payroll. Validate button reverted from "Create Draft Entry" to **Validate**; `post_init_hook` deleted (journal/structure wiring is now generic chart-template data); `base_iban` dropped from the manifest and `models/res_partner_bank.py` deleted; `hr_payroll_account_iso20022` no longer depends on this module. Move construction split into `_get_account_move_vals()` + `_action_create_account_move()`; **line merging rewritten** around an exact (label, account, analytic distribution) key — the v19 "shares any analytic account" heuristic is now dead code; `narration` is always empty; the accounting `date` is a stored computed field; batch mode also pulls draft slips and is labelled **Anonymize Journal Entries**. New: `mock.account.move.wizard` entry preview, data-driven `hr.payroll.warning`. The "IBAN check is dead code" gotcha was re-verified and is **still true**. 132 links, all verified | 2026-09-22 |
| `rs_einvoice` + `rs_base` — Down Payments / Advances | [`rs_einvoice_down_payments.md`](rs_einvoice_down_payments.md) | Complete — two-layer model (advance invoice via `save_invoice_a` + settlement), the two settlement modes (Net offset proportional-spread vs Native rs.ge attach v3.0.5), auto-advance detection, force-skipped DP offset CN, native eligibility (buyer-confirmed + strictly-earlier-month), SO-link advance resolution, over-settle/stray/MIN_DRG guards, mode-lock-after-filing, advance-correction blocks, waybill wizard forcing Native, 13-row restriction table; + VAT-compliance verdict (Native = Order #996 Art. 53 / 1-Sep-2025 field-9 reference-and-offset; Net = totals-correct fallback, YELLOW formal-defect risk). **Re-verified against the canonical `gec_odoo_modules` copy on Odoo 20 (2026-09-22):** all paths repointed off the dead `gec_rs_invoice`/`rs_base` copies, 14 line refs re-anchored by symbol, security section rewritten for the merged `ir.access.csv`, and **one claim withdrawn** — the waybill wizard no longer forces Native (`native_attach` appears nowhere in `rs_base_methods`), so the manual-vs-wizard divergence is gone. New section documents the real selector: `create()` upgrades the `net` default to `native_attach` whenever `_rs_native_advance_eligible()` passes, i.e. the advance is traceable, buyer-confirmed and dated a strictly earlier month | 2026-09-22 |
| `rs_waybill` + `rs_einvoice` + `rs_base_methods` — Findings and Fix Plan | [`rs_waybill_einvoice_fix_plan.md`](rs_waybill_einvoice_fix_plan.md) | **2026-09-18, combined Claude + Codex review, verified against Odoo 19 core** — 38 items with issue / effect+example / fix location: P0 dependency cycle, journal-entry Post button hidden by XPath, vendor-bill link writes raise; P1 paid-invoice auto-cancel unreconciles, status-8 cleanup not retried, reissue copy doubles qty_invoiced, net-advance VAT residual, supplier reissue loses PO links; P2 stock quantity math (forced validation, returns, SO qty overwrite, missing move links, distribution load), sync backfill/cursor, multi-company; P3 tech. **+ Core-safety review (same day):** verdict per item (OK / ADJUST / NO-VALIDATION / DECIDE), 5 text corrections (#1 blast radius, #2 also `button_cancel`, #6 no upselling, #17 rollback not half-apply, #24 pairs meaning), root design note cancel-vs-credit-note, and a **Provoke** line for every item (copy-DB, Shell vs rs.ge). **+ Session 2 sweep (2026-09-18/19), items 39-62, single-pass (no adversarial verify), payment/reconciliation + sale/purchase/stock focus:** 2 items reproduced on the gec_modules2 TEST DB (not production) — every posted rs.ge-pulled bill checked there books 0.00 input VAT against a certified rs.ge VAT amount (#39), and a real 0.01 rounding drift (#40); rest are real code defects, mostly not exercised there because the buyer-side PO bridge has never linked a waybill to a PO on that test DB (0 of 2,145). **+ Session 3 sweep (2026-09-19), items 63-68, the two readers left unfinished in session 2:** wizard `rs_invoice_from_waybill_wizard` can attach an invoice line to the wrong sale order or push `qty_invoiced` past what was delivered with no warning (#63/#65); the four-button invoice lock is bypassed by the Update button and both sync/drift crons (#66); goods-line matching by (bar_code, name) instead of the stable `rs_good_id` can cross-assign quantity/price between two same-product lines (#68). All six planned readers (seller accounting, buyer accounting, sale, purchase, stock core, stock actions/sync) now complete at least once; XML/CSV/security/data files and tests remain unswept by design. **+ Re-verification 2026-09-19:** the `rs_waybill` copy committed to `gec_odoo_modules` that day is byte-identical to the audited one (`diff -r` exit 0 over all 78 files, only a carried-over README added), so all 37 rs_waybill items stand; #1 and #37 are worse (cycle now at both paths, `rs_waybill` now has 2 on-disk copies). **+ 2026-09-19, same day: canonical folder decided = `custom_addons/gec_odoo_modules`, nothing else** — every path in the plan repointed there, older copies (`gec_rs_waybill`, `rs_base`, `gec_project`, `gec_rs_invoice`) declared dead and the duplicate-copies item deleted. **#1 FIXED and deleted from the file** (`rs_base_methods` dropped from `rs_waybill` depends; reproduced first as `UserError: Recursion error in modules dependencies!` on a fresh DB) — 65 open items remain (#1, #2 and #37 deleted as fixed/settled), and one residual is carried as a single line at the top: the `_get_rs_credentials` runtime guard was never added. **+ Re-verified against Odoo 20 core 2026-09-22 (delta section at the top of the file):** no finding is invalidated, but **the three modules do not run on 20.0** — `stock.return.picking` is deleted (every return path in `rs_waybill` raises), `stock.move.product_uom` / `stock.move.line.product_uom_id` / `purchase.order.line.product_uom_id` are all renamed to `uom_id` (7 call sites), and the waybill dashboard uses OWL-2 `useState` + `t-esc`, both retired in OWL 3; the `ir.access` blocker was fixed the same day. Returns are now picking-native (`stock.picking._create_return()`), and the wizard overrides that #22/#25 relied on for `purchase_line_id` / `sale_line_id` / `to_refund` / `origin_returned_move_id` survived on `stock.picking._prepare_return_move_default_values` — but return lines now start at quantity 0. `_check_draftable` dropped its tax-cash-basis refusal, so the #4 auto-cancel is marginally worse and #5's provoke recipe must use the hash lock. `is_storable` moved to `product.template` and `tracking` no longer accepts `'none'`. **#40's stated mechanism is wrong in both versions** — `price_unit` carries `min_display_digits`, not `digits`, so nothing rounds it; that item needs re-deriving. **+ Section G, same day — the fixes, source-verified against 20.0 and not yet applied:** G1 the 7 UoM renames (with the two `account.move.line` sites that must NOT be renamed), G2 a full rewrite of `_create_return_picking` on `picking._create_return()` that **also closes #16** and re-enables the return paths #21/#22/#24/#25/#60 depend on, G3 `useState` → `proxy` + `t-esc` → `t-out` in the waybill dashboard, G4 the one-line `tracking` fix that silently stops receipt completion, G5 porting the two dead `_name_search` overrides to `_search_display_name`, and G6 the order of work. Items #16, #22, #25 and #30 carry an inline **20.0** line pointing at these, and #36's path was repointed at the new `ir.access.csv`. **+ Ported and installed, same day:** running `-i rs_waybill` surfaced five more blockers that reading the source had not — **V6** `ir.config_parameter.get_param`/`set_param` removed for typed `get_str`/`get_bool`/`set_str`/`set_bool` (fails at install, in `res_company.init()`), **V7** `company_registry` deleted from `res.partner`/`res.company` with no Georgian key in its `additional_identifiers` replacement, so all 33 non-test TIN sites collapse to `vat`, **V8** the `uom` view xmlids renamed, **V9** `sale_stock`'s order-form extension now carrying priority 20 so ours must outrank it, and **V10** list columns wrapped in `<column>`. G1–G4 and G7 are applied and **all three modules install and upgrade cleanly on gec20_prod1**; G5 and the test suite (7 files still on removed APIs) are not, and the OWL dashboard is not runtime-verified (section G8) | 2026-09-22 |
| `rs_base` + `gec_rs_invoice` — Method Reference | [`rs_modules_method_reference.md`](rs_modules_method_reference.md) | Complete — per-method reference for **all 307 callables** across 23 files, each tagged Liveness/Role/Side-effects. Organised by flow: SOAP client layer; seller/buyer/sync invoice lifecycle; correction & cancel-and-reissue orchestration; advance settlement; buyer inbox + crons; rs_base credentials (SOAP + REST/2FA) and the waybill↔invoice bridge + invoice-from-waybill wizard. Includes rs.ge status-code table, module-layering note, and a dead-code/TODO summary (7 dead callables + hardcoded-18%-VAT/excise TODOs flagged) | 2026-06-27 |
| `analytic` (+ `account` bridge) — Analytic Accounting | [`analytic_accounting.md`](analytic_accounting.md) | Complete (7-agent workflow, source-verified) — plans-as-dimensions vs accounts-as-values vs lines-as-postings; the `analytic_distribution` JSON (percentages, comma-key = multi-plan combo, GIN index + array-overlap search); dynamic per-plan columns (`account_id` vs `x_plan{N}_id`, Project plan via `analytic.project_plan` param); **post-time materialization** (`_post`→`_create_analytic_lines`→`_prepare_analytic_distribution_line`, amount=−balance×pct/100 with rounding residual); mandatory-plan applicability (100%-or-block); distribution-model auto-fill (one rule per root plan); analytic.line↔move.line link (cascade/restrict, sign `*-1`); computed balances; the `group_analytic_accounting` switch | 2026-06-30 |
| `analytic` — `account.analytic.account` model deep-dive | [`account_analytic_account.md`](account_analytic_account.md) | **Reviewed against Odoo 20 (2026-09-22)** — field-by-field walkthrough of the model: plan/root-plan/`color`, company + partner indexes, `_check_company_domain_parent_of`; the balance computation corrected (**`debit` = negated sum of negative amounts, `credit` = sum of positive** — they were documented the wrong way round) and `_convert` no longer passing an explicit `date`; `_read_group_select`/`_read_group_postprocess_aggregate` under the **20.0 `table`-first ORM signature** (plus the `Domain.custom`/`Query` moves in `analytic_mixin`); plan migration SQL + `RedirectWarning`; `auto_account_id` magic link; **`copy_data` override removed — no more " (copy)" suffix**; `ir.model.access`+`ir.rule` → single `ir.access.csv` and `group_analytic_accounting` as a light group; the real distribution→line mechanism (`_create_analytic_lines` on post, `_inverse_analytic_distribution` splitting synchronously — **there is no cron**); 20.0 `account.analytic.line` changes (`name` optional, `product_uom_id` compute hook, `_check_can_write`); gotchas, programmatic access via `plan._column_name()`, performance notes | 2026-09-22 |
| `account_budget` + `project_account_budget` | [`analytic_budget.md`](analytic_budget.md) | Complete — budget flow, achieved amount logic, project integration | 2026-02-26 |
| `stock` + `sale_stock` | [`inventory.md`](inventory.md) | Complete — warehouses, routes, SO→delivery flow, reservations, quants, multi-step, secondary-location fallback pattern | 2026-03-03 |
| `stock` — Forecast Report | [`inventory_forecast_report.md`](inventory_forecast_report.md) | Complete — header numbers, graph SQL view, reconciliation algorithm, forecast widget, lead time | 2026-02-27 |
| `account` — Chart of Accounts | [`accounting_coa.md`](accounting_coa.md) | **Verified against Odoo 20** — prerequisites, account types, **parent-account hierarchy (`account.group` deleted; `parent_id` / `code_path` / `name_path`)**, roots, tags, multi-company code mapping, opening balances, security, chart template loading (`TEMPLATE_MODELS`, no group pass), **property defaults now driven by `res.company` via `company_default_for()`**, product-account cascade walking the category tree, **inventory variation/closing moved from `stock_account` into core `account`** | 2026-09-22 |
| `gec_l10n_ge_tax` (custom) — Tax Grids → DGV Chain | [`gec_l10n_ge_tax.md`](gec_l10n_ge_tax.md) | **How tax grids work, end-to-end from source (2026-07-15)** — grid = `account.account.tag` (applicability=taxes) stamped on move lines at posting, reports aggregate by tag not account; 33 GE tags ([GE-VAT]/[GE-WHT]/[GE-SmallBiz], plain names, no ± pairs) → taxes attach them via `rep()` repartition helper (names resolved by stock `_deref_account_tags`); RC ±100% double-leg fires both grids at net-zero GL; refunds reuse invoice tags so credit notes shrink boxes; DGV `account.report` engine `tax_tags`: SQL sums balance per tag, **leading `-` in formula flips sign** (verified account_report.py:3921), expressions auto-create missing tags → tags file must load first; groups settle to `3399`; pre_init adopts pre-split gec_localization tag xmlids, post_init backfills only-missing records per ge company; **DGV box layout = placeholder** (tag wiring real, reconcile vs official rs.ge form before filing); payroll `debit/credit_tag_ids` = same tag objects, none exist for salaries. WHT/CIT/return = pointer-level  **2026-09-09: partner default taxes + Not Taxed products added to the module (code written, not run), module path corrected to `custom_addons/odoo_taxes/`** | 2026-09-09 |
| `account` + `l10n_account_withholding_tax` — Tax/Account/Move Constraints | [`odoo_tax_account_constraints.md`](odoo_tax_account_constraints.md) | Reference — **158 verified constraints (6-area workflow + completeness critic)** on account.account & account types, account.tax + repartition lines, account.move/line posting (incl. the **off_balance "all lines same type"** rule that broke a WHT payment), fiscal positions + withholding (negative-amount, empty-FP-strips-taxes, CABA shared-tag), chart-template loading (tag-deref UserError, repartition duplication on re-load), journal/payment. Each: rule, trigger, exact error, file:line, how-to-avoid. Built to prevent chart-load / tax-save / posting breakage in `gec_l10n_ge_tax` | 2026-06-28 |
| `account_reports` — Reporting Engine | [`accounting_reports.md`](accounting_reports.md) | **Verified against Odoo 20 (2026-09-22)** — **8 engines** (`text` + `reference` added) renamed to `_report_engine_*` with a shortened signature; `domain` keeps only `sum`/`-sum` (`sum_if_pos`/`sum_if_neg`/`count_rows` removed); aggregation bounds (`if_above`/`if_below`/`if_between`/`round`/`cross_report(...,force_date_scope)`); **currency table replaced by `consolidation_rate`/`consolidation_balance` on `account.move.line` with explicit CTA rules per account type**; **hierarchy now walks `account.account.parent_id` (`account.group` deleted)**; `filter_analytic` → `filter_analytic_groupby`; `foldable` → `foldability`; lines/columns are dataclasses (`report_data_objects.py`); **report snapshots** (`account.report.snapshot` + `@snapshotable_engine`); corrected Balance Sheet (EQU/EAR/OCI+CTA) and P&L (REV/COS/GRP/EXP/INC/OIN/OEXP/NEP/ALLOC) structures; Trial Balance comparison blocks; **new Interco Comparison report**; options-initializer sequence map; 19→20 migration checklist | 2026-09-22 |
| `hr_holidays` (+ `hr_holidays_attendance`) — Time Off Core: leaves, allocations, approvals | [`hr_holidays.md`](hr_holidays.md) | **Reviewed against Odoo 20 (2026-09-22)** — **heaviest rewrite of the batch: only ~1/3 survived, and just 2 of ~105 line links still pointed at correct code.** 20.0 dissolved three modules: **`hr_work_entry_holidays` deleted** (`hr_holidays` now depends directly on `hr_work_entry` and maps work entries itself), **`hr_payroll_holidays` deleted** (merged into enterprise `hr_payroll`: `action_report_to_next_month` replaced by `action_adjust_corresponding_payslip`, hard-coded payslip errors replaced by configurable `hr.payroll.warning` records + an `issues` JSON field), and **`hr.leave.type` merged into `hr.work.entry.type`**. **Security replaced:** unified `ir.access.csv`, new `group_hr_holidays_employee`, officer rule now bare `crud` — the v19 "officer can't write own validated leave" restriction is gone. **Field changes:** `time_type` → `count_as` (absence/working_time) plus a separate new `count_days_as`; `responsible_ids` ("Notify HR") removed, the HR step now routing via employee `hr_responsible_id`; `allocation_type` removed; new `time_off_selectable`, `country_id`, `unit_of_measure`. Officers now auto-approve any leave they create (not just `no_validation` types); the overlap check buckets by `count_as` and requires both types to lack `allow_request_on_top`; `hr_holidays_attendance` lost `overtime_deductible` entirely (previously documented as that module's whole purpose); crons 2 → 5. **New:** bundled public-holiday CSVs for 20 countries + `load.public.holiday.wizard` + monthly `_cron_generate_public_holidays`, `hr.time.rule` leave/allocation credits (`leave_compensation_rate`, `hr.time.rule.allocation.log`), `hr.leave.employee.report`, leave rescheduling via `_amend_resource_leave_dates`, constraints `_check_max_absence_per_day` and `_check_executive_employee_type`. Gotchas deleted as fixed upstream: the `'yes'` string leftovers and the non-reactive `_error_dependencies`. **Open question CONFIRMED and closed 2026-09-22** (traced in [`hr_payroll.md`](hr_payroll.md)): nothing schedules the "Leave to Defer" activity any more. The activity type is still shipped and both the `activity_feedback` handler and `_get_to_clean_activities()` still name it, but the v19 `activity_schedule` call lived in `hr_payroll_holidays/models/hr_leave.py` and died with that module — the only `activity_schedule` left in the payroll leave override is the approval one. `deferred_time_off_manager` is therefore configurable, visible in Payroll Settings and **read by nothing**; **Correct the payslip** is the only reachable resolution for a blocked leave. 159 links, all verified | 2026-09-22 |
| `hr_holidays` — Time Off Accrual Plans | [`hr_holidays_accrual_plans.md`](hr_holidays_accrual_plans.md) | **Reviewed against Odoo 20 (2026-09-22)** — ~60% held; mechanics and mental model survive, every name in the `hr.leave.type` orbit does not. **`hr.leave.type` is gone** — plans now carry a `work_entry_type_id` made effectively mandatory by two new constraints. **Headline gotcha still true and now STRICTER: carry-over OFF still wipes the balance on the carry-over date** — `_process_carryover_date` sets `allocated_duration = leaves_taken` when `action_with_unused_accruals == 'lost'` **or** `not can_be_carryover` (two ways to lose it, was one); only the UI improved. **Prior claim inverted: backdating now DOES back-pay** — `_add_lastcalls` deleted, `_init_accrual_calls` anchors `lastcall` at `date_from` and replays (upstream test: created 1 Mar with `date_from` 1 Jan → 3 days, not 1); migration warning about double-paying added. **Also:** `allocation_type` removed (accrual now just means "has an `accrual_plan_id`"); `already_accrued` deleted, replaced by guarded start/end paths + new cursors (`last_accrual`, `previous_carryover_number_of_days`, `carried_over_days_expiration_date`); `yearly_accrued_amount` → `yearly_accrued_days`; `postpone_max_days` → `max_carriedover_duration`; engine rewritten side-effect-free over an `allocation_data` dict; the `number_of_days > 0` SQL constraint → `_check_negative_allocation` (negative allocations now supported); hours→days re-resolves at each `nextcall`. **Batch wizard lost** department, employee-tag and `allocation_mode` targeting. Two false claims removed: the "already ran once" log note, and "the cron advances exactly one frequency cycle". 180 h worked example re-derived, unchanged. 63 links verified, ~35 repointed. **Pre-20 summary retained:** plan vs milestones (`hr.leave.accrual.level`), every Configuration field, milestone fields, cron engine (`lastcall`/`nextcall`/`already_accrued`/`yearly_accrued_amount`, approved-only, catch-up loop); **carry-over OFF still wipes the balance on 1 January** (`action_with_unused_accruals` forced to `lost`); first-period calendar proration; backdated `date_from` does not back-pay; hours stored as days via calendar `hours_per_day` (flexible = 24 h); yearly cap vs balance cap; worked monthly-vs-annual 180 h example; batch allocation wizard | 2026-09-22 |
| `resource` + `hr_holidays` + work-entry/attendance/planning/payroll/timesheet bridges — Public Holidays Full Flow | [`public_holidays_flow.md`](public_holidays_flow.md) | **Codex multi-agent full audit** — canonical model/scope/timezone/security; CRUD→leave reevaluation/notification/auto-refusal; work-entry stale-window and validated-entry hazards; exact calendar vs attendance vs planning source matrix; monthly/hourly/daily payroll math; native overtime collision policy; accrual eligibility and leave-balance flag; public-holiday timesheet generation/validation-lock issue; multi-company generation gap; Planning regeneration gaps; correction settlement lineage; Basis Bank shared-move warning; operator runbook and complete regression matrix | 2026-07-12 |
| `hr_holidays` — Duration Units (Day/Half-Day/Hours) | [`hr_holidays_time_off_units.md`](hr_holidays_time_off_units.md) | **Reviewed against Odoo 20 (2026-09-22)** — **only ~20% survived and 0 of 30 links did** — restructure, not drift. **`hr.leave.type` does not exist in 20.0**: time off types are `hr.work.entry.type` records and `holiday_status_id` → `work_entry_type_id`, so the doc's premise ("one field controls everything") was wrong at the root. **Two unit fields now:** `unit_of_measure` ("Unit", hour/day, **default `hour`**) drives allocations, balances and accrual, while `request_unit` ("Duration Type", **default `day`**) only caps request granularity — the mismatched defaults are a live trap, flagged in the doc. **"No per-request choice" is now false:** `request_unit_hours`/`request_unit_half` deleted, replaced by a genuine per-request `request_duration` (full/am/pm/specific) gated by `allowed_request_durations`, so an hour-type request can be booked as a full or half day. Labels changed (`day` = "Full Day", `hour` = "Custom Hours"); hour entry is now a datetime daterange; `number_of_hours_display` → `number_of_hours`; accrual unit comes from the **plan's** `added_value_type`, falling back to `unit_of_measure`. `hours_per_day` is now `hours_per_week / days_per_week`, auto-computed only for `calendar_type == 'fixed'`, and the 24 h/day fallback needs an `undefined` calendar **and** no hours target. Unchanged: `_get_hours_per_day(date_from)` and the 24 days = 192 hours arithmetic. **Pre-20 summary retained:** `request_unit` ("Duration Type") on the type forces the unit everywhere (no per-request choice); hour requests = time range on a date; allocations entered in hours for hour types; days always the storage unit, hours converted via employee calendar avg hours/day (flexible = 24h gotcha); accrual allocations show the plan's unit, not the type's; worked "24 days as 192 hours" example | 2026-09-22 |
| `hr` core — Employee & Version Engine | [`hr_employee_versions.md`](hr_employee_versions.md) | **Reviewed against Odoo 20 (2026-09-22)** — ~60% held. Core model intact: still **no `hr.contract`**, employee = timeline of `hr.version`, computed non-stored `version_id`, stored `current_version_id` + daily cron, date-derived contract state, silent `create_version` discard, `_get_calendars` fallback. **Two prior claims reversed: `tz` IS versioned in 20.0** (required Selection on `hr.version`, resolved via `employee._get_tz(date=...)`), and **`resource_calendar_id` is now `required=True`** defaulting to the company calendar — so "empty calendar = fully flexible" is dead; flexibility is `calendar_type == 'undefined'` and the stored `is_flexible`/`is_fully_flexible` fields are gone. **Departure rewritten:** transient `hr.departure.wizard` deleted → persistent `hr.employee.departure` + daily `_cron_apply_departure`, with `action_register()` unlinking every version after the departure date. **Attendance:** `hr.attendance.overtime` deleted; overtime now comes from `hr.time.rule` with a real FK `source_attendance_id`, so the value-match claim is obsolete. **Gotchas fixed:** contract-expiry cron is now a range match with dedup (no more permanently-missed notification); `date_start`/`date_end` search≠display fixed; the seniority 4-day-gap heuristic is replaced by calendar working hours. Template whitelist is 8 fields (`hr.contract.type` → `hr.employee.type`). **New:** `hr.employee.create.version.wizard` bulk server action, `fixed_term` + ContractEndDialog, `compute_sql` on `version_id`. 20 of 22 links repointed, 2 dropped; 55 links now. **Pre-20 summary retained (v19 terms):** no hr.contract in v19: employee = timeline of `hr.version`; `version_id` is a computed non-stored delegate (context `version_id` → form edits a historical version); `current_version_id` stored compute refreshed by daily cron (staleness window); version in force = `date_version` → next version −1, clipped by contract dates; **`create_version` silently discards values if a version exists at that date**; contract = version with `contract_date_start` (no state field; date-derived is_current/past/future); **contract-date writes fan out to all sibling versions**; templates = employee-less versions through a 7-field whitelist (custom fields dropped unless whitelisted); calendar stored on version, synced to resource **current-version-only** (historical calendars invisible to direct reads); tz not versioned; `_get_calendars(date)` requires in-contract else falls back to today; departure fields on version copy=False; employee-side attendance (misnamed `hours_last_month` = month-to-date, Python-only overlap constraint, auto-checkout/absence crons, value-match overtime link) and planning (roles live on resource, allocated-hours caps, payroll bridges are UI shells); gotchas incl. import-time today() defaults, search≠display on date_start/end; **+2026-07-12 self-service reality**: base.group_user has no hr.employee ACL (RedirectWarning → hr.employee.public; "My Profile" = res.users dialog via SELF_READABLE_FIELDS) — own-records features must live on the custom model, never as employee-form pages | 2026-09-22 |
| `hr` + `hr_holidays`/`hr_attendance`/`hr_recruitment`/`hr_expense`/`hr_payroll`/`hr_appraisal`/`knowledge`/`hr_contract_salary`/`geo_payroll` — HR Access Rights (Who Sees What) | [`hr_access_rights.md`](hr_access_rights.md) | **Reviewed against Odoo 20 (2026-09-22)** — **only ~1/3 survived.** Conceptual frame held (six managers, layer order, 5-step diagnosis ladder); mechanisms did not. **Platform change: `ir.model.access` and `ir.rule` no longer exist as models — merged into `ir.access`** (one row = grant + domain, `operation` = `crud` subset), so the layer table and every ACL/rule link were rewritten. **Also changed:** approver auto-fill is now reassign-only (an empty `leave_manager_id` stays empty; `attendance_manager_id`/`expense_manager_id` computed the same way); `hr.employee` multi-company rule lost its three OR-branches; `SELF_READABLE_FIELDS` replaced by `related_employee_field()` + `user_writeable=True`; contract dates moved `group_hr_manager` → `group_hr_user` and `contract_wage` is gone from `hr.employee`; Time Off/Attendance grants moved off `base.group_user` onto revocable `group_hr_holidays_employee`/`group_hr_attendance_own`; new `group_hr_payroll_officer` with Python-gated payslip validation, old payroll user renamed "Assistant" and now implies Time Off Officer; appraisal rule now `cru` with a `child_of` branch. **Risk removed upstream:** recruitment's database-wide `mail.message` read rule is deleted. 20 of 23 line links were wrong; 71 links now, all verified. **Migration blocker flagged in-doc:** modules still shipping `ir.model.access.csv` will not install on 20.0. **Pre-20 summary retained:** the 4 layers (menu groups → ACL → record rules → field groups) and why rules only ever widen access; **the six different "managers"** (`parent_id`, `leave_manager_id`, `attendance_manager_id`, `expense_manager_id`, appraisal `manager_ids`, department `manager_id`) and which app each one drives; what a plain `base.group_user` already reaches (Directory via `hr.employee.public`, everyone's absences in the leave calendar — type/reason hidden by field groups, own attendance, own work logs) vs `hr.employee` having **no** plain-user ACL (My Profile = `res.users` whitelist); Officer-vs-Administrator split on private data vs contract wage; `leave_manager_id` auto-syncs from `parent_id` **only until hand-edited** and auto-grants Time Off Responsible; Time Off Officer **implies HR Officer** (the classic accidental over-permission); `leave_validation_type` decides the approver, not the person; department-manager access exists only on `hr.leave.report`; payroll is all-or-nothing (no per-team payslips); geo_payroll work logs follow `parent_id` (deliberate divergence from time off); Recruitment Officer carries **database-wide `mail.message` read**; offer split HR=employee-offers / recruiter=candidate-offers + Alta terminal `accepted` state, tokenized candidate URL, expiry cron recipients; Knowledge ignores HR groups (`internal_permission` inherited + member overrides); 5-step "X can't see Y" diagnosis ladder + rollout checklist | 2026-09-22 |
| `resource` core — Calendar & Interval Math Engine | [`resource_calendars.md`](resource_calendars.md) | Complete — `hours_per_day` = week hours ÷ distinct weekdays (**half-days count as full weekdays** → deflates day math); flexible calendars: hours_per_day/week manually entered caps, synthetic rolling-7-day interval fill (**≠ planning's locale-week caps — two week conventions**); fully flexible (no calendar): work-days data = 0, leave = whole period; `full_time_required_hours` **self-resets to company calendar on recompute**; interval engine (attendance/leave/work batch, lunch excluded by default, third tuple element = source rows carrying duration_days for pro-rata day counting); two-week calendars use absolute week parity (not ISO); `transfer_leaves_to` moves only future leaves | 2026-07-11 |
| `hr_work_entry` + `hr_attendance`/`hr_holidays`/`hr_holidays_attendance` + `hr_payroll` — Work Entries / Time Rules | [`work_entries.md`](work_entries.md) | **Rewritten for Odoo 20** — `hr.work.entry` is **removed**: no table, no state machine, no conflict engine, no generation cron, no regeneration wizard. Every `hr_work_entry_*` bridge plus `hr_payroll_holidays`/`hr_payroll_planning` is deleted (0 tracked files on 20.0), so planning is no longer a payroll source; `hr.leave.type` is merged into `hr.work.entry.type`. Two layers replace it: (1) `hr.time.rule` reclassifies real `hr.attendance`/`hr.leave` records **in place** via `hr.time.rule.source.mixin` — it mutates the source and spawns child records, so overtime is now a real attendance row, not a shadow record; (2) `hr.version.generate_work_entries()` returns transient vals dicts at payslip/export time and **persists nothing**. `work_entry_source` → `attendance_based`; `is_leave`/`is_work` → `count_as`; numeric type codes (`002.00`/`006.00`/`000.00`); `WORK100` hardcode → `res.company.attendance_work_entry_type_id`. Attendances knock out the whole scheduled day. Validation = attendance state + `source_stale` + `hr.payroll.warning`. Includes a v19→v20 mapping table, rule anatomy, pipeline walkthrough, payslip consumption, security, gotchas | 2026-09-22 |
| `hr_attendance` + `hr_work_entry` | [`attendance_work_entry.md`](attendance_work_entry.md) | Complete — attendance→work entry flow, timezone split, same-day archive problem, overtime ruleset setup, payslip integration, Reset vs Regenerate Overtimes; + corrected rule model (quantity vs timing modes, all timing_type options, tolerances, rate combination max/sum, `work_entry_type_id`/Salary-Rate are enterprise-only), two seeded default rules, `_update_overtime` trigger; + **the `if ruleset:` gate** — versions with `ruleset_id = NULL` (common in demo/imported/migrated data, 45/51 blank in a demo DB) get zero overtime regardless of attendance, the #1 "Extra Hours = 0" cause | 2026-06-29 |
| Accounting Migration Guide | [`accounting_migration.md`](accounting_migration.md) | **Verified against Odoo 20 (2026-09-22)** — trial balance import, AR/AP open items, inventory, fixed assets, banks, lock dates, opening entry mechanics, `matching_number` `I*` import reconciliation, SQL validation queries, official xlsx templates, error-message table. 20.0 deltas: **hierarchical COA (`parent_id`, `account.group` gone, codes optional, dashes allowed)**; **creating a bank/cash account auto-creates its journal**; `reconcile` is now a bank-reconciliation flag only; `account_opening_date` defaults to today; **Validate and Post button** on the opening-balance list; **inventory valuation defaults to periodic** (`res.company.inventory_valuation`/`cost_method`/`inventory_period`, closing entry + cron); **assets split into `account.asset` + `account.asset.variant` + `account.depreciation.model`** (import `model_id` and `import_account_depreciation_*`, not `method*`); `residual_at_date` | 2026-09-22 |
| **Certification Exam — All Modules** | [`certification_exam.md`](certification_exam.md) | 98 questions across 16 modules — Inventory, MRP, POS, HR, Spreadsheet, Accounting, Timesheets, Project, Knowledge, eCommerce, Website, Marketing, CRM, Survey, Sales, AI, Studio, Purchase | 2026-03-07 |
| `account_asset` — Fixed Assets | [`account_asset.md`](account_asset.md) | **Rewritten for Odoo 20 (2026-09-22, verified against 20.0 source)** — opens with a 19.0→20.0 break table. Covers the new **two-level model** (`account.asset` header + `account.asset.variant` schedule, context-selected `variant_id`, auto-split create/write); `account.depreciation.model` replacing `state='model'` templates (auto-generated names, config freeze once used, `company_dependent` journal/ledger accounts, 5 pre-loaded models); **multi-ledger depreciation** (`account.journal.group` = Ledger, ledger variants posting only the difference, `_mirror_main_variant_moves`, recovery account, New Depreciation wizard, close-with-main rules); 4 methods incl. **`no_depreciation`** plus `method_mode` duration/rate and the `_get_rounded_duration` display trick; prorata modes + the 30-day formula; board mechanics and `auto_post='at_date'` via `account.ir_cron_auto_post_draft_entry` (the module ships no cron); bill auto-creation now **always create+confirm** driven by `account.depreciation_model_id` + the two asset accounts (`create_asset`/`multiple_assets_per_line`/`asset_model_ids` all removed; `asset_fixed` only); per-line model override and `_auto_update_asset`; pause/resume day math, sell/dispose entry composition, re-evaluation (gross-increase child) and the new **Activate Depreciation** flow (`origin_asset_id`/`derived_asset_ids`); non-deductible tax capitalization; `already_depreciated_amount_import`; asset groups (**no menu in 20.0**); corrected menu paths; full field/method/constraint tables and the new `ir.access` security matrix | 2026-09-22 |
| `account_accountant` + `account_reports` — Deferred Expenses/Revenue | [`deferred_expenses_revenue.md`](deferred_expenses_revenue.md) | Complete — Balance Sheet/P&L/Cash Flow/Budget/Tax connections, expense+revenue mirror, on_validation vs manual entry structures, analytic propagation, lock date, audit trail, account type requirements, mixed entry constraint, all 8 config fields, report exclusion logic | 2026-03-10 |
| `account` + `account_budget` — Fixed Costs Guide | [`accounting_fixed_costs_guide.md`](accounting_fixed_costs_guide.md) | **Verified against Odoo 20** — all **19** account types, **parent accounts replace account groups**, tax repartition + account tags, reconciliation (partial/full/multi-currency), 5 lock date types (+ the 20.0 reset-does-not-unlock rule), year-end filtering mechanics, payment terms + due dates, **6** journal types, analytic distribution JSON + multi-plan, budget states + drill-down, **budget vocabulary: Commitment / Liquidation / Achieved / Theoretical**, committed amount, budget vs P&L connection | 2026-09-22 |
| `account_accountant` — Deferred Revenue | [`deferred_revenue.md`](deferred_revenue.md) | Complete — revenue-focused standalone: journal entries, sign logic, liability holding account, config, report handler, generation modes, VAT exclusion, credit notes, common mistakes | 2026-03-10 |
| `stock_account` + `stock_landed_costs` — Stock Valuation | [`stock_valuation.md`](stock_valuation.md) | Complete, **verified against Odoo 20 source 2026-09-22** — 3 costing methods (Standard/FIFO/AVCO) with algorithms, Anglo-Saxon vs Continental accounting, purchase price difference, perpetual vs periodic valuation, COGS computation chain, landed cost allocation, lot-level valuation, dropship/return flows, closing process, value priority chain, security, 9 business walkthroughs, code-level traces. **Biggest v20 change:** valuation *configuration*, COGS generation, price differences and periodic closing moved up into `account` (`_create_cogs_lines`, `_get_price_difference_lines_vals`, `action_close_stock_valuation`) — `stock_account` now only overrides the hooks (`_use_inventory_valuation`, `_get_cogs_value`). Also: `move.value` is **signed**; `_run_fifo` split into `_run_fifo`/`_get_fifo_value`/`_get_fifo_stack` with negative stacks; new replay path `_set_value(recompute_date=)`; outgoing moves now valued **after** `super()._action_done()`; landed costs no longer restricted to FIFO/AVCO (new `apply_on_product_ids` targeting); `purchase_stock/models/account_invoice.py` deleted; `_get_move_directions()` is a dead stub | 2026-09-22 |
| `board` + `spreadsheet_dashboard` — Dashboards | [`board_dashboard.md`](board_dashboard.md) | Complete — two dashboard systems (legacy board + spreadsheet), data models, storage (ir.ui.view.custom XML vs binary JSON), frontend OWL components, drag-drop layout, add-to-dashboard flow, spreadsheet dashboard groups/shares, HTTP routes, sample data mechanism, 4 customization approaches (UI pin, spreadsheet editor, XML data records, custom board view), access control, enterprise edition features | 2026-03-14 |
| `mrp` + `mrp_workorder` + `maintenance` — Manufacturing & Maintenance | [`mrp.md`](mrp.md) | Complete — MO lifecycle, work orders, BOMs (normal + phantom), operations, work centers, scheduling/planning, component consumption, backorders, enterprise shop floor/quality, configuration settings, equipment categories, maintenance requests (corrective/preventive), MTBF/MTTR, work center blocking, maintenance teams | 2026-03-19 |
| `account` + `account_inter_company_rules` + `account_reports` — Multi-Company vs Branches | [`accounting_multicompany_branches.md`](accounting_multicompany_branches.md) | **Verified against Odoo 20 (2026-09-22)** — Part 1: branches vs multi-company architecture, shared vs separate CoA, inter-company rules, consolidation reports, analytic accounting, pros/cons. Part 2: cross-module impact (Sales, Purchase, Inventory, Invoicing, Expenses, Payroll), **security model rewritten for `ir.access`** (one CSV; row with a group = permission OR-ed, row without = restriction AND-ed; same `in` / `parent_of` / `+[False]` domains), permissions matrix, end-to-end SO-to-payment flow, pitfalls, decision framework. 20.0 deltas: explicit `_compute_company_id` ancestor rule; account tree + auto bank-journal creation; company-level accounting defaults via `company_default_for`; **`intercompany_user_id`, `intercompany_warehouse_id`, `intercompany_receipt_type_id` removed** (SUPERUSER_ID + `intercompany_sync_delivery_receipt`); **new Interco Comparison report for elimination work**; `account.tax.unit` detail | 2026-09-22 |
| `equity` — Cap Table & UBO | [`equity.md`](equity.md) | **Reviewed against Odoo 20 (2026-09-22)** — model intact; ~70% of prose held. `_table_query` → `_table_sql`; two Send buttons collapsed into one `action_transaction_send`; mail rewritten around a new `equity.mail.compose.message` wizard (per-recipient tokenised portal button, replacing the `hide_recipients` hack); security moved to unified `ir.access.csv`; `ubo_birth_date` → `birth_date`; license LGPL-3 → OEEL-1; portal rule bug fixed (`user.partner_id.id`, was `user.id`). **New in 20.0:** multi-company (`company_id` related through partner, `_check_company_auto`, rules allowing `company_id = False`) and one-currency-per-company enforcement (`equity_currency_id` writable on the first transaction only). Two pre-existing errors corrected: UBO managers DO get delete, and the Equity Currency config row pointed at a field invisible in the partner view. 17 of 25 links repointed. **Pre-20 summary retained:** 4 transaction types (issuance/transfer/exercise/cancellation), security classes (shares/options), SQL-view cap table (ownership/voting/dilution), valuations, UBO (9 control methods + 8 auth roles), portal access, NO accounting integration (standalone module), security groups | 2026-09-22 |
| `account` — Off-Balance Sheet | [`off_balance_sheet.md`](off_balance_sheet.md) | Complete — what off-balance accounts are, isolation rules (all-or-nothing, no taxes, no reconciliation), real-life use cases (guarantees, leases, lawsuits, pledged assets), comparison table vs other types, report treatment, how-to setup/record/reverse, edge cases | 2026-03-23 |
| `account` — Currency Exchange & Transit Accounts | [`currency_exchange_transit.md`](currency_exchange_transit.md) | Rewritten — v19-verified, community vs enterprise payment behavior, `payment_account_id` empty by design in enterprise, 3 real-case examples with journal entries, v19 internal transfer changes (auto-pairing removed), exchange difference mechanics | 2026-03-23 |
| `ai` (+ ~20 satellites) — LLM Integration Layer | [`ai_module.md`](ai_module.md) | Complete — provider system (OpenAI + Gemini only), LLMApiService dispatch, `ai.agent`/server-action/computed-field pipelines, RAG via pgvector, full Claude/Anthropic gap analysis (API shape diffs, embedding problem, hard-coded OpenAI surfaces), MVP integration plan | 2026-04-14 |
| `ai_anthropic` — Claude Integration Plan | [`ai_anthropic_integration_plan.md`](ai_anthropic_integration_plan.md) | Triple-verified implementation plan (3 review passes, external critique applied) — full patch map, 4 architectural decisions, Anthropic API translation table, corrected lifecycle rules (BOTH PROVIDERS append and LLMApiService patches must live at import time in `__init__.py` — `post_init_hook` doesn't survive cold boots), structured-output `stop_reason` gating, callable-Selection validation mechanics, break-nothing guardrails, 9-phase roadmap, risk register, runtime hot-path matrix | 2026-04-24 |
| `employee_registry_rs` — RS Employee Registry Sync | [`employee_registry_rs.md`](employee_registry_rs.md) | Complete — target-status derivation from `hr.version`, 1-step auth with token cache, daily cron, manual suspend, foreigner payload, country code mapping via `GetCountries`, sync log | 2026-04-17 |
| `rs_einvoice` — RS E-Invoice (SOAP) | [`rs_einvoice.md`](rs_einvoice.md) | სრული (ქართულად) — 10 სტატუსის სრული ცხრილი (RS-ის ოფიციალური დასახელებებით), 8 ღილაკის ქცევა, 6 გამოყენების სცენარი + 4.1 უარყოფის reopen-flow + 4 სელერ-მხარის auto-cancel სტატუს `7`-ზე (state='cancel', არა CN) + cancel-rejection 6→2/8 detection + post-cancellation lockdown, 4 k_type, შესწორების ჯაჭვი, atomic Request Edit flow, pending-changes hash, always-refresh-first pattern, before/after snapshots, **სრული SOAP API ინტეგრაცია**: WSDL/Endpoint, action→method mapping, change_invoice_status target-status სემანტიკა, save_invoice_desc/g_unit/drg_amount წესები, k_invoice ჯაჭვი, 49 მეთოდის კატალოგი, PDF vs Live ტიპები, Python workflow ნიმუშები (Send + Request Edit), API-დონის silent-failure gotchas | 2026-05-23 |
| `rs_einvoice` — ღრმა ტესტ-ცხრილი UC #1–7 | [`rs_einvoice_deep_tests_uc1-7.md`](rs_einvoice_deep_tests_uc1-7.md) | კრიტიკული edge cases ყველა UC-ზე (1=სრული გაუქმება, 2=ნაწილობრივი, 3=ფიზიკური დაბრუნება, 4=კომერციული ჟესტი, 5=tax-only/wrong-tax, 6=supplier delta import, 7=down-payment offset) — 35+ ცალკე ტესტ-შემთხვევა code:line რეფერენსით; sub-cent residuals, NBSP key collisions, FX drift, k_type misclassification, advance/offset distinction, tax-only chain block, supplier delta corner cases, orphan recovery, race-conditions; 10-bug triage list (B1-B10); read-only SQL audit queries | 2026-05-11 |
| `rs_einvoice` — Method Reference (every method) | [`rs_einvoice_method_reference.md`](rs_einvoice_method_reference.md) | Complete (English) — exhaustive per-method reference for ALL ~224 methods across 13 Python files + the OWL widget. For each method: purpose, what-it-does (step-by-step for complex ones), when/why used, calls (deps), called-by (every caller incl. buttons/crons/JS), side effects, returns. Front matter: architecture (one `account.move` split across 5 files + `rs.soap.service` SOAP client + supporting models + wizards + cron model), full RS_STATUS/K_TYPE/skip-reason tables, button→method + cron→method maps, per-user credentials config, security, dependencies. §12 appendix consolidates every dead-code path, silent failure, hard-coded value, mid-flow commit, and permissive success check — all caller claims grep-verified. Built by 9 parallel analysis agents; + attachable-advance eligibility rules (live-verified: prior-month-only + buyer-confirmed-only, FutureDate limit) + advance guards (eligibility fail-fast, settled-advance correction lock, carry-over reconciliation, MIN_DRG floor) — **STALE: superseded by [`rs_einvoice_method_reference_2026-06-08.md`](rs_einvoice_method_reference_2026-06-08.md); documents pre-refactor code** | 2026-06-02 |
| `rs_einvoice` — Method Reference (CURRENT, every method) | [`rs_einvoice_method_reference_2026-06-08.md`](rs_einvoice_method_reference_2026-06-08.md) | Complete (English) — fresh 10-agent exhaustive per-method reference: ALL ~223 method entries across 13 Python files + the OWL widget + views/crons/security/manifest, verified by a 3-way name diff vs. live code. Front matter: architecture (one `account.move` split across 5 files + `rs.soap.service` client + `rs.einvoice.cron` + 6 wizards), rs.ge status machine, the 4 flows (seller send / correct-and-reissue / cancel-and-reissue / buyer pull-accept-reject-import), ~22-of-54 SOAP-op coverage, orphan-recovery + row-lock + always-refresh-first safety patterns, k_type-as-metadata, and a verified DRIFT report (24 methods removed + new cancel/correct-reissue engines, native advance settlement, stranded-cancellation recovery, live-data viewer added since Jun-2) | 2026-06-08 |
| `eapi.rs.ge` — RS E-Invoice REST API სპეციფიკაცია | [`eapi_rs.md`](eapi_rs.md) | სრული სამუშაო მინიშნება ახალი მოდულისთვის — ბაზური ინფორმაცია, JSON სტრუქტურა, შეცდომების სისტემა (HTTP + API codes), სრული ავტორიზაცია (one-step/two-step/device-pairing), ზოგადი მეთოდები (4), INVOICE ობიექტი ყველა ველით, ქვე-ობიექტები (GOODS/RETURN/ADVANCE/SUB_DIST), INV_CATEGORY × INV_TYPE მატრიცა, ლაიფ-საიკლი (state machine), დოკუმენტის მეთოდები (Save/Activate/Delete/Cancel/Refuse/Confirm + bulk + List), დამხმარე მეთოდები (Goods/Excise/BarCode/SeqNum/Decl), 6 ტიპური სცენარი, Odoo მოდელის რეკომენდაციები, **rs_base ავტორიზაციის შეფასება** (85% mapped, 4 ხარვეზი ჩამოწერილია) | 2026-04-27 |
| `ai_accountant_cockpit` (custom) — AI Accountant | [`ai_accountant_cockpit.md`](ai_accountant_cockpit.md) | **Phase 5/8** — OWL chat UI shipped: fresh `ChatView`/`ChatMessage`/`ChatInput` components, JSON-RPC controller (`/providers`, `/sessions`, `/start`, `/messages`, `/send`), client action `ai_accountant_cockpit_chat` wired to "New Chat" menu + "Open Chat" button on session form. Phases 0–4 done: provider/session/message/query.log models, SQL guard, protocol, DeepSeek adapter, orchestrator loop. 124 tests passing (121 unit + 3 live DeepSeek gated). Strict JSON-contract: NEED_DATA / INTROSPECT / CLARIFY / FINAL / ERROR. Namespace `accountant_cockpit.*` (avoids `ai.*` reserved by enterprise). | 2026-05-02 |
| **Multi-Branch Cost Allocation — Atlas Group სცენარი** | [`multi_branch_cost_allocation_plan.md`](multi_branch_cost_allocation_plan.md) | **დაგეგმვის დოკუმენტი v3.2** (compressed — 1949 lines, 34% reduction) — TL;DR + 21 §0 corrections (P0 risks: credit note Mirror §0.17, distribution.model overwrite §0.14, `_create_analytic_lines` idempotency §0.13, `distribution_analytic_account_ids` computed §0.12, payroll merge §0.1), edge cases (§0.19, 16 cases), performance thresholds (§0.20). **Module 2 code v3.1** with credit note Mirror, null checks, zero-guards, archived/multi-company filters. **Test scenarios** (§10): 16 unit/integration/risk/perf tests in compact tables. **Backfill scripts** (§11): ORM Python. **Module manifests** (§9.4-9.5) with post_init_hook composite index. **Audit & logging** (§12), **rollback** (§13), **glossary** (§14, 22 terms), **decision log** (§15, 14 decisions). 11 risks (3 P0 + 5 P1 + 3 P2). Phase A=OPEX-by-Branch only. | 2026-04-28 (v3.2) |
| `account` + `sale` + `purchase` — Partner Default Taxes + Product "Not Taxed" (plan) | [`partner_taxes_plan.md`](partner_taxes_plan.md) | **IMPLEMENTED 2026-09-09 inside `gec_l10n_ge_tax` (user's call); code written, not run.** Plain summary for the user + full design: two company-dependent commercial fields on the contact (Default Sales / Purchase Tax) and a product Not Taxed flag, applied by one helper behind the three tax-proposal hooks (`_get_computed_taxes`, SO `_compute_tax_ids`, PO `_compute_tax_id`) with `super()` post-processing. Decisions: user chose literal Not Taxed (nothing proposed, manual add) and `VAT Exempt (Purchase)` as the non-VAT-vendor tax; fiscal position still remaps, commercial-field copy to children, partner-change recompute accepted by default; Q6 open (may the partner tax drop product WHT?). Dependency sweep, gaps (hr_expense, rs_base wizard, purchase_stock), build steps, acceptance matrix | 2026-09-09 |
| `basis_bank` (custom) — Basis Bank REST API | [`basisbank_api.md`](../basisbank_api.md) | Reference for `bis.bankonline.ge` — 8 endpoints, auth + 10-min session, Digipass OTP, batch all-or-nothing (ops 4/5/6) vs single per-line (op 7), 14 error codes with Odoo action mapping, GetReport schema, Odoo field-to-API mapping, security/redaction checklist | 2026-05-26 |
| `basis_bank` (custom, `custom_addons/gec_odoo_modules/basis_bank/`) — Module Reference | [`basis_bank.md`](basis_bank.md) | **Rewritten 2026-09-17 from full source + bank PDF + live `gec_new` data (19.0.1.15.0).** Send/import/confirm flow, decision points (processing mode, transfer type, computed endpoint, BIC-only journal detection), 4 scenarios, matching rules, Paid gating, 48 h cron. **Answers "does the statement import bring both inbound and outbound?" — YES, proven three ways:** spec (`DebitAcc`/`CreditAcc` per item), code (`_amount_sign` +1/−1, only the auto-matcher filters `amount < 0`), live data (4 `GetReport` calls, 48 items = 39 in / 9 out / 0 unclassified; 34 imported lines = 32 positive / 2 negative, all created by the daily-sync cron method run via *Run Manually* as `__system__`; the cron itself has never been active anywhere). Resolves README open question: statement `Amount` is always a positive magnitude. **New finding:** upload reply reference (`I345`) and statement `BankInternalID` (9-digit) are different identifier spaces, so single-batch payments cannot auto-confirm by bank id; `DocNum` echo still unverified. Two byte-identical copies on disk (`gec_odoo_modules` and `gec_basis_bank_integration`). Historical: the original 2026-05 plan/handover is [`../basis_bank.md`](../basis_bank.md) (root; states and paths differ from what shipped). 2026-07-12 Codex payroll audit warning still applies: with `batch_payroll_move_lines=True` a shared move can expose slips outside the intended subset. | 2026-09-17 |
| `basis_bank` (custom) — Safety Audit | `AUDIT.md` — **file no longer on disk in either module copy (checked 2026-09-17)**; surviving backlog is [`KNOWN_ISSUES.md`](../custom_addons/gec_odoo_modules/basis_bank/KNOWN_ISSUES.md) + [`README_DEV.md` §17](../custom_addons/gec_odoo_modules/basis_bank/README_DEV.md) | Integration-safety audit (7-dimension parallel review + adversarial verification against v19 framework source + bank spec). **33 confirmed findings: 1 critical, 12 high, 20 medium**, plus 8 critic gaps; 108 verified-sound mechanisms; 1 refuted. Verdict: not yet safe for real money — architecture sound but edges leak. Critical: standard **Validate** button fakes a send (posts + marks sent, no bank call, no guard). High: double-pay on crash/timeout/re-batch/late-execution/cross-currency, false confirmation (no direction check), 48h guard self-disables on non-reconcilable outstanding account, `allow_out_payment` trust bypass, reconciled-reset ledger split (README "payable reopens" is false). Includes remediation order. | 2026-06-13 |
| `account` + `account_online_synchronization` — Bank Journal Multi-Currency Setup | [`bank_journal_multicurrency_setup.md`](bank_journal_multicurrency_setup.md) | Q&A + setup guide for one-bank-many-currencies — the 3-layer model (res.bank / res.partner.bank / account.journal), correct vs wrong dialog (manual wizard vs online sync `+ Add XYZ` that never dedupes), why duplicates appear, dropdown display-name trap (`name - bic`), how to fix existing duplicates, currency-must-be-set-before-posting rule | 2026-05-26 |
| `id_ge_sign` (custom) — Sign with Georgian ID Card (QES) | [`id_ge_sign.md`](id_ge_sign.md) | Complete — **document mode** (v19.0.3.x): the `geoeid-unitool://` desktop app downloads the whole PDF, draws its own visible block, signs and uploads it back; launch→init→document→submit→status flow; PAdES verification reusing `cryptography`/`asn1crypto` (no new deps); **multiple card signers** (incremental stacked signatures, completed = last signer's PDF); zero-typing UX (auto-filled signature field kept in the PDF as the card-app placement guide — the card stamps the cert identity, not the Odoo name; public-link signer auto-created with synthetic `@id-card.invalid` email, name from cert); manual multipart re-parse for the app's unquoted `=` boundary; trust-anchor/QES gating. Includes a **Design Journey** section analysing the hash→document-mode switch and every fix. Scope: PAdES Baseline-B; LT/LTA, qualified TSA `DocTimeStamp`/OCSP deferred | 2026-06-05 |
| `hr_recruitment` + `website_hr_recruitment` + `utm` — Applicant Sourcing (Source / Medium / Campaign) | [`hr_recruitment_sourcing.md`](hr_recruitment_sourcing.md) | **Reviewed against Odoo 20 (2026-09-22)** — the attribution model is unchanged, the plumbing under it was rebuilt. **`utm.source.mixin` deleted** (303 refs in 19.0 → 0), so `hr.recruitment.source` declares its own `source_id` (required, `ondelete='restrict'`, defaulting to LinkedIn) and typing a name no longer springs a `utm.source` into existence. **`medium_id` now defaults to Social Media, not Website** — a tracker URL built without touching that field tags every applicant Social Media. New fourth tracked field `utm_reference` (cookie `odoo_utm_reference`); `_fetch_or_create_utm_medium()` → `_utm_ref` + `SELF_REQUIRED_UTM_REF`, which also widens `utm.source` delete protection from Referral-only to 9 core sources; alias name keys off `source_id.name`; job alias defaults `user_id` → `recruiter_id`. `utm/models/ir_http.py` is byte-identical, so the 31-day host-scoped cookie story stands unchanged. All 37 links repointed. **Pre-20 summary retained:** Complete (focused) — how `applicant.source_id` actually gets filled: **no inference logic anywhere**, only two mechanisms — the `odoo_utm_source` cookie (set by any page hit carrying `?utm_source=`, host-wide, 31 days) read at create time via `utm.mixin.default_get` through the ORM's `_add_missing_default_values`, and per-recruitment-source email aliases whose `alias_defaults` hardcode source/medium=email/campaign=Job. Covers the auto-creation of unknown `utm.source` names, the salesman-group skip in `default_get`, tracker-URL generation, and the full list of paths that leave source empty (job's own alias, `hr.job.platform` forwarders, Monster/job-board integrations, backend manual create); `utm.source` vs `hr.recruitment.source` and their ondelete rules | 2026-09-22 |
| `hr` (+ `hr_recruitment`, `website_hr_recruitment`, enterprise `hr_contract_salary`) — Job Positions & `department_id` | [`hr_job_positions.md`](hr_job_positions.md) | **Reviewed against Odoo 20 (2026-09-22)** — the thesis holds (job department ≠ employee department; an empty department still lets duplicate names through, and `hr.job` does not use the `NULLS NOT DISTINCT` that 20.0 `models.Constraint` now supports). Three corrections, two of which were wrong in 19.0 too: the **"department manager is auto-subscribed" claim was false** (`manager_id` is an untracked related `hr.employee` and `hr.job` has no `user_id`; its real effect is an unsubscribe guard), **`work_email` seeding on Create Employee was removed** (only `work_phone` survives), and `_compute_company` now uses the department's company only when it matches the job's. `user_id`/`contract_type_id` → `recruiter_id`/`employee_type_id`. New consumer of `department_id`: website **JSON-LD** (`occupationalCategory`). All 14 anchors repointed. **Pre-20 summary retained:** Complete (focused) — `hr.job.department_id` is optional, manual, decoupled from the employee's department; full consumer map of what the job's department drives (recruitment applicant dept + company fallback, email alias defaults, department manager, Create-Employee vals, website "Other" bucket, department KPIs, group-by, salary offer) and what it does NOT do (never sets employee dept, no headcount effect, uniqueness not enforced when NULL — Postgres NULLS-distinct trap) | 2026-09-22 |
| `account` core + `sale`/`purchase`/`product`/`point_of_sale` (+ enterprise `account_reports`) — Taxes (Complete Guide) | [`taxes.md`](taxes.md) | Complete — full picture of how taxes work in **Odoo 20**, built by a parallel research workflow + adversarial completeness/accuracy critic. Covers: the `account.tax` engine; computation methods (percent/fixed/division/group/python); price-included vs excluded + back-computation; repartition lines + tax grids (+/- tags); tax groups + invoice totals; fiscal positions (tax/account mapping, auto-apply, EU OSS); product customer/vendor taxes + company defaults; full Sales (SO→invoice) and Purchase (PO→bill) flows; the GL side (base vs tax lines, `tax_repartition_line_id`, move-line tax details); cash-basis exigibility (CABA); withholding; configuration + rounding (round-per-line vs round-globally, HALF-UP verified — not banker's) + multi-company; POS taxes (shared engine, session posting); non-deductible / multi-currency / EDI & credit-note special cases; tax report & **closing entry** (community stores the tax-group accounts, enterprise `account_reports` generates the move); tax lock dates; **per-partner tax FAQ** (added 2026-09-09: a contact has no tax field — its fiscal position is remap-only and cannot add a tax; sale/purchase/withholding selection paths; localization pattern for partner-driven positions). Withholding section corrected 2026-09-10: the vendor-bill worked-example table had Outstanding and Payable both on the debit side (core docstring is the customer-invoice mirror), plus a new **gross-vs-net** subsection — the posted payment's `amount` is the gross settled amount, the net is only in the liquidity line and in the register wizard's `withholding_net_amount`. Every claim links to source. **Verified against Odoo 20 source 2026-09-22:** new per-document `document_tax_mode` (Tax Incl./Excl. now frozen on the move/SO/PO, so `account_price_include` is a plain default again and `_check_set_account_price_include` is gone); the tax-group payable/receivable/advance accounts **moved to enterprise `account.return.type`**, so the closing `RedirectWarning` now targets the Tax Return Type form and all taxes consolidate onto one counterpart; `base_vat` **removed** (VAT checks folded into `account`, VIES → `l10n_eu_account_vies`); `is_withholding_tax_on_payment` → `is_withholding_tax` plus invoice-side Net Due Amount and a Withhold-and-Pay selector; `deductible_amount` (0-100) → `deductible_percentage` (0-1) — an off-by-100 trap; cash-basis transition-account Python constraint removed; POS session closing entry is now an `out_invoice`/`out_refund`, so `_accumulate_amounts` is gone; `show_update_fpos` removed from `sale.order`. Every claim links to source. | 2026-09-22 |
| `hr_payroll_account` + `account.payment.register` + all 25 `l10n_*_hr_payroll_account` — Payslip Payment Flow & NET Reconciliation | [`payroll_payment_flow.md`](payroll_payment_flow.md) | Complete (7-agent sweep, adversarially verified, 2 claims corrected) — the stock Pay-button chain (NET-reconcilable guard at hr_payslip.py:283 = stock enterprise; button sweeps ALL move lines; wizard keeps payable/current-type lines with nonzero residual; batch key partner+account+currency; paid flips ONLY if every line hits zero residual during the wizard run — never re-fires); why the guard exists (payment must close an open item; also blocks double bank booking); full 25-module l10n survey table (majority pattern = earnings debit-only / deductions sign-flip / NET credit-only to reconcilable payable; BE+CH never use the button — batch_payroll_move_lines forced, SEPA files; CH = the only stock gross-clearing precedent incl. no NET mapping; stock bugs in KE/US mappings); GEC gross-payable design = Swiss family paired with the wrong payment flow — 1210 experiment hits the guard, 3160 experiment yields 5/3 junk payments (transit + pension-interim sweeps, ± pair is 3130-vs-3160 not same-account); 3 hidden gaps: pension members can never auto-flip paid while 3181/3182 reconcilable (handoff Edit C insufficient), journal-16 payments are entry-less (no outstanding account → nothing reconciles at wizard time), NET credited 1210 while payment journal defaults 1211; options table; **DECIDED 2026-08-05: Option B standard net-payable layout** (accountant rejected 3160 → NET payable = 3130 itself; handoff Edits A/B/C superseded); code shipped in geo_payroll 19.0.1.8.0, hr3 UI realignment + 3181/3182 checkbox + cleanup pending; 6 accountant questions; hr3 garbage inventory | 2026-08-05 |
| `hr_payroll` + `hr_work_entry_attendance/planning` + l10n patterns — Wage Types (fixed/hourly/daily + combinations) | [`payroll_wage_types.md`](payroll_wage_types.md) | Complete (2 parallel sweeps + direct source verification) — the only 2 standard wage types (monthly/hourly) and the full computation chain (work entries → worked-days `_compute_amount` → BASIC=paid_amount); `schedule_pay='daily'` = 1-day payslips trap; work_entry_source calendar/attendance/planning; **no timesheet→payroll bridge exists**; scheme matrix for fixed/hourly/daily + combos (only fixed+hourly-overtime is standard); daily-wage dev pattern (selection_add on both models + `_compute_amount` override, 6 l10n precedents incl. HK daily wage); 3 combo designs (rule-layer / per-work-entry-type rate routing / Swiss `l10n.ch.swiss.wage.component` multi-component); 11-item gotcha catalog (one-version-per-payslip no mid-month proration, fixed-wage dilution via `is_extra_hours` denominator, flexible-calendar hours_per_day=0, ruleset NULL → no overtime, no l10n_ge payroll); + **§8 design decision (24-agent workflow, adversarially verified, 0 refuted)**: chosen "B+ hybrid" (`daily` selection_add on both models + `paid_at` routing on work entry type + filtered-super `_compute_amount` + own day derivation + shipped OT data pack), 3-design judge scoring, 9-step roadmap, 12 additional CONFIRMED gotchas (display-grade `number_of_days`, attendance→hardcoded WORK100, default OVERTIME pays 0.00, sum-mode ruleset double-count, attendance edit wipes week of OT approvals, tamper-lock only on default_struct_id, source-switch no-op, binary `=='hourly'` consumer branches, mid-month switch = unpaid half-month); **+2026-07-12**: §2 one-rate-per-slip proof (monthly slips never read `hourly_wage` — independent second rates require code, 429.55-vs-630 example); §7 gotcha 12 schedule-flavor outcomes (flexible = quota-pay for hourly, fully-flexible = silent zero payslip / all-or-nothing monthly); §7.7 rewritten (day-based entries + `payslip.env` rule pattern w/ UAE precedent); **new §9 worked examples** — full schedule×wage×source walkthroughs with arithmetic (fixed/flexible/fully-flexible cases, entry tables → payslip lines), the three payslip money surfaces (Worked Days = readonly projection / Inputs / Computation), Worked Days tab per scheme stock-vs-module amount tables (480-vs-500, 429.55-vs-630), timesheet-paid flow incl. pure-timesheet structure (presence unpaid, leaves still pay) and the late-validation boundary; **+§8.3 plan double-check (2026-07-12, 4 verification agents, file:line-confirmed)**: `ondelete='set default'` crashes at load (use `'set monthly'`), employees can't open hr.employee (self-service must live on the custom model), `edited` doesn't stop compute_sheet, rule-context `worked_days` is a plain dict, recompute bug = bound method vs string keys, hr_payroll_expense claim/release precedent, conflicts block batch-only, `contract_wage` depends gap, dashboard warnings = data records, timesheet-create requirements | 2026-07-12 |
| `geo_payroll` (custom, `custom_addons/gec_payroll_types/`) — How Salary Is Calculated | [`geo_payroll.md`](geo_payroll.md) | **Learner-first rewrite** — what's standard Odoo vs what the module adds (daily wage type, overtime, daily/timesheet pay, settlement ledger); the core "two money channels" model (Channel A base pay from work entries → BASIC; Channel B add-ons from work logs/timesheets → ALW, never work entries); one Pay Scheme selector → `wage_type` + flags (6 schemes); how each base wage type is priced (fixed proration, hourly, daily-from-logs) + the fixed-wage/attendance trap; add-on formulas frozen into the ledger; the work-log lifecycle (file → approve+freeze → claim → pay); six worked examples; payslip assembly (BASIC+ALW→GROSS, NET=GROSS since no tax layer); guards grouped by intent; **mid-period version changes: one full-period payslip per version slice, OUT-prorated, version coverage gate, leaf-first correction-chain lifecycle (rework 2026-07-13, suite run pending)**; **Hourly-from-Timesheets base mode SHIPPED 2026-07-14** ("nothing else pays": work entries/leave/holidays/OT logs = 0; `TIMESHEET_BASIC` on all structures; routing guard + manager waiver wizard; per-day 24h aggregate cap — closed a known limitation); **Unit Work (piecework) SHIPPED 2026-07-14 (19.0.1.3.0)**: Per Unit + Daily + Per Unit schemes, global `hr.payroll.unit.rate` catalog (manager-managed, employee-readable, currency-match approval guard), Unit work logs frozen at approval incl. UoM snapshot, `UNIT_BASIC`/`UNIT_EXTRA` on all 4 structures for carry-forward; **not-done-yet: cross-source overlap, separation of duties**. Disabled calendar-Daily mode in an appendix. Companion: [`geo_payroll_implementation_plan.md`](geo_payroll_implementation_plan.md) — §13 = the locked spec, marked IMPLEMENTED; **+2026-07-15 accounting gap analysis** (§10, corrected same day after live-DB check): zero of 40 rule records across all 4 structures have `account_debit`/`account_credit`/tax grids in either `codex_geo_payroll_tests` or `gec_modules_hr`; `hr_payroll_account` is uninstalled in the former but **is installed** in `gec_modules_hr` with `journal_id` already set on 3/4 structures (GEO Unit Salary still missing one); none of `gec_modules_hr`'s companies use the `gec_localization` "ge" chart_template (they're on stock `generic_coa`/`us`), so `7410`/`3130` don't exist there — corrected mapping uses the real per-company codes (`630000`/`611000` expense, `230000` payable); candidate PIT/pension accounts (`3325`, `3180`-`3183`) only apply if a company ever adopts the `ge` template; §10 now also carries the concrete per-rule `ge`-chart mapping (earnings→debit `7410`, NET→credit `3130` reconcile-true, GROSS unbooked, future PIT/pension → `3325`/`3182`/`7411`+`3181`) and the "don't hardcode company-dependent accounts in XML — use `_configure_payroll_account_ge` in a bridge module" rule; future tax-layer table now tax-law-validated (PIT base = gross − employee 2% → 19.6% effective, participant-conditional rules, interim accounts exist because withholding crystallizes at *payment* per Art. 154 — accrue to `3325`/`3181`/`3182`, settle at payment; state pension share never booked; grids permanently empty — rs.ge declaration is per-employee detail, not GL grid totals); field-level gotcha verified from engine + UAE precedent: negative DED rules put the liability in the **Debit Account field** (sign-flip books it as credit; wrong field = silent Adjustment Entry), employer 2% = stock COMP category with both fields; **TAX LAYER SHIPPED 19.0.1.4.0 (2026-07-15, suite run pending)**: `PENSION_EE`/`PIT`/`PENSION_ER` (seq 120/140/160) on all 4 structures — PIT 20% on (GROSS + DED-so-far), pension 2%+2% conditional on `pension_fund_member` (field MOVED from `hr_customization`, which now depends on `geo_payroll`; offer question keeps working via model+name field lookup); NET = 0.80×GROSS non-member / 0.784×GROSS member; suite expectations rescaled + new `test_tax_rules.py`; accounts AUTO-CONFIGURED same day (user rejected bridge module): `hr_payroll_account` now a hard dep of geo_payroll, `_configure_payroll_account_ge` + `<function>` tag re-apply journal+accounts to every ge-chart company on each upgrade (no-op elsewhere); booking pattern = **Georgian gross-payable** (user override of stock l10n pattern): earnings Dr `7410`/Cr `3130` at gross, `PENSION_EE`/`PIT` pull out of `3130` (liability in Debit field + `3130` in Credit field), `PENSION_ER` `7411`/`3181`, NET books NOTHING (accounts force-cleared) — accepted cost: per-slip Pay button dead, pay via accounting/run Mark-as-Paid, Basis Bank export needs retest; also fixed pre-existing `groups_id`→`group_ids` crash in waiver views (core rename); **19.0.1.5.0 (Codex-review fixes, same day)**: PIT self-contained (derives pension exclusion inline, immune to future DED rules), empty timesheet-project allowlist on additive schemes = ALL projects pay (pure ts-mode still requires explicit list), configurator now virgin-companies-only (any existing account/journal → whole company skipped, manual edits survive upgrades; code changes no longer propagate to configured companies), +5 tests (PIT-vs-LOAN, journal per-account balances, empty-projects claim, configurator skip); ACCEPTED as-is: live pension flag (no effective dating), dead Pay button; PENDING accountant: Employer Cost = NET+ER instead of GROSS+ER; ACCOUNTANT CORRECTION applied in code: PIT books to `3320` Income Tax Payable directly (was `3325` interim — now unused by payroll; pension legs confirmed unchanged); hr3 itself needs the 4 PIT rules' Debit Account switched in UI (configurator skips configured companies by design); accountant sheet round: EE/ER interims confirmed as-built (sheet columns were transposed), PENSION_ER expense switched to `7490` Other Tax Expense per accountant (7411 unused; hr3 UI: 4 more rules), sheet is GROSS-priced (1000→784, cost 1020) = shipped math; pricing question CLOSED as gross (accountant + user confirmed: wage 30 = gross, cost 30.60, net 23.52; PIT base = gross − employee 2% only — matches shipped formula; the brief all-in/÷1.02 idea is obsolete); **19.0.1.6.0 (2026-07-23, suite run pending)**: work-item salary lines now show real quantities — `gec_component_line_vals()` sets `result_qty` = claimed settlement qty (hours/days/units) + per-unit amount on all 7 component rules x 4 structures; total stays settlement-exact (engine multiplies unrounded unit amount back), zero-qty falls back to 1.00; adversarial review caught + fixed an Edit-Payslip-Lines-wizard corruption (wizard rebuilds totals from rounded amount x qty → `action_edit_payslip_lines` override reseeds work-item lines as total x 1); new test_line_quantity.py (10 tests); BASIC/GROSS/tax lines stay qty 1.00; result_rules['amount'] for the 7 codes is now per-unit (gotcha in README 2.10); **19.0.1.7.0 (2026-07-23, suite run pending)**: edited-slip claim-loss hazard CLOSED — compute_sheet refuses edited slips holding active claims (recompute would release all claims with no re-claim path = silent pay loss), `_gec_check_unclaimed` no longer exempts edited slips (new `_gec_in_claim_scope` = can-claim minus edited; edited slip with eligible unclaimed items cannot validate — cancel+replace or waive), `_gec_settlement_still_valid` keeps valid claims of edited slips (edited != stale); automated recompute paths already excluded edited slips; new test_edited_slip_claims.py (8 tests); README error guide now 95 raises; **19.0.1.8.0 (2026-08-05, suite run pending)**: booking switched BACK to stock net-payable layout (gross-payable transit retired — it fed junk payments to the Pay button/Basis send and blocked auto-paid, see payroll_payment_flow.md): configurator now maps earnings/PENSION_EE/PIT debit-only + NET credit 3130 (reconcilable, employee partner via employee_move_line now load-bearing); test_journal_entry_per_account_balances re-asserted (1020 debits, NET partner check); README §1.10/§2.8 rewritten; **19.0.1.9.0 same day**: fingerprint-guarded migration realigns already-configured ge companies on upgrade (rewrites only rules still matching the old shipped mapping incl. PIT 3325→3320, ER 7411→7490, NET 1210/empty→Cr 3130; custom values untouched) — no manual UI realignment needed; accountant checkbox DROPPED — user rejected type change (Payable accounts cannot be un-reconciled, core constraint); **19.0.1.10.0 same day (suite run pending)**: NET-only payment filter in code (AU-superannuation precedent): action_register_payment override swaps wizard active_ids to NET-account lines (falls back to stock when NET has no account), new account_payment_register.py flips slip to paid when NET lines settle (stock all-lines-zero would deadlock on open pension interims), new test_payment_flow.py (2 tests: active_ids targeting, single 784.0 payment + paid flip + interims untouched); companion basis_bank 19.0.1.12.0: _basisbank_send_salaries passes only _basisbank_net_payable_lines (handoff Edit A; Edits B/C obsolete); **19.0.1.24.0 (2026-08-28): art. 82 income-tax exemption categories** — `hr.payroll.tax.exemption` (8 seeded noupdate=1 records, batch employee m2m, Payroll→Configuration→Salary menu, no new group), all 5 PIT rules exempt the employee's calendar-year GROSS up to the category limit via `gec_pit_taxable_gross` (max limit wins, crossing month taxes the excess only, pension never exempt) | 2026-08-28 |
| `geo_payroll` (custom) — Unit Work (Piecework) Extension Plan | [`geo_payroll_unit_work_plan.md`](geo_payroll_unit_work_plan.md) | **IMPLEMENTED 2026-07-14 (19.0.1.3.0, code written, suite not yet run)** — shared Claude+Codex implementation spec for two new Pay Schemes: `unit` (Per Unit: base = approved Unit work logs × frozen catalog price) and `daily_unit` (Daily + Per Unit). New global `hr.payroll.unit.rate` catalog (name/uom/price, no company_id, manager-managed, currency-match refusal at approval); Work Log type `unit` (work_unit_id + unit_qty, freeze-at-approval, label-only UoM — v19 has no uom categories); components `unit_basic` (BASIC) / `unit_extra` (ALW) on all 4 GEO structures for carry-forward; new `unit` wage type + GEO Unit structure, worked-day zeroing + stale-BASIC guard extension; settlement gains frozen `uom_id`; ledger/correction/version-split machinery explicitly untouched. §12 = settled cross-review corrections (do not re-litigate) | 2026-07-14 |
| `geo_payroll` (custom) — Benefits & Deductions Extension Plan | [`geo_payroll_benefits_plan.md`](geo_payroll_benefits_plan.md) | **IMPLEMENTED 2026-08-22 (now 19.0.1.15.0, suite not run)** — central `hr.payroll.benefit` program model (batch employee assignment, one-time vs monthly, company/employee/both amounts) feeding payslips via `hr.payslip.input` injection (`_compute_input_line_ids` extension, marker field, idempotent, scope table for regular/refund/correction/Benefits slips); 2 input-reading rules (BONUS ALW / BENEFIT_DED post-tax DED) on all 4 GEO structures; **manager confirmed semantics via scenarios 2026-08-25** (company amount → gross, taxed; employee amount → post-tax net deduction) and the `taxable` flag + BONUS_NT/BENNT machinery was **removed same day in 19.0.1.15.0** (always-taxed rule, deleted pre-deployment); **SALARY ADVANCES added 19.0.1.16.0–.21.0 (2026-08-26/27, §16)**: two-phase, both in payroll, rebooked 19.0.1.21.0 to the accountant's 4-account prepaid split — pay-out payslip books ONLY assets (net → `1730`, prepaid pension/PIT/pension-ER → `1731`/`1733`/`1732` against their liabilities, no expense, nothing in GROSS; `pay_mode` picks with-salary vs separate Benefits slip; validation stamps the line), then later-month salary slips accrue FULL salary and close the prepaids with 4 mirror legs (net Cr `1730` + liability closings); new `ADV` category in all NET formulas; `1731/1732/1733` added to gec_localization CoA (additive CSV rows + created-if-missing per company); stateless input-ledger installment tracking (one per correction chain), slip deletion/cancel self-heals; R7 cap and C2/C5 termination/storno deliberately manual; separate-payment mode via off-cycle "GEO Benefits Pay" structure whose type has NO default_struct_id (load-bearing: is_regular=False escapes every overlap/coverage/claim guard — adversarially verified 6-agent/37-claim review, §14); PIT/pension automatic via GROSS linearity; MANDATORY accounting config + migration (§9 — separate flow dead without it); phase 2: in-kind fringe gross-up, salary advances | 2026-08-22 |
| `geo_payroll` (custom) — Manual Testing Guide (step by step) | [`geo_payroll_testing.md`](geo_payroll_testing.md) | Beginner, click-by-click manual test playbook for the `gec_modules_hr` DB — explains the 4 core terms first (Pay Scheme vs Wage Type vs **Structure Type** vs Structure, and what `structure_type_id` does: it picks which rules run + fixes the wage type, auto-copied onto the payslip); one-time setup (upgrade to 19.0.1.1.0, test as payroll manager, throwaway employee + schedule); the repeatable 5-step routine (set contract → add inputs → new payslip → Compute Sheet → read Worked Days vs Salary Computation tabs); per-case steps + expected GROSS for all six schemes + overtime; how to file/approve a work log and add a payable timesheet; guard/negative tests (attendance trap, no-route approval refusal, frozen-after-approval, double-payslip block, self-approval block); Case 2b Hourly-from-Timesheets (zero work-entry pay, config-gate refusals, waiver flow, 24h/day block); troubleshooting table; read-only SQL to verify BASIC+ALW=GROSS | 2026-07-14 |
| `hr_timesheet` + `timesheet_grid` + `project_timesheet_holidays` — Timesheets & `account.analytic.line` | [`hr_timesheet.md`](hr_timesheet.md) | **Reviewed against Odoo 20 (2026-09-22)** — two sections described code that no longer exists. **Project profitability is gone** (`_get_profitability_items`/`_get_items_from_aal` removed; zero files in `project`/`sale_timesheet` even mention profitability) — replaced by the embedded **Analytic Items** action and the new `analytic_profitability` selection, which files a timesheet line under **Loss**. **Server-side timers are gone**: `timesheet_grid` no longer inherits `timer.mixin`, the timer is client-side in `localStorage` and writes no `timer.timer` record. Code moved `models/hr_timesheet.py` → `models/account_analytic_line.py`, so every link was broken. `hourly_cost` is now a core `hr` field (the `hr_hourly_cost` module is gone), on the employee **Settings** tab. Leave-generation skip test moved from `time_type == 'other'` to `work_entry_type_id.count_as == 'working_time'`; leave lines are now `_is_readonly()`. New Timesheet Assistant (`aw.rule`) creation path. **The "no native payroll link" claim survives but its evidence died** — `work_entry_source` no longer exists. 32 links verified. **Pre-20 summary retained:** Complete (focused) — a timesheet = analytic line with `project_id`; all 6 creation paths and their exact triggers (manual, grid timer, leave validation via sudo `_generate_timesheets` with `holiday_id`, public-holiday `global_leave_id`, FSM timers, planned geo_payroll OT bridge); the money mechanics (`amount = −unit_amount × employee.hourly_cost` recomputed in `_timesheet_postprocess_values`, analytic-only cost on the project account — never payroll); enterprise validation + `last_validated_timesheet_date` lock; consumer map (profitability, sale invoicing, reports — payroll reads nothing, verified); gotchas (leave-line sweep-up, hours-always storage, stale historical costs) | 2026-09-22 |
| `hr_timesheet` + `timesheet_grid` + `project_timesheet_holidays` + `timesheet_grid_holidays` — Timesheets, Validation & Locks (payroll-first) | [`timesheets.md`](timesheets.md) | **Verified against Odoo 20 source 2026-09-22** — a timesheet is an `account.analytic.line` with `project_id`; creation paths including the new **Timesheets Assistant** (`aw.rule`) that replaced the v19 timer (`timer.mixin` is gone from the analytic line); `hr_hourly_cost` module removed, `hourly_cost` now ships in `hr`; `ir.access` security model, with enterprise deactivating the community rule rather than stripping it; enterprise validation + `last_validated_timesheet_date` rolling lock — approvers can now **self-validate** through a new OR-branch that also bypasses the `date <= today` and `validated` guards, `timesheet_manager_id` no longer auto-defaults, and the lock escape window is per-user timezone (`context_today`); leave bridge now keyed on `work_entry_type_id.count_as` (`hr.leave.type` is gone) and **silently skips fully-flexible or timezone-less employees**; public-holiday lines now do have a write guard. Confirms again that `hr_payroll` reads nothing from timesheets. Overlaps [`hr_timesheet.md`](hr_timesheet.md) — different angle, same cluster | 2026-09-22 |
| `account_reports` (enterprise) — Tax Returns & Period Locking (`account.return`) | [`account_returns.md`](account_returns.md) | **Verified against Odoo 20** — return types + `category` (tax return / audit), periodicity resolution (type `deadline_periodicity` → company `account_return_periodicity`, default **monthly**), rolling card-generation window + branch-own-VAT rule, deadline fallback to `account_return_reminder_day`, state workflows + check templates/approvers, `action_validate()` → `_proceed_with_locking` (no `bypass_failing_tests` any more; sequential guard, posts closing entry, sets `tax_lock_date = date_to`, revokes lock exceptions, computes amounts to pay), **closing accounts moved from `account.tax.group` to `account.return.type`** (one settlement triplet per return), **reset no longer rolls the tax lock back — it refuses while the lock stands**, lock-date family table, GE DGV specifics + the two blockers stopping `gec_l10n_ge_tax` on 20 (tax-group fields removed; core `l10n_ge` now claims `@template('ge')`) | 2026-09-22 |
| `base` (`ir.mail_server`) + `mail` (`mail.alias.domain`, `mail.alias`, `fetchmail.server`, `mail.mail`, `mail.blacklist`) — Mail Configuration (Complete Guide) | [`mail_configuration.md`](mail_configuration.md) | Complete — built from a 4-agent source sweep (outgoing/aliases/incoming+bounces/config-UI). Covers the whole mail stack: **Alias Domain** as the config hub (catchall/bounce/default-from, company derivation, multi-company, seeding); **outgoing servers** (`ir.mail_server` fields, encryption/auth, CLI fallback, personal-server throttling); **From resolution** (`_find_mail_server` order + `from_filter` matching rules + **encapsulation** `"Name" <notifications@domain>`, with the Gmail-relay worked example — relay rewrites `From` to the authenticated account); **Reply-To=catchall vs Return-Path=bounce** + owner-vs-target gotcha; **outgoing queue** (`mail.mail` states, hourly cron, grouping, force-send limit, no auto-retry, failure types); **incoming** (`fetchmail.server` types/OAuth, 5-min auto-toggled cron); **gateway routing** precedence (bounce→reply→alias→fallback→catchall) + loop detection; **aliases** (`alias_contact` policy + bounce-on-reject, mixin, project/crm examples); **bounces** (no VERP in v19 — embedded Message-Id; counters, reset, mass_mailing auto-blacklist, `mail.blacklist`); **notifications** (`notification_type` email/inbox, recipient groups + access-button rules, email branding); config UI map; DNS/SPF/DKIM/DMARC notes; ICP reference table; symptom→cause troubleshooting. Every claim links to source. | 2026-06-26 |
| `auth_oauth` + `auth_signup` + `auth_ldap` (+ `microsoft_*`) — Azure / Microsoft Entra ID Integration | [`azure_entra_sso.md`](azure_entra_sso.md) | Complete — **no Azure connector exists**; Entra is wired as a hand-configured `auth.oauth.provider`. Covers: implicit flow (`response_type='token'`, no id_token validation), exact Entra app-registration + Odoo provider values, the mandatory `auth_oauth.authorization_header` param (Graph rejects query-string tokens — #1 silent failure); identity anchored on `sub`→`oauth_uid`, **not email**; the two supported linking paths (invitation token, or manual OAuth UID) and why uninvited OAuth signup only ever makes a **portal** user from the portal template; what Entra genuinely controls (assignment-required, Conditional Access, MFA, audit) vs what it does not (**no SCIM, no group mapping, no deprovisioning, local password fallback stays live, sessions survive Entra revocation**); LDAP alternative has real auto-provisioning but Entra ID has no LDAP endpoint (needs Entra Domain Services or on-prem AD); scope of a custom module for real lifecycle control; unrelated `microsoft_outlook`/`microsoft_calendar` Graph modules; gotchas incl. pairwise `sub` orphaning on app re-registration and the `b2c` invitation-scope exposure | 2026-08-11 |
| `hr_customization` (custom, Alta) — Candidate Offer Accept/Reject + HR Notifications | [`hr_customization.md`](hr_customization.md) | Complete (all 27 files + base-source verification) — replaces sign flow with tokenized Accept/Reject for applicants: locked server-side terms, personal-info validation (ID doc, bank dropdown, pension flag), archived version+employee snapshot, terminal `accepted` state (no refuse/delete), recruiter/team-leader/lawyer mails; contract+trial expiry cron (7-day window, per-date markers); knowledge welcome-article mail on user create; public job Responsibilities. **Known issues:** confirmed data-loss path (sibling-offer refuse/delete → `unlink_archived_versions` nukes accepted version+employee), duplicate expiry mails per version row, minor multi-company/config edges. **+2026-09-21:** Email Templates section — all 6 templates (1 candidate-facing override + 4 internal + welcome), Python-injected recipients, the `show_offer_details` one-template-two-audiences split, non-`noupdate` upgrade wipe | 2026-09-21 |
| **rs.ge Form №30 — Monthly Withholding Declaration Export (Plan)** | [`rs_form30_declaration_plan.md`](rs_form30_declaration_plan.md) | **PLAN — implemented 2026-09-02 as `gec_income_tax_report` (trimmed v1.0, see row below)**; original design: module `gec_rs_form30`: declaration+line models, 3 sources (payslips → ხელფასი rows via GROSS+ADVANCE_PAY/PIT+ADV_PIT_PRE math with art.82 relief inversion; `account.payment.withholding.line` → dividend/interest/royalty/rent/services rows with GEL-from-move-lines + rate=ABS(tax.amount); manual rows), classifier data from `30_data_7_202505.xlsx` (18 categories, 15 types, 127-combo rate matrix, RS country codes via `employee_registry_rs.rs_country_code`), 14-column SpreadsheetML import file (sheet `data`, all strings, portal computes pension+tax cols), partner `rs_form30_category_id` + tax `rs_form30_income_type_id` mapping fields, 4 new WHT tax records (stipend/services/rent/0%-treaty), CIT-wizard dividend WHT moved to payment step (D3), pre-flight validation + GL control + unmatched-payments helper; decisions D1–D6 + portal verifications V1/V2; ≈6–7 dev days. Companion domain reference: [`../sashemosavlo_deklaraciebi_sruli_cnobari.md`](../sashemosavlo_deklaraciebi_sruli_cnobari.md) | 2026-09-01 |
| `gec_income_tax_report` (custom, `custom_addons/gec_odoo_modules/gec_income_tax_report/`) — rs.ge Form 30 annex export | [`gec_income_tax_report.md`](gec_income_tax_report.md) | **2026-09-17 (19.0.1.14.0, run on hr3 through the shell):** `Explanation Workbook` header button downloads one xlsx (8 sheets: Read me, Annex A with base / portal tax / withheld / cells fed / source links, Payslips, Payments with the category-on-date source and the annex routing reason, Annex D, Annex E, Main part with kind / status / formula, Cell details = the trace) built by `models/rs_form30_declaration_xlsx.py` from the same `_summary_values(trace)` as the per-cell page, stored in the new Binary `explanation_file`; `RsForm30Explain._formulas` / `_cell_state` shared; doc §10. **2026-09-17 (19.0.1.13.0, source, not run):** per-cell explanation page — question-mark button on a main-part row opens `rs.form30.explain` with the formula in words, the stored value, its kind and every contributing row or journal item with its arithmetic and a link; the repeated glossary header and the unreachable all-cells mode were removed (`cell_code` required) and the organization list moved into the `{orgs}` placeholder of the cell 39/56 formulas; `proposed` cells follow a `proposed_value` shadow (refreshed until the accountant edits them), not the earlier fill-only-while-empty rule. **2026-09-15 (source, authoritative copy `custom_addons/gec_odoo_modules/gec_income_tax_report`, not run):** main part cells 16–69 derived — `_main_part_cells` routes every annex A row to its art. 134 cell, `_ledger_values` fills 58/59/63/67–69 from posted journal items and cash receipts, 57¹/57² from a new `hr.work.location` flag, `proposed` cells follow a `proposed_value` shadow; annex D (cars) + annex E (own-tax partners) + treaty allowance shipped in 1.12.0 (2026-09-14); doc §10. **2026-09-13 (source, not yet upgraded on hr3):** `rs.form30.country` classifier, partner `rs_form30_country_id` and the 1.9.0 nationality↔residency sync REMOVED; residency = contact `country_id`, rs.ge code = `res.country.rs_form30_code` (235/251, `data/res.country.csv`), country without code blocks export; history `date_from` default removed. **source 19.0.1.11.0 (2026-09-05, installed on hr3)** — declaration + line models, 4 classifier models loaded from `30_data_7_202505.xlsx` (18 categories, 15 types, 262 countries, 152 period-valid rate rules), rows from paid payslips (GROSS+ADVANCE_PAY, PIT+ADV_PIT_PRE, art.82 relief inversion, `paid_date` cash basis) and `account.payment.withholding.line` (base/amount converted at payment date, income type from tax), manual rows kept on regenerate, matrix validation before export, 14-column SpreadsheetML file; partner `rs_form30_category_id`/`rs_form30_country_id`, tax `rs_form30_income_type_id` (data `<function>` maps the 15 GE WHT taxes on every update); 1.2–1.4: read-only OWL lines grid `rs_form30_grid` (client filters, resizable columns, page-only totals), `ka_GE` translation, translated `warning_display`; open: manual rows/edits have NO UI path while the grid is read-only (guides still describe editing), grid HttpCase test is weak, portal probe, tests unrun; 1.7.0: matrix constraint on lines (category/type/rate outside the period's rs.ge matrix is never stored, generation aborts listing every offender); 1.8.0: residency derived partner field → partner country → employee nationality → Georgia, 235/262 country codes mapped to res.country from core payment ISO table; 1.9.0: employee Nationality ↔ contact Form 30 Residency two-way write sync (hr.version/res.partner/hr.employee overrides, data function fills empties); 1.10.0 (Codex): per-contact category history `rs.form30.partner.category` (inclusive Valid From/To, resolved by `paid_date` / `payment.date`, legacy `rs_form30_category_id` only while no history exists, gap → blank category + warning, export blocked); 1.11.0: Codex cleanup (totals once per render, batched country lookups, company locked once lines exist); **TRAP:** Valid From defaults to today, so a first history entry blanks every row of the month being declared (seen live 2026-09-05; proposed fix = drop the default, undecided); no residency ↔ category cross-check (Georgian residency + 3.4 passes) | 2026-09-17 |
| **rs.ge Form №30 — full declaration gap analysis + plan (pages 1–6)** | [`rs_form30_full_declaration_gap_plan.md`](rs_form30_full_declaration_gap_plan.md) | **PLAN (Georgian), 2026-09-11** — built from the official Order 996 art. 38 instruction (2026-02-09), the 2025 pension-reporting guide and the portal screenshots; module 19.0.1.12.0 covers only page 2 (annex ა); portal computes main-part cells 16–57 from annex ა; missing: annex დ (cars, `fleet.vehicle` + 2 new fields), annex ე (non-withheld service payments to ი/მ etc., needs partner status + service-type classifier), main-part cells 58–69/571-572 (GL accounts 6110/1110/1120/1431 + declaration lines), employer 2% control total; **finding:** `geo_payroll` PIT rule deducts 2% of the *taxable* part while the portal formula (სვ7−სვ8−სვ9)×სვ11 deducts 2% of gross → 0.004×relief gap (slip 66's 24.00 explained); re-import duplicates rows (delete first); README §1.3 pensions.ge claim stale; 8 accountant decisions Q1–Q8; ≈6–7 dev days in 4 phases | 2026-09-11 |
| `hr_appraisal` (+ `hr_appraisal_skills`, `hr_appraisal_survey`) — Performance Reviews | [`hr_appraisal.md`](hr_appraisal.md) | **Reviewed against Odoo 20 (2026-09-22)** — ~60% held; state machine, three creation paths and sharing model survive. **Biggest change: Done is now a real server-side lock** — `_check_write_access()` raises `AccessError` on writes to a Done record and on an employee writing their own `assessment_note`/`note`/`manager_feedback_published`/`state`, closing the v19 RPC hole; new Reopen transition plus bulk Confirm/Done/Reset server actions. **Gotchas fixed upstream and deleted:** Final Rating no longer required for Done (`action_done()` is a bare state write); Skills Evolution report replaced entirely (`hr.appraisal.skill.report` deleted → `hr.appraisal.skill.evolution.report`, a SQL view over `hr_employee_skill` validity intervals = real time series that also catches non-appraisal skill changes); dead `_create_multi_appraisals` gone; record rule widened to `employee_id child_of user.employee_ids`. **Surviving gotchas:** no org-wide fixed-date cycle, confirm email only on Ongoing, Skills tab hidden from the employee during Ongoing, goals not snapshotted per cycle, no real PDF export. **Also:** both feedback publish toggles now default `False` (feedback starts private, the reverse of v19); campaign wizard lost its `mode` selector and reuses same-date appraisals instead of duplicating; Private Note redaction now returns `False`, not the string `"Note"`; new auto-install `spreadsheet_dashboard_hr_appraisal`. Upstream inconsistency left documented: the Skills Evolution help text still claims a latest-two comparison. 24 of 42 links corrected. **Pre-20 summary retained:** Draft/Ongoing/Done state machine, manual vs. automatic (per-employee, interval-based) vs. bulk-campaign (shared-date) creation paths, parallel employee/manager feedback with publish toggles (not approvals), meeting scheduling via standard Calendar, configurable Final Rating + server-side-redacted Private Note. Gotchas: no org-wide fixed-date auto-cycle, confirm email only fires on Ongoing (not Draft), Skills tab hidden from the employee during Ongoing and editable only by the manager, Goals have no per-cycle link (persist across cycles, not snapshotted), Skills Evolution report only diffs latest-vs-previous cycle, no real PDF export, dead `_create_multi_appraisals` action | 2026-09-22 |

| **VAT Declaration — annex "a" parts I–III — build plan** | [`vat_declaration_annex_a_plan.md`](vat_declaration_annex_a_plan.md) | **PLAN, 2026-09-21** — built from the accountant's `დღგ დანართი ა.xlsx` (3 sheets) + 3 portal screenshots; target module `gec_income_tax_report` per user instruction, with the objection recorded (`gec_l10n_ge_tax` is the natural home — it owns the 15 `[GE-VAT]` tags, the DGV report and `ge_vat_return_type`; the income-tax module drags `geo_payroll` in). **Separate model and separate menu — the VAT declaration is NOT Form 30**: new `rs.vat.declaration` / `rs.vat.cell` / `rs.vat.line` with no inheritance, reuse by copy not by a `declaration_type` selection on the Form 30 models, and its own *Accounting → Reporting → VAT Declaration* menuitem (sequence 61) beside `menu_rs_form30_declaration` (60), not nested under it. Design = reuse the Form 30 main-part pattern: cell catalogue CSV + value row per cell + the proven three-way refresh (`computed` always follows / `proposed` follows until edited via a `proposed_value` shadow / manual never touched, `rs_form30_declaration.py:522`), values summed off `account.move.line.tax_tag_ids` so the DGV report and the declaration share ONE tax mapping. **Row-by-row source table for all 34 cells across the 3 parts**, classified AUTO (8) / CALC (10) / TYPED (16), rising to ~12 AUTO once 4 new tags exist (T1 diplomatic 172.3, T2 treaty project 172.5, T3 financial services, T4 immovable/land — no migration, so the first month needs a manual 14¹/14²/14³ reconcile). Column rules captured: part I/II `E = D × 18%`, part III `D = E + F`, part III code 4 = part II total, codes 6/6¹ = part I 6/6¹ × 18%. Files, 4-phase build order (phase 1 is usable on its own), and 7 open questions — **Q1 down-payment split must be verified against a real invoice in the DB**, Q6 fixed-asset split rule, Q7 whether a filing file is needed at all, block phase 2 | 2026-09-21 |

---

## External Legal Sources (Georgian tax law)

Converted source texts and analyses of the legislation our Georgian modules implement. These are **not** Odoo module docs — they are the law behind the code.

| Source | Analysis | Converted text | Status |
|---|---|---|---|
| MoF Order #996, 31 Dec 2010 — Tax Administration Instruction, **original (publication 0) text** | [`mof_order_996_2010.md`](mof_order_996_2010.md) | [`sources/mof_order_996_2010_ka.md`](sources/mof_order_996_2010_ka.md) (Georgian, 906 KB) | **2026-09-21** — 6 parts / 26 chapters / **107 articles read end to end** / ~90 annexes. Analysis covers: registration and the immutable TIN, tax secrecy, e-communication (unsigned e-document = signed paper, Art. 11(2)), tax demand and advance rulings, the personal accounting card and its per-tax / reverse-charge / two-excise-card design; waybill mechanics incl. **Art. 25(22) — a waybill is never a VAT or excise credit document**, the GEL 1m electronic-register threshold, the sealed simplified book, the 10% related-party safe harbour, and Art. 30 leasing depreciation with its 11 worked examples; the full VAT chain — death-of-payer registration from the date of death, the Art. 50 anti-abuse safeguard, the three invoice types and when the **special invoice replaces the waybill**, the two-row correction invoice, computer-invoice register defects blocking credit, the petroleum `ნსაფ` regime (series encoding, "wrongly filled = no legal force", journals, 72-hour form issuance, fuel-card discount spec), **reverse-charge credit arising when the bank debits (Art. 66)**, exemption-by-register-lookup (Art. 71), and credit unwinding (5% trigger, 1/5 over 5 years, 1/10 over 10 years, year-end boxes 20/21); excise declaration credits and the two-card transfer, stamp physics and euro nominal values, confiscation procedure, bunker-fuel bank guarantees; property tax's **two reference dates** (land at 1 April current year, other property for the past year) and the leased-out shadow valuation; gambling payment marks and the six measurement periods of the natural-resources fee. Also: a mapping of which rules still shape our `rs_*`, `gec_l10n_ge_tax` and `geo_payroll` code, **12 defects in the original text** (wrong annex cross-references in Arts. 24/54/62, wrong box references in Arts. 33/38, duplicate and missing paragraph numbers in Arts. 28/30/72/78/81), and a conversion-fidelity list. **WARNING: this is the text as adopted, effective 1 Jan 2011 — not consolidated.** The CIT half (Arts. 40–41) predates the 2017 Estonian model and the invoice/waybill chapters predate the electronic rs.ge services. For current law use the consolidated digest in the `georgian-tax-advisor` skill (`references/order-996-administration.md`, consolidated 24.04.2026) |

---

## Module READMEs (user guide + developer guide)

Each custom module below carries two READMEs in its own root folder (the folder holding `__manifest__.py`), rewritten from source on 2026-09-02/03: `README.md` for accountants, payroll managers and users, `README_DEV.md` for developers. Both use Mermaid diagrams (render on GitHub).

| Module | User guide | Developer guide |
|---|---|---|
| `gec_localization` (Georgian chart of accounts) | [`README.md`](../custom_addons/gec_extra_modules/gec_localization/README.md) | [`README_DEV.md`](../custom_addons/gec_extra_modules/gec_localization/README_DEV.md) |
| `gec_l10n_ge_tax` (VAT, withholding, DGV, CIT) | [`README.md`](../custom_addons/odoo_taxes/gec_l10n_ge_tax/README.md) | [`README_DEV.md`](../custom_addons/odoo_taxes/gec_l10n_ge_tax/README_DEV.md) |
| `geo_payroll` (pay schemes, work logs, Georgian taxes, benefits) | [`README.md`](../custom_addons/gec_payroll_types/geo_payroll/README.md) | [`README_DEV.md`](../custom_addons/gec_payroll_types/geo_payroll/README_DEV.md) |
| `gec_payroll_bank` (payroll to bank bridge) | [`README.md`](../custom_addons/gec_payroll_types/gec_payroll_bank/README.md) | [`README_DEV.md`](../custom_addons/gec_payroll_types/gec_payroll_bank/README_DEV.md) |
| `basis_bank` (Basis Bank transfers and statements) — module doc [`basis_bank.md`](basis_bank.md); two identical copies on disk, `gec_odoo_modules` is the one documented | [`README.md`](../custom_addons/gec_odoo_modules/basis_bank/README.md) | [`README_DEV.md`](../custom_addons/gec_odoo_modules/basis_bank/README_DEV.md) |
| `gec_income_tax_report` (rs.ge Form 30 annex export) — rewritten 2026-09-05, `README_USER.md` folded into `README.md`; interactive wiring map (v2, 2026-09-06: 73 methods, call graph with ORM reaches, 7 playable scenarios) at [`developer_map.html`](../custom_addons/gec_odoo_modules/gec_income_tax_report/developer_map.html), built by [`docs/developer-map/build_v2.py`](../docs/developer-map/build_v2.py) per [`DEVELOPER_MAP_BLUEPRINT.md`](../DEVELOPER_MAP_BLUEPRINT.md) | [`README.md`](../custom_addons/gec_odoo_modules/gec_income_tax_report/README.md) | [`README_DEV.md`](../custom_addons/gec_odoo_modules/gec_income_tax_report/README_DEV.md) |

---

## Documentation Standards

Each module file follows this structure:

1. **Overview** — what the module does in 3–5 sentences
2. **Dependencies** — required modules, optional integrations, what it provides to others
3. **Business Flow** — the end-to-end process with state transitions
4. **Key Models** — core data models with purpose and key fields
5. **Key Methods** — important business logic methods with file:line references
6. **UI Entry Points** — menus, views, wizards the user interacts with
7. **Configuration** — settings, groups, and options that change behavior
8. **Edge Cases & Gotchas** — non-obvious behavior, constraints, known limits
