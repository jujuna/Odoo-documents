# Odoo 19 — Tax / Account / Move Constraints Reference

> Verified against Odoo 19 source for the `gec_l10n_ge_tax` build. 158 constraints across 6 areas.
> Goal: every validation that can block chart load, tax save, or move posting — so we avoid breakage like the off_balance-in-journal-entry error.
> Paths are relative to the repo root (`addons/...`, `enterprise/...`).

## Top gotchas for `gec_l10n_ge_tax`

| Rule | How to avoid | Source |
|---|---|---|
| Receivable/Payable accounts MUST be reconcilable (reconcile=True) | Never set reconcile=False on a receivable/payable account. Note _compute_reconcile (664-672) auto-forces reconcile=True for these types, so only an explicit ove | `account_account.py:27-31 (_check_reconcile)` |
| Off-Balance accounts cannot be reconcilable | Keep reconcile=False on off_balance accounts. _compute_reconcile (671) already forces reconcile=False for off_balance, so only explicit override breaks it. This | `account_account.py:187-194 (_constrains_reconcile), raise at 192` |
| Off-Balance accounts cannot have taxes (tax_ids) | Clear tax_ids on off_balance accounts. There is also an @api.onchange('account_type') (851-854, _onchange_account_type) that auto-clears tax_ids=False in the fo | `account_account.py:187,193-194 (_constrains_reconcile), raise at 194` |
| Account codes must be unique within a company hierarchy (no duplicate across the recordset, parent, or child companies) | Pick codes unique across the whole parent/child company chain (not just one company). Note code uniqueness is enforced in Python, NOT via a DB _sql_constraint — | `account_account.py:1083-1116 (_ensure_code_is_unique, Check 2.1/2.2), raise at 1114-1115` |
| account_type is REQUIRED (no off_balance/empty allowed at storage) | Always provide account_type, or rely on _compute_account_type (603-606, precompute) which derives it from the nearest parent account by code, defaulting to 'ass | `account_account.py:44-70 (account_type field, required=True)` |
| account.account.tag: (name, applicability, country_id) must be unique — SQL constraint | Use a distinct name per (applicability, country) combination for account tags. | `account_account_tag.py:22-25 (_name_uniq Constraint)` |
| _check_repartition_lines: each document_type set (invoice list, refund list) must contain EXACTLY ONE base repartition line (repartition_typ | For every tax that has repartition lines, supply exactly one repartition_type=='base' line in the invoice set and exactly one in the refund set. The default _co | `addons/account/models/account_tax.py:550-551 (check), invoked at :564-565 from _validate_repartition_lines :554` |
| _validate_repartition_lines: invoice and refund repartition lists must have the SAME NUMBER of lines (len(invoice_repartition_line_ids)==len | Always add/remove repartition lines in matched pairs — one invoice line for every refund line. When generating repartition programmatically, build both document | `addons/account/models/account_tax.py:567-568` |
| _validate_repartition_lines: BOTH the invoice list and the refund list must contain AT LEAST ONE tax repartition line (repartition_type=='ta | Keep at least one repartition_type=='tax' line on each side. The default compute seeds one tax line per side; never strip all tax lines leaving only base lines. | `addons/account/models/account_tax.py:570-572` |
| _validate_repartition_lines: invoice/refund SYMMETRY. Walking both sorted lists by index (sorted by (sequence, id)), the line at each positi | Mirror the invoice and refund distributions exactly: same ordering (control via sequence), same per-line repartition_type, same factor_percent. If you change an | `addons/account/models/account_tax.py:574-580 (loop), sort at :562-563` |
| _validate_repartition_lines: POSITIVE factor sum. Sum of factor (=factor_percent/100) over invoice tax-type lines with factor>0 must equal 1 | Make the positive tax repartition lines' percentages add up to exactly 100 (e.g. a single +100% line, or +40%/+60%). Note this checks ONLY the invoice side's ta | `addons/account/models/account_tax.py:582-585` |
| _validate_repartition_lines: NEGATIVE factor sum. Sum of factor over invoice tax-type lines with factor<0 must equal -1.0 (i.e. -100%). Comp | For reverse-charge / negative-distribution taxes, ensure the negative tax lines total exactly -100% (e.g. one -100% line alongside the +100% line). has_negative | `addons/account/models/account_tax.py:586-588` |
| _constrains_name: tax name uniqueness. For taxes with type_tax_use!='none', the tuple (name, type_tax_use, tax_scope, country_id) must be un | Give each tax a name unique per (type_tax_use, tax_scope, country_id) within the company group. To reuse a name, vary tax_scope or country, or set type_tax_use= | `addons/account/models/account_tax.py:211-238 (search at :225, raise at :226)` |
| account.tax.repartition.line ACCOUNT DOMAIN: account_id is restricted to accounts whose account_type is NOT in ('asset_receivable','liabilit | Route tax repartition to income/expense or current-asset/current-liability tax accounts (e.g. tax payable/receivable accounts), never to AR/AP control accounts  | `addons/account/models/account_tax.py:4942 — domain="[('account_type', 'not in', ('asset_receivable', 'liability_payable', 'off_balance'))]"` |
| OFF-BALANCE HOMOGENEITY — THE EXACT CHECK YOU ASKED FOR. If ANY line of the move uses an account with account_type == 'off_balance', then EV | Never mix an off_balance account with non-off_balance accounts on the same move. If you route an account to off_balance type (per our gec rule of not flipping C | `addons/account/models/account_move_line.py#L1363 (method _check_off_balance, decorator+def at L1359-L1360, raise at L1364)` |
| OFF-BALANCE: NO TAXES. A line whose account is off_balance must have no tax_ids and no tax_line_id. | Do not attach taxes (input or output, base or tax line) to any line posting to an off_balance account. | `addons/account/models/account_move_line.py#L1366 (method _check_off_balance)` |
| OFF-BALANCE: NO RECONCILIATION. A line on an off_balance account cannot be reconciled (reconciled must be False). | Off_balance accounts must not be set reconcilable / must not be matched. | `addons/account/models/account_move_line.py#L1368 (method _check_off_balance)` |
| BALANCED ENTRY (debit = credit). For every move that has lines, ROUND(SUM(balance), company_currency.decimal_places) must equal 0. Computed  | Ensure debit total equals credit total in COMPANY currency. When building lines manually, set balance (or debit/credit) so they sum to zero at company-currency  | `addons/account/models/account_move.py#L2760 (single) and #L2766 (multi); method _check_balanced at L2748-2766; SQL in _get_unbalanced_moves at L2768-2788` |
| CASH-BASIS vs NON-CASH-BASIS SHARED TAGS. On a single move line, taxes exigible on_payment (cash basis) and on invoice cannot share repartit | Don't mix CABA and non-CABA taxes sharing the same report tag on one line. Workaround: use a tax group whose children carry the tag separately. | `addons/account/models/account_move_line.py#L1454 (method _check_caba_non_caba_shared_tags at L1416)` |
| Empty fiscal position strips ALL taxes. map_tax() returns an empty account.tax recordset when the fiscal position has no tax_ids; this remov | Never leave tax_ids empty on a fiscal position unless you intend to strip all taxes. If you only want to remap specific taxes, you must still list every tax tha | `addons/account/models/partner.py:154 (map_tax), the decisive branch at :157-158 `if not self.tax_ids: return self.env['account.tax']`` |
| Account-mapping rows must be unique per (position, src account, dest account). | Do not duplicate a src/dest account pair within one fiscal position. | `addons/account/models/partner.py:319-322 (_account_src_dest_uniq)` |
| Withholding-on-payment requires a NEGATIVE tax amount. The is_withholding_tax_on_payment flag is silently reset to False whenever amount >=  | Define withholding taxes with a negative amount (e.g. -10 for 10% withholding). This is an onchange, not a constrains — it fires in the UI/onchange flow; a dire | `addons/l10n_account_withholding_tax/models/account_tax.py:36-40 (_onchange_amount)` |
| Withholding-on-payment enabling forces tax_exigibility='on_invoice' and price_include_override='tax_excluded'. | Do not expect a withholding tax to be cash-basis or price-included; the onchange clears those. In code, set tax_exigibility='on_invoice' and price_include_overr | `addons/l10n_account_withholding_tax/models/account_tax.py:29-34 (_onchange_is_withholding_tax_on_payment)` |
| Withholding-on-payment tax amount_type cannot be 'group' or 'division'. (Note: this is a @api.constrains that raises UserError, despite bein | Use amount_type='percent' or 'fixed' for withholding-on-payment taxes. Note the prompt's phrasing 'must be percent' is imprecise: 'fixed' is also allowed; only  | `addons/l10n_account_withholding_tax/models/account_tax.py:42-47 (_check_amount_type)` |
| Withholding line base amount must be strictly above 0. | Ensure each withholding line has a positive base_amount; comparison uses comodel_currency_id.compare_amounts so it respects currency rounding. | `addons/l10n_account_withholding_tax/models/account_withholding_line.py:290-295 (_constrains_base_amount)` |
| Withholding line account cannot be a liquidity/transfer account. | Use a dedicated tax/base account (e.g. company.withholding_tax_base_account_id), never the bank/cash/outstanding/transfer accounts. The abstract base returns an | `addons/l10n_account_withholding_tax/models/account_withholding_line.py:297-302 (_constrains_account_id); liquidity set built in account_payment_withholding_line.py:82-94 and account_payment_register_withholding_line.py:97-109` |
| Withholding number (name) is required at payment build time when the tax has no withholding sequence. UserError raised before any sequence i | Either set a withholding_sequence_id on the withholding tax (account_tax.py:17-23) so a number is auto-generated (line.name = withholding_sequence_id.next_by_id | `addons/l10n_account_withholding_tax/models/account_withholding_line.py:363-366; consumers account_payment.py:120 and :167` |
| Withholding net amount cannot be negative (register payment wizard). | Keep the sum of withholding line amounts <= the payment amount. withholding_net_amount = amount - sum(line.amount). | `addons/l10n_account_withholding_tax/wizards/account_payment_register.py:204-205; net computed at :57-67` |
| Withholding base account: lines default account_id to company.withholding_tax_base_account_id; when that company setting is set, the per-lin | Set company.withholding_tax_base_account_id to centralize the WHT base account; otherwise each line needs a manually chosen valid (non-liquidity) account. Note  | `company field addons/l10n_account_withholding_tax/models/res_company.py:12-16; default at account_withholding_line.py:235-242 (_compute_account_id); grouping at :483; UI hide flags account_payment.py:60-67 and account_payment_register.py:160-167 (_compute_withholding_hide_tax_base_account)` |
| Withholding taxes are excluded from normal tax computation unless explicitly requested. | To compute withholding amounts you must pass calculate_withholding_taxes=True when building the base line (as the withholding line prep does at account_withhold | `addons/l10n_account_withholding_tax/models/account_tax.py:70-79 (_add_tax_details_in_base_line)` |
| Withholding line tax_id domain enforces matching type_tax_use and is_withholding_tax_on_payment=True; the available-taxes domain also enforc | Only withholding taxes whose type_tax_use matches the payment direction are selectable (outbound payment -> purchase taxes, inbound -> sale; inverted for refund | `field domain addons/l10n_account_withholding_tax/models/account_withholding_line.py:47-52; domain builder :430-435 (_get_withholding_tax_domain maps outbound->purchase, inbound->sale); refund inversion in register wizard account_payment_register.py:112-119` |
| Account xmlid resolution during deref. In _load_data.deref_values, every many2one / non-numeric integer string value, and every SET / LINK / | Every account/tax/fiscal-position/group xmlid referenced in a template CSV or @template dict must be DEFINED (have its own row in the same data set or already e | `ValueError raised at odoo/addons/base/models/ir_model.py:2241 (_xmlid_lookup). Caught for m2o/scalar at chart_template.py:589-595 (warning at 594); NOT caught for SET at 605-608, LINK at 609-610, m2m-string at 611-616. self.ref/company_xmlid at chart_template.py:1213-1223.` |
| Command.create appending repartition lines -> 'exactly one base line' violation. account.tax._validate_repartition_lines / _check_repartitio | To update an EXISTING tax's repartition without duplicating, you have two correct mechanisms. (1) On FRESH creation in the same batch, rely on delay(): chart_te | `raise at addons/account/models/account_tax.py:551 (_check_repartition_lines 546-551); constraint _validate_repartition_lines account_tax.py:553-588 (calls _check_repartition_lines at 564-565). Related companion errors: 'same number of lines' 568, 'at least one tax repartition line' 572, 'should match (same percentages, in the same order)' 579, 'total factor (+) equals to 100' 585, 'total factor (-) equals to 100' 588.` |
| Receivable/Payable accounts must be reconcilable; the template often loads such accounts. Also off_balance accounts cannot be reconcilable o | In the account CSV, set reconcile=True for any account with account_type asset_receivable or liability_payable. Do not set reconcile=True or tax_ids on off_bala | `addons/account/models/account_account.py:31 (_check_reconcile 27-31); 192 and 194 (_constrains_reconcile 187-194)` |
| Tax name uniqueness within the company tree. account.tax names must be unique per (company tree, name, type_tax_use, tax_scope, country_id)  | On reload, _pre_reload_data proactively renames colliding pre-existing taxes to '[old] <name>' / '[old1] <name>' (chart_template.py:392-397) before creating the | `addons/account/models/account_tax.py:226-238 (_constrains_name 211-238)` |
| Cash-basis transition account must be reconcilable — but this check is SKIPPED during chart template load. | During normal template load this is bypassed because _load runs with chart_template_load=True (chart_template.py:199-206). If you create/modify such a tax OUTSI | `addons/account/models/account_tax.py:254 (guarded by 'not self.env.context.get("chart_template_load")' at 252); _load sets context chart_template_load=True at chart_template.py:205` |
| Tax repartition line account domain (check_company + type exclusion). account.tax.repartition.line.account_id must belong to the tax's compa | Set account_id on repartition lines only for repartition_type='tax' lines, never 'base'. Use accounts that exist in the same company (or parent) and are not rec | `addons/account/models/account_tax.py:4923-4946 (AccountTaxRepartitionLine: _check_company_auto=True 4927, _check_company_domain 4928, account_id domain 4942, _onchange_repartition_type nulls account for base 4977-4980)` |
| x2many positional update on reload (count must align). In _pre_reload_data, for any existing record, each Command.CREATE in an x2many is con | When editing an existing tax via reload, keep the template's repartition line list the SAME length and order as the live tax's lines (positional mapping). If yo | `chart_template.py:463-482 (zip+Command.update at 480-482); reload entry _load chart_template.py:232-234` |
| A withholding-on-payment tax may not use amount_type 'group' or 'division'. | Define GE withholding taxes as 'percent' or 'fixed' amount_type, never as group/division. | `addons/l10n_account_withholding_tax/models/account_tax.py:42 (_check_amount_type, raise at :47)` |
| A withholding line's account_id may not be a liquidity (bank/cash) account nor the company transfer_account_id; and base_amount must be > 0. | Route withholding to a dedicated liability/asset withholding account (never 12xx bank/cash or the transfer account), and only register withholding lines with a  | `addons/l10n_account_withholding_tax/models/account_withholding_line.py:297 (_constrains_account_id, raise :302) and :290 (_constrains_base_amount, raise :295)` |

## Full reference by area

### account.account and account types

| Rule | Triggers on | Error message | Source | How to avoid |
|---|---|---|---|---|
| Receivable/Payable accounts MUST be reconcilable (reconcile=True) | @api.constrains on account_type, reconcile — fires on create/write whenever account_type or reconcile changes. account_type in ('asset_receivable','liability_payable') with reconcile=False | You cannot have a receivable/payable account that is not reconcilable. (account code: %s) | `account_account.py:27-31 (_check_reconcile)` | Never set reconcile=False on a receivable/payable account. Note _compute_reconcile (664-672) auto-forces reconcile=True for these types, so only an explicit override trips this. Leave reconcile alone for these types. |
| Off-Balance accounts cannot be reconcilable | @api.constrains on reconcile, account_type, tax_ids. account_type == 'off_balance' AND reconcile truthy | An Off-Balance account can not be reconcilable | `account_account.py:187-194 (_constrains_reconcile), raise at 192` | Keep reconcile=False on off_balance accounts. _compute_reconcile (671) already forces reconcile=False for off_balance, so only explicit override breaks it. This raises UserError (not ValidationError). |
| Off-Balance accounts cannot have taxes (tax_ids) | @api.constrains on reconcile, account_type, tax_ids. account_type == 'off_balance' AND tax_ids set | An Off-Balance account can not have taxes | `account_account.py:187,193-194 (_constrains_reconcile), raise at 194` | Clear tax_ids on off_balance accounts. There is also an @api.onchange('account_type') (851-854, _onchange_account_type) that auto-clears tax_ids=False in the form when type becomes off_balance; on backend writes you must clear them yourself. Raises UserError. |
| Journal foreign currency and its account currency must match | @api.constrains on currency_id. Raw SQL checks account.currency_id against journal.currency_id for journals whose currency differs from company currency, across default_account_id and inbound/outbound payment_method_line.payment_account_id | The foreign currency set on the journal '%(journal)s' and the account '%(account)s' must be the same. | `account_account.py:196-267 (_check_journal_consistency), raise at 263` | When an account is used as a journal default/payment account on a foreign-currency journal, set the same currency_id on the account as on the journal (or leave the journal currency = company currency). |
| Every account must belong to at least one company | @api.constrains on company_ids, account_type. sudo().company_ids is empty for the record | The following accounts must be assigned to at least one company:\n%(accounts)s | `account_account.py:269-277 (_check_company_consistency), raise at 272` | Always keep company_ids non-empty. company_ids field is also required=True with default=env.company (line 97). Don't Command.set([]) it. |
| Bank & Cash accounts cannot be shared across multiple companies | @api.constrains on company_ids, account_type. account_type == 'asset_cash' AND len(company_ids) > 1 | Bank & Cash accounts cannot be shared between companies. | `account_account.py:269,278-279 (_check_company_consistency), raise at 279` | Keep asset_cash accounts assigned to exactly one company; do not add more companies to company_ids. |
| Cannot unlink a company from an account that has journal items in that company | @api.constrains on company_ids, account_type. After grouping by company_ids, an account.move.line exists with account_id in the set whose company is NOT child_of the account's companies | You can't unlink this company from this account since there are some journal items linked to it. | `account_account.py:269,283-288 (_check_company_consistency), raise at 288` | Do not remove a company from company_ids if posted/draft journal items reference the account in that company. Raises UserError. |
| Receivable/Payable type not allowed if account is the default account of a sale/purchase journal | @api.constrains on account_type. Raw SQL: account is journal.default_account_id, account_type in ('asset_receivable','liability_payable'), journal.type in ('sale','purchase') | The account is already in use in a 'sale' or 'purchase' journal. This means that the account's type couldn't be 'receivable' or 'payable'. | `account_account.py:290-308 (_check_account_type_sales_purchase_journal), raise at 308` | Don't change an account to receivable/payable while it is set as the default account on a sale or purchase journal; detach it from the journal first. |
| Account code may contain only alphanumeric characters and dots | @api.constrains on code. code present and fails regex ACCOUNT_CODE_REGEX = ^[A-Za-z0-9.]+$ | The account code can only contain alphanumeric characters and dots. | `account_account.py:14-15 (regex), 310-316 (_check_account_code), raise at 314` | Use only letters, digits, and dots in codes — no spaces, hyphens, underscores, slashes, etc. |
| Cannot change an account that is a journal's Bank Account to Receivable/Payable | @api.constrains on account_type. Raw SQL: account is journal.default_account_id AND new account_type in ('asset_receivable','liability_payable') | You cannot change the type of an account set as Bank Account on a journal to Receivable or Payable. | `account_account.py:318-332 (_check_account_is_bank_journal_bank_account), raise at 332` | If an account is a journal's default (bank) account, do not switch its type to receivable/payable. Overlaps with the sale/purchase check but applies to any journal type. |
| Account code must be set for every (root) company the account belongs to | Called from create() (after super) and write() when company_ids/code/code_mapping_ids change and context 'defer_account_code_checks' is not set. For each company root, with_company(company).code is empty | The code must be set for every company to which this account belongs. | `account_account.py:1066-1081 (_ensure_code_is_unique, Check 1), raise at 1081; invoked 1037, 1057-1062` | When assigning an account to a company (multi-company), provide a code for that company via code_mapping_ids (per-company code_store). Each company in company_ids.root_id needs a code. |
| Account codes must be unique within a company hierarchy (no duplicate across the recordset, parent, or child companies) | _ensure_code_is_unique Check 2, run from create/write. Duplicate code among accounts in the same company in the written recordset (2.1), OR another DB account with same code whose company_ids is parent_of/child_of the company (2.2) | Account codes must be unique. You can't create accounts with these duplicate codes: %s | `account_account.py:1083-1116 (_ensure_code_is_unique, Check 2.1/2.2), raise at 1114-1115` | Pick codes unique across the whole parent/child company chain (not just one company). Note code uniqueness is enforced in Python, NOT via a DB _sql_constraint — there is no UNIQUE constraint on account_account.code_store. Use _search_new_account_code (466-544) to find a free code. |
| Cannot set a currency on an account that already has journal items in a different foreign currency | write() guard, fires when vals contains a truthy currency_id. Any account.move.line on the account with currency_id not in (False, new currency) | You cannot set a currency on this account as it already has some journal entries having a different foreign currency. | `account_account.py:1047-1050 (write), raise at 1050` | Only set/change currency_id before postings exist, or ensure all existing lines already use that currency (or no foreign currency). Raises UserError. |
| Cannot deprecate an account used in a tax distribution (repartition line) | write() guard, fires when vals.get('deprecated') is truthy AND an account.tax.repartition.line references the account | You cannot deprecate an account that is used in a tax distribution. | `account_account.py:1052-1053 (write), raise at 1053` | Remove the account from all tax repartition lines before setting deprecated=True. NOTE: the 'deprecated' field is NOT defined in the base account_account.py model — it is added by an inheriting module (e.g. enterprise/localization); this base guard only reacts to it in write(). |
| Switching reconcile False->True is allowed but resets residuals; switching True->False is blocked if partial reconciliations are pending | write() with 'reconcile' in vals. reconcile=True calls _toggle_reconcile_to_true; reconcile=False calls _toggle_reconcile_to_false which counts move lines with no full_reconcile_id but matched_debit_ids/matched_credit_ids | You cannot switch an account to prevent the reconciliation if some partial reconciliations are still pending. | `account_account.py:1040-1045 (write), 960-976 (_toggle_reconcile_to_false), raise at 975-976` | Before disabling reconciliation on an account, fully reconcile or unreconcile all partially-matched lines. Raises UserError. |
| Cannot delete/deactivate an account that contains journal items | @api.ondelete(at_uninstall=False) on unlink. Any account.move.line with account_id in the set | You cannot perform this action on an account that contains journal items. | `account_account.py:1124-1127 (_unlink_except_contains_journal_items), raise at 1127` | Only delete accounts with no journal items. (Applies on unlink, not on archive via active=False.) Raises UserError. |
| Cannot delete/deactivate an account referenced by a fiscal position account mapping | @api.ondelete(at_uninstall=False) on unlink. account.fiscal.position.account references the account as account_src_id or account_dest_id | You cannot remove/deactivate the accounts "%s" which are set on the account mapping of a fiscal position. | `account_account.py:1129-1132 (_unlink_except_linked_to_fiscal_position), raise at 1132` | Remove the account from all fiscal position mappings before unlinking. Raises UserError. |
| Cannot delete/deactivate an account set on a tax repartition line | @api.ondelete(at_uninstall=False) on unlink. account.tax.repartition.line references the account | You cannot remove/deactivate the accounts "%s" which are set on a tax repartition line. | `account_account.py:1134-1137 (_unlink_except_linked_to_tax_repartition_line), raise at 1137` | Detach the account from all tax repartition lines before unlinking. Raises UserError. |
| Accounts cannot be merged | _merge_method override called by the base record-merge mechanism (deduplication tooling) | You cannot merge accounts. | `account_account.py:1158-1159 (_merge_method), raise at 1159` | Do not attempt to merge account.account records; use action_unmerge to split, not merge. Raises UserError. |
| Unmerge requires access to all of the account's companies | _check_action_unmerge_possible (called by action_unmerge / _action_unmerge). Account's company_ids include companies not in env.user.company_ids | You do not have the right to perform this operation as you do not have access to the following companies: %s. | `account_account.py:1178-1186 (_check_action_unmerge_possible), raise at 1183-1186; also calls check_access('write') at 1180` | Only unmerge accounts whose companies you have access to. Also requires write access (check_access('write')). Raises UserError. |
| Cannot unmerge an account that already belongs to a single company | _check_action_unmerge_possible. len(account.company_ids) == 1 | Account %s cannot be unmerged as it already belongs to a single company. The unmerge operation only splits an account based on its companies. | `account_account.py:1187-1193 (_check_action_unmerge_possible), raise at 1189-1193` | Only unmerge multi-company accounts. Raises UserError. |
| Account creation via name_create (typing free text in a m2o) is blocked outside import | name_create called when NOT in import context ('import_file' not in context) | Please create new accounts from the Chart of Accounts menu. | `account_account.py:986-996 (name_create), raise at 996` | Create accounts from the Chart of Accounts (or via create()), not by typing a new name in a many2one quick-create. During import, name is auto-split into code+name. Raises ValidationError. |
| Cannot generate an unused account code (auto-increment exhausted) | _search_new_account_code when neither digit-incrementing nor .copy/.copyN (up to 99) yields a free code | Cannot generate an unused account code. | `account_account.py:466-544 (_search_new_account_code), raise at 544` | Free up codes / use a distinct prefix so increment search can find a gap; happens only in pathological saturation. Raises UserError. |
| account_type is REQUIRED (no off_balance/empty allowed at storage) | Field-level required=True on account_type Selection; enforced on create/write by ORM (NOT NULL) | (ORM/DB required-field error — no custom message) | `account_account.py:44-70 (account_type field, required=True)` | Always provide account_type, or rely on _compute_account_type (603-606, precompute) which derives it from the nearest parent account by code, defaulting to 'asset_current'. |
| name is REQUIRED | Field-level required=True on name Char; enforced by ORM on create/write | (ORM/DB required-field error — no custom message) | `account_account.py:33 (name field, required=True)` | Always supply a name. During import/quick paths, name may be split from a 'code name' string by _split_code_name (856-859). |
| company_ids is REQUIRED | Field-level required=True on company_ids Many2many; enforced by ORM plus the _check_company_consistency constraint above | (ORM required-field error; constraint message is the 'must be assigned to at least one company' one) | `account_account.py:97-99 (company_ids field, required=True, default env.company)` | Keep at least one company assigned; defaults to env.company. |
| code field max length 64 | Field-level size=64 on code Char (the underlying code_store) | (ORM size enforcement — value truncated/limited; no custom message) | `account_account.py:39 (code field, size=64)` | Keep account codes <= 64 chars. |
| tag_ids ondelete='restrict' — a tag in use on accounts cannot be deleted | Deleting an account.account.tag still referenced by account.account.tag_ids (relation account_account_account_tag) | (ORM RESTRICT foreign-key error on the m2m; no custom message in this model) | `account_account.py:104-112 (tag_ids field, ondelete='restrict')` | Remove the tag from all accounts before deleting it. |
| _check_company_auto / _check_company_domain enforce company coherence on relational check_company fields (e.g. tax_ids) | _check_company_auto=True with _check_company_domain = check_companies_domain_parent_of. ORM auto-runs _check_company on create/write for check_company fields (tax_ids has check_company=True) | (standard Odoo 'incompatible companies' error from _check_company) | `account_account.py:24-25 (_check_company_auto, _check_company_domain), 92-95 (tax_ids check_company=True)` | Ensure related records (e.g. default taxes) belong to a company that is parent-of the account's companies. |
| account.account.tag: (name, applicability, country_id) must be unique — SQL constraint | _sql constraint (models.Constraint) UNIQUE(name, applicability, country_id) on create/write of a tag with a colliding triple | A tag with the same name and applicability already exists in this country. | `account_account_tag.py:22-25 (_name_uniq Constraint)` | Use a distinct name per (applicability, country) combination for account tags. |
| account.account.tag: applicability is REQUIRED (default 'accounts') | Field-level required=True on applicability Selection ('accounts'/'taxes'/'products') | (ORM required-field error — no custom message) | `account_account_tag.py:12 (applicability field, required=True, default='accounts')` | Always set applicability (defaults to 'accounts'). |
| account.account.tag: name is REQUIRED | Field-level required=True on name Char | (ORM required-field error — no custom message) | `account_account_tag.py:11 (name field, required=True)` | Always provide a tag name. |
| account.account.tag: cannot delete the three master cash-flow tags | @api.ondelete(at_uninstall=False). Deleting a tag that is account.account_tag_operating / account_tag_financing / account_tag_investing | You cannot delete this account tag (%s), it is used on the chart of account definition. | `account_account_tag.py:110-120 (_unlink_except_master_tags), raise at 120` | Never delete the operating/financing/investing master tags. Raises UserError. |
| account.group: starting and ending code prefixes must have equal length — SQL CHECK constraint | _check_length_prefix DB CHECK on create/write. char_length(code_prefix_start) != char_length(code_prefix_end) | The length of the starting and the ending code prefix must be the same | `account_account.py:1497-1500 (AccountGroup._check_length_prefix Constraint)` | Make code_prefix_start and code_prefix_end the same number of characters. The _compute_code_prefix_start/_end methods (1502-1512) auto-align one to the other when one is missing/inverted. |
| account.group: groups of the same granularity (same prefix length) in the same company cannot overlap | @api.constrains on code_prefix_start, code_prefix_end. Raw SQL finds another group in same company with equal prefix length whose ranges overlap | Account Groups with the same granularity can't overlap | `account_account.py:1536-1555 (_constraint_prefix_overlap), raise at 1555` | Define non-overlapping prefix ranges for groups of the same prefix length within a company. Raises ValidationError. |
| account.group: parent hierarchy cannot be recursive | @api.constrains on parent_id. self._has_cycle() returns True | You cannot create recursive groups. | `account_account.py:1564-1567 (_check_parent_not_circular), raise at 1567` | Don't create circular parent_id chains. Note parent_id is auto-managed by _adapt_parent_account_group (1587-1628) based on prefix nesting. Raises ValidationError. |
| account.group: name is REQUIRED; company_id is REQUIRED | Field-level required=True on name and company_id (default env.company.root_id) | (ORM required-field error — no custom message) | `account_account.py:1492 (name required), 1495 (company_id required, default root_id)` | Always provide a name; company_id defaults to the active company's root. |

### Validations and constraints on account.tax

| Rule | Triggers on | Error message | Source | How to avoid |
|---|---|---|---|---|
| _check_repartition_lines: each document_type set (invoice list, refund list) must contain EXACTLY ONE base repartition line (repartition_type=='base'). Called once for the sorted invoice list and once for the sorted refund list from _validate_repartition_lines. | Write/create of a non-group tax (or group tax that has repartition lines) where invoice_repartition_line_ids or refund_repartition_line_ids has zero or >1 base lines. Fires via @api.constrains on invoice_repartition_line_ids, refund_repartition_line_ids, repartition_line_ids. | Invoice and credit note distribution should each contain exactly one line for the base. | `addons/account/models/account_tax.py:550-551 (check), invoked at :564-565 from _validate_repartition_lines :554` | For every tax that has repartition lines, supply exactly one repartition_type=='base' line in the invoice set and exactly one in the refund set. The default _compute_invoice/refund_repartition_line_ids already creates one base + one tax line per side (:485-497) — do not delete the base line and do not add a second base line. |
| _validate_repartition_lines: group taxes are exempt. A tax with amount_type=='group' AND no invoice_repartition_line_ids AND no refund_repartition_line_ids skips ALL repartition checks below. (Not an error — an early continue. Means: only group taxes may legally have zero repartition lines.) | Evaluated first inside the @api.constrains loop for every record. If amount_type!='group' OR any repartition lines exist, the full battery of checks runs. | — | `addons/account/models/account_tax.py:557-560` | A non-group tax (fixed/percent/division) must always carry a full, valid invoice+refund repartition set. Only set amount_type=='group' with empty repartition lines when you genuinely want a tax-of-taxes aggregate. |
| _validate_repartition_lines: invoice and refund repartition lists must have the SAME NUMBER of lines (len(invoice_repartition_line_ids)==len(refund_repartition_line_ids)). | Non-exempt tax where the count of invoice_repartition_line_ids differs from refund_repartition_line_ids. | Invoice and credit note distribution should have the same number of lines. | `addons/account/models/account_tax.py:567-568` | Always add/remove repartition lines in matched pairs — one invoice line for every refund line. When generating repartition programmatically, build both document_type=='invoice' and document_type=='refund' sets with identical cardinality. |
| _validate_repartition_lines: BOTH the invoice list and the refund list must contain AT LEAST ONE tax repartition line (repartition_type=='tax'). | Non-exempt tax where either the invoice side or the refund side has no repartition_type=='tax' line. | Invoice and credit note repartition should have at least one tax repartition line. | `addons/account/models/account_tax.py:570-572` | Keep at least one repartition_type=='tax' line on each side. The default compute seeds one tax line per side; never strip all tax lines leaving only base lines. |
| _validate_repartition_lines: invoice/refund SYMMETRY. Walking both sorted lists by index (sorted by (sequence, id)), the line at each position must have the SAME repartition_type AND the SAME factor_percent on the invoice side as on the refund side. | Non-exempt tax where, after sorting each side by (sequence,id), position i differs in repartition_type or factor_percent between invoice and refund. | Invoice and credit note distribution should match (same percentages, in the same order). | `addons/account/models/account_tax.py:574-580 (loop), sort at :562-563` | Mirror the invoice and refund distributions exactly: same ordering (control via sequence), same per-line repartition_type, same factor_percent. If you change an invoice line's percent or type, apply the identical change to the corresponding refund line. |
| _validate_repartition_lines: POSITIVE factor sum. Sum of factor (=factor_percent/100) over invoice tax-type lines with factor>0 must equal 1.0 (i.e. 100%). Compared with float_compare(total_pos_factor, 1.0, precision_digits=2); only enforced when total_pos_factor is non-zero. | Non-exempt tax where the positive tax repartition factors do not sum to 100% (e.g. two +60% tax lines = 120%). | Invoice and credit note distribution should have a total factor (+) equals to 100. | `addons/account/models/account_tax.py:582-585` | Make the positive tax repartition lines' percentages add up to exactly 100 (e.g. a single +100% line, or +40%/+60%). Note this checks ONLY the invoice side's tax lines (tax_reps from invoice_repartition_line_ids); refund is kept consistent via the symmetry check above. |
| _validate_repartition_lines: NEGATIVE factor sum. Sum of factor over invoice tax-type lines with factor<0 must equal -1.0 (i.e. -100%). Compared with float_compare(total_neg_factor, -1.0, precision_digits=2); only enforced when total_neg_factor is non-zero. | Non-exempt tax with negative tax repartition lines (reverse-charge style +100/-100) whose negative factors do not sum to -100% . | Invoice and credit note distribution should have a total factor (-) equals to 100. | `addons/account/models/account_tax.py:586-588` | For reverse-charge / negative-distribution taxes, ensure the negative tax lines total exactly -100% (e.g. one -100% line alongside the +100% line). has_negative_factor (:500-503) is what flags such taxes; the +/- sums are checked independently, so a +100/-100 pair satisfies both. |
| _check_children_scope: no recursion. A tax's children_tax_ids graph must not contain a cycle (uses _has_cycle('children_tax_ids')). | @api.constrains('children_tax_ids','type_tax_use'): setting children_tax_ids such that a tax is (transitively) its own child. | Recursion found for tax “%s”. (with the tax name substituted) | `addons/account/models/account_tax.py:593-594` | Do not create circular group memberships. A group tax must not include itself or any ancestor among children_tax_ids. |
| _check_children_scope: child scope compatibility. Every child in children_tax_ids must have child.type_tax_use in ('none', parent.type_tax_use) AND child.tax_scope in (parent.tax_scope, False). | @api.constrains('children_tax_ids','type_tax_use'): adding a child whose type_tax_use is a different concrete use than the group, or whose tax_scope differs from the group's (and is not empty/False). | The application scope of taxes in a group must be either the same as the group or left empty. | `addons/account/models/account_tax.py:595-600` | Children of a group must share the group's type_tax_use (or be 'none') and share the group's tax_scope (or leave tax_scope empty). Set child type_tax_use='none' for taxes meant only to live inside groups. |
| _check_children_scope: no nested groups. No child in children_tax_ids may itself have amount_type=='group'. | @api.constrains('children_tax_ids','type_tax_use'): adding a child tax that is itself a group of taxes. | Nested group of taxes are not allowed. | `addons/account/models/account_tax.py:601-605` | Keep group hierarchies flat — a group's children must all be leaf taxes (fixed/percent/division), never another amount_type=='group' tax. |
| _constrains_name: tax name uniqueness. For taxes with type_tax_use!='none', the tuple (name, type_tax_use, tax_scope, country_id) must be unique within the company tree (company_id child_of root company). type_tax_use=='none' taxes are exempt from uniqueness. | @api.constrains('company_id','name','type_tax_use','tax_scope','country_id'): creating/writing a tax whose (name,type_tax_use,tax_scope,country_id) collides with another tax in the same root company tree. Batched in groups of 100 via split_every. | Tax names must be unique!\n%(taxes)s (followed by a bulleted list: "- <name> in <company>" per duplicate) | `addons/account/models/account_tax.py:211-238 (search at :225, raise at :226)` | Give each tax a name unique per (type_tax_use, tax_scope, country_id) within the company group. To reuse a name, vary tax_scope or country, or set type_tax_use='none' (which is excluded from the check). |
| validate_tax_group_id: tax.tax_group_id.country_id, when set, must equal tax.country_id. | @api.constrains('tax_group_id'): assigning a tax_group_id whose country_id is set and differs from the tax's country_id. | The tax group must have the same country_id as the tax using it. | `addons/account/models/account_tax.py:240-244` | Pick a tax_group_id whose country_id matches the tax's country_id, or use a tax group with no country (country_id==False, which passes). The field domain on tax_group_id ([('country_id','in',(country_id,False))], :161) already steers the UI toward this. |
| _constrains_cash_basis_transition_account: when tax_exigibility=='on_payment', the cash_basis_transition_account_id must allow reconciliation (account.reconcile==True). Skipped when context flag chart_template_load is set. | @api.constrains('tax_exigibility','cash_basis_transition_account_id'): setting a cash-basis (on_payment) tax whose transition account has reconcile=False, outside chart-template loading. | The cash basis transition account needs to allow reconciliation. | `addons/account/models/account_tax.py:246-254` | For cash-basis taxes (tax_exigibility=='on_payment'), choose a cash_basis_transition_account_id with Allow Reconciliation enabled (account.reconcile=True). |
| _check_company_consistency: you cannot change a tax's company_id if journal items already reference the tax. Blocks if any account.move.line has tax_line_id in the taxes OR tax_ids in the taxes while NOT being in the company tree (company_id child_of company). | @api.constrains('company_id'): writing a new company_id on a tax that is already used on posted/existing move lines belonging outside the new company tree. | You can't change the company of your tax since there are some journal items linked to it. (raised as UserError, not ValidationError) | `addons/account/models/account_tax.py:607-616 (search_count at :610, raise at :616)` | Do not reassign company_id on a tax that has been used in journal entries. Create a new tax under the target company instead. Note company_id is also readonly=True on the field (:126). |
| account.tax field-level requirements: name (required, translate), type_tax_use (required, default 'sale'), amount_type (required, default 'percent'), sequence (required, default 1), amount (required, digits (16,4), default 0.0), company_id (required, readonly, default env.company), tax_group_id (required, computed/stored, precompute), country_id (required, computed/stored, precompute). These are enforced by the ORM NOT NULL / required logic, not by @api.constrains. | Create/write leaving any required field empty (e.g. tax_group_id or country_id null), or writing company_id (readonly). | (ORM-generated, not a literal string in this file — standard 'required field' error; tax_group_id and country_id are auto-populated by their compute methods so rarely surface as errors.) | `addons/account/models/account_tax.py:80,81,84,131,133,126,156-161,196-202` | Always provide name, type_tax_use, amount_type, amount. Rely on the precompute defaults for tax_group_id (_compute_tax_group_id :273-290) and country_id (_compute_country_id :268-271) rather than forcing them null. Do not attempt to write company_id (readonly). |
| account.tax field DOMAIN: tax_group_id domain restricts selectable groups to [('country_id','in',(country_id,False))] — group must be country-less or match the tax country. (UI/onchange domain, complements the validate_tax_group_id constraint.) | Selecting a tax group in the form whose country differs from the tax's country. | (domain — no exception text; non-matching groups are simply not selectable; hard enforcement is validate_tax_group_id at :244) | `addons/account/models/account_tax.py:161` | Choose a group whose country_id matches the tax or is False. |
| account.tax.repartition.line ACCOUNT DOMAIN: account_id is restricted to accounts whose account_type is NOT in ('asset_receivable','liability_payable','off_balance'). i.e. you cannot post tax repartition to a receivable, payable, or off-balance account. | Setting account_id on a repartition line to an account of type asset_receivable, liability_payable, or off_balance (blocked by the field domain in the UI; the domain is the enforcement mechanism — there is no separate @api.constrains backing it). | (domain — no literal exception text; such accounts are excluded from the selection) | `addons/account/models/account_tax.py:4942 — domain="[('account_type', 'not in', ('asset_receivable', 'liability_payable', 'off_balance'))]"` | Route tax repartition to income/expense or current-asset/current-liability tax accounts (e.g. tax payable/receivable accounts), never to AR/AP control accounts or off-balance accounts. Per project rule, off-balance taxes must be redirected to an existing on-balance account (e.g. 3330/3344/3399). |
| account.tax.repartition.line field-level requirements: factor_percent (required, default 100, digits (16,12)), repartition_type (required, default 'tax'), document_type (required, no default). company_id is related to tax_id.company_id (stored, effectively not user-settable). check_company=True on account_id, tax_id (company consistency vs tax/company). | Create/write a repartition line without document_type (no default) or without repartition_type/factor_percent; or assigning account_id/tax_id from an incompatible company (check_company). | (ORM-generated required-field / company-mismatch errors; no literal strings in this file. document_type has no default so it MUST be supplied explicitly.) | `addons/account/models/account_tax.py:4930-4947 (factor_percent :4930-4936, repartition_type :4938, document_type :4939 required no default, check_company :4943,4946)` | Always set document_type ('invoice' or 'refund') explicitly when creating repartition lines directly (the parent tax's _sanitize_vals :633-644 injects it when you use invoice_/refund_repartition_line_ids). Keep account_id within the tax's company. |
| account.tax.repartition.line onchange normalizer: when repartition_type=='base', account_id is forced to None (a base line cannot carry an account). | @api.onchange('repartition_type'): switching a line to 'base' in the UI clears its account. | (onchange — no exception; silent normalization) | `addons/account/models/account_tax.py:4977-4980` | Do not set account_id on base repartition lines; it will be cleared. Only tax-type lines carry accounts. |
| account.tax.repartition.line tag_ids domain: tag_ids restricted to account.account.tag with applicability=='taxes'; dynamic tag_ids_domain further restricts by country (account_fiscal_country_id + multi_vat_foreign_country_ids + False). ondelete='restrict' on tag_ids. | Assigning a tag whose applicability!='taxes' or whose country is outside the company's fiscal/foreign-VAT countries; or deleting a tag still referenced by a repartition line (restrict). | (domain + ORM restrict — no literal text in this file for the domain; delete-restrict raises the standard ORM restrict error) | `addons/account/models/account_tax.py:4945 (static domain), 4955-4961 (dynamic tag_ids_domain)` | Only attach tax-applicability tags belonging to the company's fiscal country or registered foreign-VAT countries. Do not delete account.account.tag records still used by repartition lines. |
| account.tax.group field-level requirements: name (required, translate), company_id (required, default env.company). Account fields (tax_payable_account_id, tax_receivable_account_id, advance_tax_payment_account_id) are check_company=True. country_id is computed/stored (readonly=False) — defaults to company's fiscal/normal country. | Create/write a tax group without name or company_id; or assigning a payable/receivable/advance account from a different company (check_company). | (ORM-generated required-field / company-mismatch errors; no literal strings — account.tax.group has NO @api.constrains and NO _sql_constraints of its own in this file.) | `addons/account/models/account_tax.py:32 (name required), 34 (company_id required), 35-49 (check_company accounts), 50-55 (country_id compute :65-68)` | Provide name and company_id. Keep the three tax accounts within the group's company. The group's own country validity vs. the taxes using it is enforced on the tax side via validate_tax_group_id (:240-244), not on the group model. |
| NO DB-LEVEL constraints. There are no _sql_constraints and no models.Constraint declarations on account.tax, account.tax.repartition.line, or account.tax.group in this file, and no other addons/account file inherits these models to add constraints. All validation is Python @api.constrains, field required=/domain= attributes, check_company, and onchange normalizers. | N/A — informational. Means uniqueness/symmetry/factor rules live entirely in Python and run on create/write through @api.constrains, not as Postgres CHECK/UNIQUE constraints. | — | `addons/account/models/account_tax.py (whole file: AccountTaxGroup :25-68, AccountTax :71-4920, AccountTaxRepartitionLine :4923-4991) — grep for _sql_constraints/models.Constraint returns nothing` | When mirroring these models in custom code, replicate the Python @api.constrains (especially _validate_repartition_lines symmetry + factor sums and _check_children_scope) rather than expecting the database to enforce them. amount_type rules are enforced only indirectly: 'group' exemption in _validate_repartition_lines (:557) and 'no nested group' / scope checks in _check_children_scope (:601-605). There is NO constraint forbidding a negative amount or restricting price_include — price_include is a computed field (from price_include_override / company_price_include, :292-299) with no validation, and amount has no sign check. |

### Validations and constraints on account.move and account.move.line that

| Rule | Triggers on | Error message | Source | How to avoid |
|---|---|---|---|---|
| OFF-BALANCE HOMOGENEITY — THE EXACT CHECK YOU ASKED FOR. If ANY line of the move uses an account with account_type == 'off_balance', then EVERY account on EVERY line of that move must also be account_type 'off_balance'. It iterates line.move_id.line_ids and compares all account_id.account_type values. | @api.constrains on fields account_id, tax_ids, tax_line_id, reconciled — fires on create/write of any account.move.line whenever one of those fields changes (and at posting since posting writes lines). Note it scans the WHOLE move, not just the changed line. | If you want to use "Off-Balance Sheet" accounts, all the accounts of the journal entry must be of this type | `addons/account/models/account_move_line.py#L1363 (method _check_off_balance, decorator+def at L1359-L1360, raise at L1364)` | Never mix an off_balance account with non-off_balance accounts on the same move. If you route an account to off_balance type (per our gec rule of not flipping CoA), ensure the entire counterpart side of the entry is also off_balance, or keep both sides on-balance. The check looks at account.account_type, so it is driven by the account's type, not the move. |
| OFF-BALANCE: NO TAXES. A line whose account is off_balance must have no tax_ids and no tax_line_id. | Same @api.constrains('account_id','tax_ids','tax_line_id','reconciled') on account.move.line, create/write. | You cannot use taxes on lines with an Off-Balance account | `addons/account/models/account_move_line.py#L1366 (method _check_off_balance)` | Do not attach taxes (input or output, base or tax line) to any line posting to an off_balance account. |
| OFF-BALANCE: NO RECONCILIATION. A line on an off_balance account cannot be reconciled (reconciled must be False). | Same @api.constrains, also triggered by 'reconciled' field change. | Lines from "Off-Balance Sheet" accounts cannot be reconciled | `addons/account/models/account_move_line.py#L1368 (method _check_off_balance)` | Off_balance accounts must not be set reconcilable / must not be matched. |
| BALANCED ENTRY (debit = credit). For every move that has lines, ROUND(SUM(balance), company_currency.decimal_places) must equal 0. Computed in SQL summing account_move_line.balance per move, rounded to the COMPANY currency precision (not the move/foreign currency). | Context manager _check_balanced wraps account.move.create (L3843), account.move.write (L3922), account.move.line.create (L1648) and account.move.line.write (L1752). It can be disabled via context flag 'check_move_validity'=False (_disable_recursion). Runs on the move container after the write/create completes. | The entry is not balanced. (single move). For multiple unbalanced moves: "The following entries are unbalanced:" followed by each move name. | `addons/account/models/account_move.py#L2760 (single) and #L2766 (multi); method _check_balanced at L2748-2766; SQL in _get_unbalanced_moves at L2768-2788` | Ensure debit total equals credit total in COMPANY currency. When building lines manually, set balance (or debit/credit) so they sum to zero at company-currency rounding. Foreign-currency amount_currency does NOT have to net to zero — only company-currency balance does. Don't bypass via Command writes that leave the move lopsided. |
| EVERY ACCOUNTABLE LINE NEEDS AN ACCOUNT (DB-level NOT NULL). A line must have account_id set UNLESS its display_type is line_section/line_subsection/line_note. Enforced as a Postgres CHECK constraint. | DB constraint checked at flush/commit on INSERT/UPDATE of account_move_line. Fires on create/write/post for any non-display line missing account_id. | Missing required account on accountable line. | `addons/account/models/account_move_line.py#L470-473 (_check_accountable_required_fields = models.Constraint, CHECK(display_type IN (...) OR account_id IS NOT NULL))` | Always provide account_id on product/tax/payment_term/term and any non-display line. Only section/subsection/note lines may omit it. |
| NON-ACCOUNTABLE LINE MUST BE NULL/ZERO. For display_type in (line_section, line_subsection, line_note): amount_currency=0, debit=0, credit=0, and account_id IS NULL. DB CHECK constraint. | DB constraint at flush/commit on INSERT/UPDATE of account_move_line. | Forbidden balance or account on non-accountable line | `addons/account/models/account_move_line.py#L474-477 (_check_non_accountable_fields_null)` | Never put an amount or account on a section/note line. _prepare_create_values (L1591-1592) already pops account_id for these display types — don't re-add it. |
| CREDIT/DEBIT MUTUALLY EXCLUSIVE. credit * debit must equal 0 (a line cannot have both a non-zero debit and a non-zero credit), unless it is a section/subsection/note line. DB CHECK. | DB constraint at flush/commit on INSERT/UPDATE of account_move_line. | Wrong credit or debit value in accounting entry! | `addons/account/models/account_move_line.py#L462-465 (_check_credit_debit)` | Set only one of debit/credit non-zero per line. Prefer setting balance and let the ORM derive debit/credit (debit=balance>0, credit=-balance<0). |
| AMOUNT_CURRENCY / BALANCE SIGN AGREEMENT. balance and amount_currency must share sign: (balance<=0 AND amount_currency<=0) OR (balance>=0 AND amount_currency>=0), unless section/note line. DB CHECK. | DB constraint at flush/commit on INSERT/UPDATE of account_move_line. NOTE: flush_model/flush_recordset are overridden (L1804-1814) to always flush balance+amount_currency together so this constraint is never seen mid-computation. | The amount expressed in the secondary currency must be positive when account is debited and negative when account is credited. If the currency is the same as the one from the company, this amount must strictly be equal to the balance. | `addons/account/models/account_move_line.py#L466-468 (_check_amount_currency_balance_sign)` | Keep amount_currency the same sign as balance. For a debit (balance>0) amount_currency must be >=0; for a credit (balance<0) amount_currency must be <=0. Don't set a negative amount_currency on a debit line. |
| ACCOUNT MUST BE ACTIVE (not archived). The account on each non-display line must be active, unless line.is_imported or context skip_account_deprecation_check is set. Also: writing an archived account onto a line is blocked directly in write(). | _check_constrains_account_id_journal_id is called: in _post (L5542), in account.move.write when journal_id/currency_id change (L3957), in account.move.line.write when account_id/currency_id change (L1783). The write-level archived-account guard fires on any account.move.line.write that sets account_id (L1686-1687). Also _post adds a per-move validation (L5561-5562). | The account NAME (CODE) is archived. (from _check_constrains...) / You cannot use an archived account. (from write) / A line of this move is using a archived account, you cannot post it. (from _post) | `addons/account/models/account_move_line.py#L1349-1350 (constrains helper) and #L1687 (write); addons/account/models/account_move.py#L5561-5562 (_post)` | Don't post or write moves referencing archived accounts. Reactivate the account or pass skip_account_deprecation_check in context only for legitimate imports. |
| ACCOUNT SECONDARY-CURRENCY CONSISTENCY. If the account has a forced currency_id, it must equal either the company currency or the line currency. Otherwise rejected. | _check_constrains_account_id_journal_id — same call sites: _post (L5542), move.write on journal/currency change (L3957), line.write on account/currency change (L1783). | The account selected on your journal entry forces to provide a secondary currency. You should remove the secondary currency on the account. | `addons/account/models/account_move_line.py#L1353-1354` | If an account has a fixed currency, only use it on lines whose currency matches it (or whose currency is the company currency). Don't post a foreign-currency line to a fixed-currency account of a third currency. |
| RECONCILABLE-ACCOUNT RULES (at reconciliation, not posting). To reconcile lines together: none already reconciled; none on a cancelled move; all on the SAME account; same company root; and the account must allow reconciliation (reconcile=True) OR be of type asset_cash / liability_credit_card. | _check_amls_exigibility_for_reconciliation, called during reconcile() flows. | You are trying to reconcile some entries that are already reconciled. / You can not reconcile cancelled entries. / Entries are not from the same account: ... / Entries don't belong to the same company: ... / Account X does not allow reconciliation. First change the configuration of this account to allow it. | `addons/account/models/account_move_line.py#L2521-2541 (method _check_amls_exigibility_for_reconciliation at L2506)` | Reconcile only posted, non-cancelled, same-account, same-company lines, and set account.reconcile=True (unless cash/credit-card type). The _reconcile_marked import flow (L3030-3032) auto-flips reconcile=True for the account when needed. |
| TAX-LINE DELETION PROTECTION. A line with display_type=='tax' cannot be deleted while the move still has lines carrying tax_ids (would corrupt the tax report). Bypassed only with context dynamic_unlink (used by internal dynamic-line sync). | @api.ondelete(at_uninstall=False) _prevent_automatic_line_deletion on account.move.line.unlink(). | You cannot delete a tax line as it would impact the tax report | `addons/account/models/account_move_line.py#L1830-1833 (method _prevent_automatic_line_deletion at L1826)` | Don't manually unlink tax lines; remove the tax from the base line and let the dynamic-line sync recompute/remove the tax line (it runs with dynamic_unlink). |
| TAX EDIT ON POSTED LINE FORBIDDEN. You cannot change tax_ids or tax_line_id on a line whose move is posted. | account.move.line.write() pre-check, per line, when tax_ids or tax_line_id will change and parent_state=='posted'. | You cannot modify the taxes related to a posted journal item, you should reset the journal entry to draft to do so. | `addons/account/models/account_move_line.py#L1711-1712` | Reset the move to draft before changing taxes, then re-post. |
| PAYMENT-TERM LINE DELETION PROTECTION. A receivable/payable (display_type=='payment_term') line cannot be deleted (would break payment-term consistency). | @api.ondelete _prevent_automatic_line_deletion on unlink (context dynamic_unlink bypasses). | You cannot delete a payable/receivable line as it would not be consistent with the payment terms | `addons/account/models/account_move_line.py#L1834-1838` | Don't unlink the receivable/payable term line directly; change the partner/payment-term/amounts and let sync rebuild it. |
| CASH-BASIS vs NON-CASH-BASIS SHARED TAGS. On a single move line, taxes exigible on_payment (cash basis) and on invoice cannot share repartition base tags (would double-count the base in reports). | @api.constrains('tax_ids','tax_repartition_line_id') on account.move.line, create/write. | Taxes exigible on payment and on invoice cannot be mixed on the same journal item if they share some tag. | `addons/account/models/account_move_line.py#L1454 (method _check_caba_non_caba_shared_tags at L1416)` | Don't mix CABA and non-CABA taxes sharing the same report tag on one line. Workaround: use a tax group whose children carry the tag separately. |
| PAYABLE/RECEIVABLE ACCOUNT TYPE vs DOCUMENT DIRECTION. On a sale document: a liability_payable account is forbidden, and exactly the payment_term line (and only it) must be asset_receivable (XOR). On a purchase document: asset_receivable is forbidden, and exactly the payment_term line must be liability_payable (XOR). The XOR enforces 'receivable line must have a due date and vice versa'. | @api.constrains('account_id','display_type') on account.move.line, create/write. | Account X is of payable type, but is used in a sale operation. / Any journal item on a receivable account must have a due date and vice versa. / Account X is of receivable type, but is used in a purchase operation. / Any journal item on a payable account must have a due date and vice versa. | `addons/account/models/account_move_line.py#L1376, L1378, L1381, L1383 (method _check_payable_receivable at L1370)` | Put receivable accounts only on the payment_term line of sales docs, payable only on the payment_term line of purchase docs; never put a receivable on a purchase doc product line or a payable on a sale doc product line. |
| DEDUCTIBILITY ONLY ON VENDOR BILLS / RANGE 0-100. deductible_amount may differ from 100 only on purchase documents, and must be between 0 and 100. | @api.constrains('deductible_amount') on account.move.line, create/write. | Only vendor bills allow for deductibility of product/services. / The deductibility must be a value between 0 and 100. | `addons/account/models/account_move_line.py#L1479 and L1481 (method _constrains_deductible_amount at L1475)` | Leave deductible_amount=100 on non-purchase lines; keep it within [0,100] on vendor bill lines. |
| CURRENCY CONSISTENCY — INVOICE RATE STRICTLY POSITIVE. When the move currency differs from company currency and the move is an invoice, invoice_currency_rate must be > 0. | @api.constrains('invoice_currency_rate') on account.move, create/write. | The currency rate must be strictly positive. | `addons/account/models/account_move.py#L2850 (method _check_invoice_currency_rate at L2839)` | Ensure a positive exchange rate is set before saving a foreign-currency invoice. |
| CURRENCY CONSISTENCY — INACTIVE CURRENCY AT POST. Cannot post a move whose currency is inactive (display_inactive_currency_warning). | _post() validation loop, per move. | You cannot validate a document with an inactive currency: NAME | `addons/account/models/account_move.py#L5555-5559` | Activate the currency before posting. |
| TAX COUNTRY COMPATIBILITY. The countries of all taxes used on the move (tax_ids + tax_line_id countries) must match the move's tax_country_id, or — if a fiscal position is set — the fiscal position's country. | @api.constrains('line_ids','fiscal_position_id','company_id') on account.move; recomputes tax_country_id first. | This entry contains taxes that are not compatible with your fiscal position. Check the country set in fiscal position and in your tax configuration. / This entry contains one or more taxes that are incompatible with your fiscal country. Check company fiscal country in the settings and tax country in taxes configuration. | `addons/account/models/account_move.py#L2836 and L2837 (method _validate_taxes_country at L2824)` | Use taxes whose country matches the company fiscal country (or the fiscal position country). For Georgian taxes ensure tax.country_id matches the company's fiscal country/fiscal position. |
| JOURNAL TYPE vs MOVE TYPE. A purchase document must be in a 'purchase' journal; a sale document must be in a 'sale' journal. | @api.constrains('journal_id','move_type') on account.move, create/write. | Cannot create a purchase document in a non purchase journal / Cannot create a sale document in a non sale journal | `addons/account/models/account_move.py#L2820 and L2822 (method _check_journal_move_type at L2816)` | Match journal.type to the move_type (in_invoice/in_refund -> purchase; out_invoice/out_refund -> sale). |
| FISCAL (HARD) LOCK DATE. You cannot add or modify entries on/before a hard fiscal lock date (fiscalyear + sale/purchase lock depending on journal type). Checked on date/state changes of posted moves and on move-line writes touching fiscally protected fields. | _check_fiscal_lock_dates on account.move: called in write() when posted move name/date changes (L3895), when un-posting (L3900), after write if date/state changed (L3942), and in line.write when a posted line's fiscal-protected field changes (L1716). Bypassed by context bypass_lock_check == BYPASS_LOCK_CHECK. | You cannot add/modify entries prior to and inclusive of: LOCK_DATE_INFO. | `addons/account/models/account_move.py#L2804-2806 (method _check_fiscal_lock_dates at L2790)` | Use a move date after the hard lock date, or have an admin move the lock date. Don't edit date/account/amounts of posted moves in a locked period. |
| TAX (HARD) LOCK DATE. Operations that affect the tax report (line has tax_ids/tax_line_id/tax tags) cannot touch a posted move dated on/before the hard tax lock date. | _check_tax_lock_date on account.move.line: called in line.create (L1657), line.write for tax-protected fields (L1719/L1749/L1786), and move.write date/state changes (L3896/L3901/L3943). Only fires for posted lines that _affect_tax_report(). | The operation is refused as it would impact an already issued tax statement. Please change the journal entry date or the following lock dates to proceed: LOCK_DATE_INFO. | `addons/account/models/account_move_line.py#L1403-1405 (method _check_tax_lock_date at L1389)` | Don't create/modify tax-affecting lines in a tax-locked period; change the move date or the tax lock date. |
| POSTED MOVE: READONLY FIELDS. On a posted move you cannot modify: invoice_line_ids, line_ids, invoice_date, date, partner_id, invoice_payment_term_id, currency_id, fiscal_position_id, invoice_cash_rounding_id. (Bypass via context skip_readonly_check.) | account.move.write(), per move, when move_state resolves to 'posted' and vals contains any of these. | You cannot modify the following readonly fields on a posted move: FIELDS | `addons/account/models/account_move.py#L3909-3910` | Reset to draft to change these; or modify before posting. Non-legal fields not in this list can still be edited on posted moves. |
| VALIDATED ENTRY (checked) — REVIEW RIGHTS. A 'checked' move (validated) can only be set back to draft, or have 'checked' toggled, by a user with review rights. | account.move.write() when vals sets checked, or sets state=draft on a checked move. | You don't have the access rights to perform this action. (AccessError) / Validated entries can only be changed by your accountant. | `addons/account/models/account_move.py#L3864 and L3866` | Only accountants with the review group can un-validate/edit a validated entry. |
| POSTED-MOVE JOURNAL CHANGE BLOCKED. Cannot change journal_id of a move that has been posted before, unless name is cleared/'/'. Also cannot change journal when a sequence number is already assigned (sequence_number not in (0,1)) unless name is cleared. | account.move.write(), per move. | You cannot edit the journal of an account move if it has been posted once, unless the name is removed or set to "/". This might create a gap in the sequence. / You cannot edit the journal of an account move with a sequence number assigned, unless the name is removed or set to "/". This might create a gap in the sequence. | `addons/account/models/account_move.py#L3880 and L3888` | Don't switch journals on numbered/posted moves; if you must, clear the name first (creates a sequence gap). |
| HASH (INALTERABILITY) — MOVE FIELDS. On a move with an inalterable_hash, you cannot edit any integrity-hash field (name, date, journal_id, company_id) or inalterable_hash itself. | account.move.write(), per move, when vals intersects _get_integrity_hash_fields()+['inalterable_hash']. | This document is protected by a hash. Therefore, you cannot edit the following fields: FIELDS. | `addons/account/models/account_move.py#L3869-3874 (hash fields list at L4533-4540)` | Never edit hashed legal fields. Once a journal runs in restrict_mode_hash_table and the move is hashed, those fields are frozen. |
| HASH (INALTERABILITY) — MOVE LINE FIELDS. Lines whose move is hashed cannot have integrity-hash line fields edited. | account.move.line.write(), when vals intersects the line integrity-hash fields and the parent move has inalterable_hash. | You cannot edit the following fields: FIELDS. The following entries are already hashed: ENTRIES | `addons/account/models/account_move_line.py#L1692-1698` | Don't edit hashed-line legal fields; the move is locked by hash chain. |
| HASH (INALTERABILITY) — NO LINE DELETION. Lines belonging to a hashed (locked) move cannot be deleted. | @api.ondelete _except_hashed_entry_lines on account.move.line.unlink(). | You cannot delete journal items belonging to a locked journal entry. | `addons/account/models/account_move_line.py#L1846-1847` | Never delete lines of a hashed move; reverse instead. |
| NO DELETING POSTED LINES. Cannot delete a line whose move is posted (unless context force_delete). | @api.ondelete _unlink_except_posted on account.move.line.unlink(). | You can't delete a posted journal item. Don't play games with your accounting records; reset the journal entry to draft before deleting it. | `addons/account/models/account_move_line.py#L1823-1824` | Reset move to draft before deleting lines. |
| CANNOT CREATE AN ALREADY-POSTED MOVE. create() rejects vals with state=='posted'. | account.move.create(). | You cannot create a move already in the posted state. Please create a draft move and post it after. | `addons/account/models/account_move.py#L3840-3841` | Create as draft then call action_post()/_post(). |
| POST-TIME COMPLETENESS CHECKS (aggregated). At _post, a move must: be in draft (not posted/cancel); have at least one non-display line; not be a future auto-post move when soft=False; have an active journal; have an active currency; and not use archived accounts. Invoices additionally: quick-edit total must match; recipient bank account must be active; total must not be negative (credit note instead); sale invoice requires Customer, purchase requires Vendor; purchase requires a Bill/Refund date. All collected into a set and raised together. | account.move._post() / action_post(). | The entry NAME (id ID) must be in draft. / Even magicians can't post nothing! / This move is configured to be auto-posted on DATE / You cannot post an entry in an archived journal (JOURNAL) / You cannot validate a document with an inactive currency: NAME / A line of this move is using a archived account, you cannot post it. / The current total is ... but the expected total is ... / The recipient bank account linked to this invoice is archived... / You cannot validate an invoice with a negative total amount... / The 'Customer' field is required to validate the invoice... / The field 'Vendor' is required... / The Bill/Refund date is required to validate this document. | `addons/account/models/account_move.py#L5496-5566 (method _post at L5476; aggregated raise at L5566). action_post 'no draft to post' at L5995.` | Before posting: ensure draft state, real lines present, active journal+currency, no archived accounts, partner set per direction, invoice date on bills, non-negative total, valid bank account. |
| POST RIGHTS. Posting requires group account.group_account_invoice (unless sudo). | _post() first line. | You don't have the access rights to post an invoice. (AccessError) | `addons/account/models/account_move.py#L5490-5491` | Grant the invoicing/accounting group to the posting user, or run sudo for system flows. |
| RESET-TO-DRAFT RESTRICTIONS. Only posted/cancelled entries can be reset to draft; some require a cancellation request instead; exchange-difference and tax-cash-basis entries and locked entries cannot be reset. | button_draft / _check_draftable on account.move. | Only posted/cancelled journal entries can be reset to draft. / You can't reset to draft those journal entries. You need to request a cancellation instead. / You cannot reset to draft an exchange difference journal entry. / You cannot reset to draft a tax cash basis journal entry. / You cannot reset to draft a locked journal entry. | `addons/account/models/account_move.py#L6049, L6051 and method _check_draftable at L6090 (raises L6106/L6113/L6115)` | Don't try to draft system-generated (exchange diff / CABA) or locked moves; use the cancellation request flow where required. |
| AUTO-POST VENDOR BILL NEEDS DATE. A move set to auto_post (!= 'no') that is a purchase document must have invoice_date. | @api.constrains('auto_post','invoice_date') on account.move. | For this entry to be automatically posted, it required a bill date. | `addons/account/models/account_move.py#L2814 (method _require_bill_date_for_autopost at L2809)` | Set invoice_date before enabling auto-post on a vendor bill. |
| COMPANY REQUIRED. A move cannot be saved without a company_id (enforced via onchange because the compute needs it before save). | @api.onchange('company_id') _inverse_company_id at save. | We can't leave this document without any company. Please select a company for this document. | `addons/account/models/account_move.py#L2581` | Always set company_id. |
| MULTI-COMPANY FIELD CONSISTENCY (_check_company). _check_company_auto=True on both models forces Many2one company-dependent fields (journal, account, partner-related, taxes, etc.) to belong to the move's company. Standard Odoo cross-company check. | ORM _check_company runs on create/write for both account.move and account.move.line (because _check_company_auto = True). | (Standard Odoo) Variant of: 'You cannot use this account for this company' / company-mismatch ValidationError listing the incompatible records. | `addons/account/models/account_move.py#L77 and addons/account/models/account_move_line.py#L24 (_check_company_auto = True; check implemented in base ORM models.py)` | Use accounts/journals/taxes/fiscal positions belonging to the move's company (or shared/no-company). |
| MATCHING NUMBER INTEGRITY (internal invariant). matching_number must match regex ^((P?\d+)/(I.+))$; 'I' temp numbers can't be in a real matching; 'P' must have partials and no full reconcile; full numbers require full_reconcile_id; a line with partials must have a number. Most violations raise plain Exception (developer-level). | @api.constrains('matching_number','matched_debit_ids','matched_credit_ids') on account.move.line. | A temporary number can not be used in a real matching (ValidationError) plus internal Exceptions: 'Invalid matching number format' / 'Should have partials' / 'Should not be partial number' / 'Should not be full number' / 'Matching number should be the full reconcile' / 'Should have number' | `addons/account/models/account_move_line.py#L1457-1473 (method _constrains_matching_number at L1456)` | Don't write matching_number / matched_*_ids directly; let the reconciliation engine manage them. |
| ANALYTIC DISTRIBUTION 100% (mandatory plans). Product lines whose applicable analytic plans are mandatory must have a 100% distribution. | _validate_analytic_distribution (invoked during posting/validation flows of the move). | One or more lines require a 100% analytic distribution. (ValidationError, or RedirectWarning with 'See items' when multiple moves) | `addons/account/models/account_move_line.py#L3060-3074 (method _validate_analytic_distribution at L3043)` | Complete analytic distribution to 100% on product lines where a plan is mandatory. |

### Fiscal positions and withholding tax

| Rule | Triggers on | Error message | Source | How to avoid |
|---|---|---|---|---|
| Empty fiscal position strips ALL taxes. map_tax() returns an empty account.tax recordset when the fiscal position has no tax_ids; this removes every tax from the line instead of leaving them unchanged. A non-empty fp only swaps taxes listed in tax_map and passes others through unchanged. | A fiscal position with tax_ids == empty is applied to a line and map_tax(taxes) is called (e.g. tax units create such empty fps deliberately to zero out taxes). | — | `addons/account/models/partner.py:154 (map_tax), the decisive branch at :157-158 `if not self.tax_ids: return self.env['account.tax']`` | Never leave tax_ids empty on a fiscal position unless you intend to strip all taxes. If you only want to remap specific taxes, you must still list every tax that should survive — taxes NOT present as a src_tax in any tax_map key pass through unchanged, but only because tax_ids is non-empty. An fp with even one tax_id behaves as a selective map; an fp with zero tax_ids is a total wipe. |
| Fiscal position with a foreign VAT must have a country. | create/write of account.fiscal.position where foreign_vat is set but country_id is empty (constraint on country_id/country_group_id/state_ids/foreign_vat). | The country of the foreign VAT number could not be detected. Please assign a country to the fiscal position. | `addons/account/models/partner.py:118-123 (_validate_foreign_vat_country)` | Whenever you set foreign_vat on a fiscal position, always set country_id in the same write. |
| Foreign-VAT fp inside your own fiscal country must specify a state (only if that country has states). | foreign_vat set, country_id == company.account_fiscal_country_id, no state_ids, and the fiscal country has state_ids. | You cannot create a fiscal position with a foreign VAT within your fiscal country without assigning it a state. | `addons/account/models/partner.py:124-127` | For a domestic-country foreign-VAT fp, populate state_ids (used by US-style state registrations). |
| Foreign-VAT fp country must belong to the selected country group. | foreign_vat set, both country_group_id and country_id set, and country_id not in country_group_id.country_ids. | You cannot create a fiscal position with a country outside of the selected country group. | `addons/account/models/partner.py:128-130` | Pick a country_id that is a member of the chosen country_group_id, or clear the group. |
| Only one foreign-VAT fiscal position per country per company. | foreign_vat set and another fp (same company tree, different foreign_vat, same country_id) already exists. | A fiscal position with a foreign VAT already exists in this country. | `addons/account/models/partner.py:132-139` | Reuse the existing foreign-VAT fp for that country instead of creating a second one; the search excludes records sharing the same foreign_vat value and the record itself. |
| Zip range must have both bounds and From <= To. | create/write of fiscal position where exactly one of zip_from/zip_to is set, OR zip_from > zip_to (string comparison; note create/write pad numeric zips with leading zeros via _convert_zip_values). | Invalid "Zip Range", You have to configure both "From" and "To" values for the zip range and "To" should be greater than "From". | `addons/account/models/partner.py:112-116 (_check_zip); padding at :181-198 (create) and :200-206 (write)` | Always set zip_from and zip_to together; ensure From <= To. Numeric zips are right-justified to equal length before comparison, so mixed-length numeric ranges still compare correctly; non-numeric zips are compared raw. |
| auto_apply has NO Python constraint requiring a country — this is UI/matching behavior only, NOT a hard validation. Do not expect a server-side error. | Setting auto_apply=True with no country_id/country_group_id. The form merely hides vat_required/country_group_id/zip fields behind `invisible="not auto_apply"`. At match time, _get_fpos_validation_functions treats an absent country as 'always matches' (`not fpos.country_id or ...`). | — | `field def addons/account/models/partner.py:52; matching functions :215-244 (country lambda at :234-237); view invisibility addons/account/views/partner_view.xml:50-61; auto-apply search at partner.py:277 filters only on `auto_apply=True`` | If you rely on country-based auto-detection, set country_id (or country_group_id) explicitly; an auto_apply fp with no geographic criteria matches every partner that has a country_id (subject to vat_required/zip/state). There is no constraint to catch a mis-configured auto_apply fp — validate it yourself. |
| Auto-apply fiscal position is skipped entirely when the partner has no country. | _get_fiscal_position(partner): no manual property_account_position_id and partner.country_id is empty. | — | `addons/account/models/partner.py:274-275` | Auto-detection short-circuits to an empty fp before evaluating any auto_apply rule when partner.country_id is False. Manual fp on partner/delivery (property_account_position_id) still wins regardless (:266-272). |
| Account-mapping rows must be unique per (position, src account, dest account). | Two account.fiscal.position.account rows on the same fp with identical account_src_id and account_dest_id (SQL unique constraint). | An account fiscal position could be defined only one time on same accounts. | `addons/account/models/partner.py:319-322 (_account_src_dest_uniq)` | Do not duplicate a src/dest account pair within one fiscal position. |
| Withholding-on-payment requires a NEGATIVE tax amount. The is_withholding_tax_on_payment flag is silently reset to False whenever amount >= 0 (onchange resets it because the field is hidden for non-negative taxes). | Editing account.tax: setting amount to >= 0 while is_withholding_tax_on_payment is True triggers _onchange_amount which sets the flag back to False. | — | `addons/l10n_account_withholding_tax/models/account_tax.py:36-40 (_onchange_amount)` | Define withholding taxes with a negative amount (e.g. -10 for 10% withholding). This is an onchange, not a constrains — it fires in the UI/onchange flow; a direct ORM write that sets a non-negative amount without triggering onchange will NOT auto-reset the flag, so set amount negative explicitly when scripting. |
| Withholding-on-payment enabling forces tax_exigibility='on_invoice' and price_include_override='tax_excluded'. | onchange when is_withholding_tax_on_payment is checked True. | — | `addons/l10n_account_withholding_tax/models/account_tax.py:29-34 (_onchange_is_withholding_tax_on_payment)` | Do not expect a withholding tax to be cash-basis or price-included; the onchange clears those. In code, set tax_exigibility='on_invoice' and price_include_override='tax_excluded' yourself if not going through onchange. |
| Withholding-on-payment tax amount_type cannot be 'group' or 'division'. (Note: this is a @api.constrains that raises UserError, despite being a validation — so it fires on create/write, not only onchange.) | create/write account.tax with is_withholding_tax_on_payment=True and amount_type in ('group','division'). | Withholding On Payment taxes cannot use the 'Group of Taxes' or the 'Percentage Tax Included' computations. | `addons/l10n_account_withholding_tax/models/account_tax.py:42-47 (_check_amount_type)` | Use amount_type='percent' or 'fixed' for withholding-on-payment taxes. Note the prompt's phrasing 'must be percent' is imprecise: 'fixed' is also allowed; only 'group' and 'division' are blocked. |
| Withholding line base amount must be strictly above 0. | create/write of any account.withholding.line (payment or register wizard line) where base_amount <= 0 in line currency. | The base amount of a withholding tax line must be above 0. | `addons/l10n_account_withholding_tax/models/account_withholding_line.py:290-295 (_constrains_base_amount)` | Ensure each withholding line has a positive base_amount; comparison uses comodel_currency_id.compare_amounts so it respects currency rounding. |
| Withholding line account cannot be a liquidity/transfer account. | create/write of a withholding line whose account_id is one of the journal's liquidity accounts (default_account_id, payment_account_id, inbound/outbound payment method accounts, the payment's outstanding_account_id) or company.transfer_account_id. | The account "<account_name>" is not valid to use on withholding lines. | `addons/l10n_account_withholding_tax/models/account_withholding_line.py:297-302 (_constrains_account_id); liquidity set built in account_payment_withholding_line.py:82-94 and account_payment_register_withholding_line.py:97-109` | Use a dedicated tax/base account (e.g. company.withholding_tax_base_account_id), never the bank/cash/outstanding/transfer accounts. The abstract base returns an empty liquidity set (:540-542); the concrete payment and register models override _get_valid_liquidity_accounts to populate it. |
| Withholding number (name) is required at payment build time when the tax has no withholding sequence. UserError raised before any sequence is consumed. | _prepare_withholding_amls_create_values() (called during payment posting / move synchronization via _generate_move_vals and _synchronize_to_moves) for a line where name is empty AND tax_id.withholding_sequence_id is empty. | Please enter the withholding number for the tax <tax_name> | `addons/l10n_account_withholding_tax/models/account_withholding_line.py:363-366; consumers account_payment.py:120 and :167` | Either set a withholding_sequence_id on the withholding tax (account_tax.py:17-23) so a number is auto-generated (line.name = withholding_sequence_id.next_by_id() at :371-372), or manually fill the line's name before posting. The check loops all lines first so no sequence is consumed if any line is missing both. |
| Withholding net amount cannot be negative (register payment wizard). | Confirming the Register Payment wizard (_create_payment_vals_from_wizard) with should_withhold_tax and withholding lines whose total exceeds the payment amount, making withholding_net_amount < 0. | The withholding net amount cannot be negative. | `addons/l10n_account_withholding_tax/wizards/account_payment_register.py:204-205; net computed at :57-67` | Keep the sum of withholding line amounts <= the payment amount. withholding_net_amount = amount - sum(line.amount). |
| Withholding base account: lines default account_id to company.withholding_tax_base_account_id; when that company setting is set, the per-line account column is hidden and used as the base account; the grouping/base counterpart lines also prefer it. | Computing a withholding line's account_id (_compute_account_id) and building base lines (_prepare_withholding_lines_commands grouping_function uses `company.withholding_tax_base_account_id or base_line_data['account_id']`). | — | `company field addons/l10n_account_withholding_tax/models/res_company.py:12-16; default at account_withholding_line.py:235-242 (_compute_account_id); grouping at :483; UI hide flags account_payment.py:60-67 and account_payment_register.py:160-167 (_compute_withholding_hide_tax_base_account)` | Set company.withholding_tax_base_account_id to centralize the WHT base account; otherwise each line needs a manually chosen valid (non-liquidity) account. Note this base account still must pass _constrains_account_id (not a liquidity/transfer account). |
| original_tax_ids ('Replaces') domain restricts selectable source taxes to same type_tax_use AND is_domestic=True. | Selecting taxes in original_tax_ids on account.tax (the 'Replaces' M2M). is_domestic is a stored computed flag. | — | `addons/account/models/account_tax.py:102-114 (field + domain); is_domestic compute at :320-323 (_compute_is_domestic: True when tax has no fiscal_position_ids OR the company's domestic_fiscal_position_id is among them)` | Only domestic taxes (no fiscal_position_ids, or linked to the company's domestic fiscal position) of the same type_tax_use can be replaced by a fiscal-position alternative tax. To make a tax selectable as a replacement source, keep it domestic and matching type_tax_use. The reverse M2M is replacing_tax_ids (:115-122). The fp tax_map (partner.py:98-105) is built from dest_tax.original_tax_ids, so this domain governs what map_tax can swap. |
| tax_scope is an optional Selection (Services/Goods) with NO standalone constraint, but it participates in the tax-name uniqueness constraint. | tax_scope itself never raises. It is part of the duplicate-name key: create/write of a tax duplicating (company tree, name, type_tax_use, tax_scope, country_id) of another tax raises. Also used in tax-children compatibility filtering (account_tax.py:597) and label markdown (:669-685). | Tax names must be unique!\n<list of taxes> (each line '- <name> in <company>') | `field addons/account/models/account_tax.py:83; uniqueness constraint :211-238 (_constrains_name, keyed partly on tax_scope at :221); only applies when type_tax_use != 'none'` | Two taxes may share a name only if they differ in type_tax_use, tax_scope, or country_id (and type_tax_use != 'none'). Use tax_scope to restrict a tax to Goods vs Services; leaving it empty means it applies to both. There is no error from setting/clearing tax_scope by itself. |
| Withholding taxes are excluded from normal tax computation unless explicitly requested. | _add_tax_details_in_base_line: when the base line lacks 'calculate_withholding_taxes', a filter_tax_function is injected that drops all is_withholding_tax_on_payment taxes. | — | `addons/l10n_account_withholding_tax/models/account_tax.py:70-79 (_add_tax_details_in_base_line)` | To compute withholding amounts you must pass calculate_withholding_taxes=True when building the base line (as the withholding line prep does at account_withholding_line.py:176-177, :336, :469). Otherwise WHT taxes silently contribute nothing to invoice/move totals (their whole point: no GL impact until payment). |
| Withholding line tax_id domain enforces matching type_tax_use and is_withholding_tax_on_payment=True; the available-taxes domain also enforces the company tree. | Selecting tax_id on a withholding line; and the wizard/payment building the available WHT tax set via _get_withholding_tax_domain. | — | `field domain addons/l10n_account_withholding_tax/models/account_withholding_line.py:47-52; domain builder :430-435 (_get_withholding_tax_domain maps outbound->purchase, inbound->sale); refund inversion in register wizard account_payment_register.py:112-119` | Only withholding taxes whose type_tax_use matches the payment direction are selectable (outbound payment -> purchase taxes, inbound -> sale; inverted for refund lines). type_tax_use on the line is computed from the tax or, if none, from payment_type (account_payment_withholding_line.py:33-39). |

### Odoo 19 account.chart.template loading: every validation/constraint th

| Rule | Triggers on | Error message | Source | How to avoid |
|---|---|---|---|---|
| Tax-tag name -> id mapping. _get_tag_mapper(country_id) builds a dict of {tag.name: tag.id} restricted to account.account.tag with applicability='taxes' and country_id == the template's country_id (from _get_chart_template_mapping()[code]['country_id']). For each tag string referenced by a repartition line's tag_ids, if it is NOT an xmlid (not matching ^(module).(name)$ with an installed module) it is whitespace-normalized (re.sub(r'\s+',' ',tag.strip())) and looked up in that dict. A miss raises UserError UNLESS context flag ignore_missing_tags is set (then it only _logger.error and skips the tag). | Loading account.tax data: _get_account_tax -> _deref_account_tags(template_code, tax_data) -> _get_tag_mapper(...)(*tags.split('//')). Fires at data-build time (before any DB write), once per tag token in repartition_line_ids/invoice_repartition_line_ids/refund_repartition_line_ids tag_ids strings (delimited by '//'). | Error while loading the localization: missing tax tag %(tag_name)s for country %(country_name)s. You should probably update your localization app first. | `addons/account/models/chart_template.py:1252 (raise inside _get_tag_mapper, lines 1233-1266); called from _deref_account_tags chart_template.py:1268-1275; tag string passed via _get_account_tax chart_template.py:1126-1130` | The tag MUST pre-exist as an account.account.tag with applicability='taxes' and country_id equal to the template country, matched by exact (whitespace-collapsed) en_US name. These tags are normally created by the country's tax report (account.report.line tax_tags expressions) or an explicit account.account.tag XML record. To add a new grid: define the tag record/report-line FIRST (so the localization app provides it), then reference its name in the CSV tag_ids. Alternatively reference it as a fully-qualified xmlid 'module.record_id' (the regex+module-existence check at line 1243 lets a real xmlid pass through unmapped). The 'update your localization app first' message literally means: the tag name in your tax CSV has no matching tag for that country yet. Note tag names are looked up in lang='en_US' (line 1234) and active_test=False, so inactive tags still match but a translated-only name will not. |
| Tag country matching. The mapper dict only contains tags whose country_id == country_id passed in. Tags for a different country (or country_id=False) are invisible to the mapper and therefore treated as missing. The country_id used is the chart template's declared country, NOT necessarily company.account_fiscal_country_id. | Same path as above. country_id = self._get_chart_template_mapping()[template_code]['country_id'] (chart_template.py:1269). | Error while loading the localization: missing tax tag %(tag_name)s for country %(country_name)s. You should probably update your localization app first. | `addons/account/models/chart_template.py:1234-1237 (search domain ('applicability','=','taxes'),('country_id','=',country_id)); raise at 1252` | Create the tag with country_id set to the SAME country as the chart template declares. A tag with country_id=False or a different country will not be found. account.account.tag has SQL constraint unique(name, applicability, country_id) (addons/account/models/account_account_tag.py:22-25), so you can have per-country tags of the same name; make sure the one for THIS template's country exists. Mapper search ignores active state (active_test=False) so deactivating is not enough to break it, but wrong/empty country_id is. |
| Account xmlid resolution during deref. In _load_data.deref_values, every many2one / non-numeric integer string value, and every SET / LINK / CREATE-subdict relational value, is resolved via self.ref(value) -> env.ref(...) which calls _xmlid_lookup and raises ValueError('External ID not found in the system: %s') if the xmlid (or its company-prefixed form) has no ir_model_data row with a non-zero res_id. For most models this ValueError is caught and the field is dropped (logged as a warning); for res.company it falls back to the company value; BUT for SET/LINK/CREATE-nested references (lines 605-610) and for one2many/many2many comma-string refs (611-616) the ValueError is NOT caught and propagates, aborting the load. | _load_data -> deref_values (chart_template.py:573-619), run for every record's values right before _load_records. Account/tax/fiscal-position references given as xmlid strings (e.g. account_id, account_src_id, account_dest_id, children_tax_ids, tag_ids as xmlid) are dereferenced here. | External ID not found in the system: <xmlid> (raised as ValueError; for many2one/scalar fields it is swallowed with log 'Failed when trying to recover %s for field=%s', for Command.SET/LINK and m2m comma-strings it is NOT swallowed) | `ValueError raised at odoo/addons/base/models/ir_model.py:2241 (_xmlid_lookup). Caught for m2o/scalar at chart_template.py:589-595 (warning at 594); NOT caught for SET at 605-608, LINK at 609-610, m2m-string at 611-616. self.ref/company_xmlid at chart_template.py:1213-1223.` | Every account/tax/fiscal-position/group xmlid referenced in a template CSV or @template dict must be DEFINED (have its own row in the same data set or already exist in DB) before it is referenced. Within a single _load_data run, ordering/delay handles forward refs to records being created in the same batch; but a typo'd or never-defined xmlid in a Command.set([...])/Command.link()/comma-separated m2m (e.g. children_tax_ids, fiscal_position_ids, original_tax_ids, tag_ids xmlids) hard-fails. Use the bare local xmlid (no module prefix) for same-company records (it is auto-prefixed to account.{company_id}_{xmlid} via company_xmlid), or a full 'module.xmlid' for cross-module records. For scalar m2o fields a bad ref is silently dropped to False -> the field ends up empty, which is its own silent bug to watch for. |
| TEMPLATE_MODELS load order. Records are created model-by-model in the fixed order: account.group, account.account, account.fiscal.position, account.tax.group, account.tax, account.journal, account.reconcile.model. _get_chart_template_data sorts model funcs by TEMPLATE_MODELS.index (unknown models sort last with key 1000). _load_data.delay() then iterates in that order and DEFERS any relational field whose comodel is not yet created (or is a forward/self reference) to a second pass appended at the end. | _get_chart_template_data sort (chart_template.py:813-817) builds data in this order; _load_data.delay() (621-671) consumes it in dict order and re-appends deferred relations. | (no direct error; wrong order surfaces as the ValueError 'External ID not found' above, or as the duplicate-base-line repartition error below if defaults are not cleared) | `TEMPLATE_MODELS tuple chart_template.py:22-30; sort key chart_template.py:814-816; delay()/should_delay chart_template.py:621-671` | Do not assume taxes exist when accounts/tax.groups are created (they don't yet) and do not assume fiscal positions exist when accounts are created. Forward references between records created in the same _load_data batch ARE handled by delay(): if a field's comodel is yet-to-be-created or is the same model (self-ref like children_tax_ids), the field is stripped from the initial create and re-applied in a deferred second write. Crucially (chart_template.py:653-663), when account.tax repartition_line_ids is deferred for a NOT-yet-existing tax, delay() prepends Command.clear() so the auto-created default repartition lines are wiped before the template lines are written — this is what prevents duplicate base lines on fresh creation. |
| Command.create appending repartition lines -> 'exactly one base line' violation. account.tax._validate_repartition_lines / _check_repartition_lines enforces that invoice_repartition_line_ids and refund_repartition_line_ids each contain EXACTLY ONE repartition_type='base' line. When you write Command.create({...}) repartition lines onto an EXISTING tax that already has its lines (e.g. a default base+tax pair created automatically, or pre-existing template lines), the new lines are APPENDED, giving 2+ base lines -> ValidationError. | @api.constrains('invoice_repartition_line_ids','refund_repartition_line_ids','repartition_line_ids') runs on every create/write of an account.tax that touches these fields — including the _load_records write done by _load_data. On a fresh tax, Odoo auto-creates a default base + tax line; if your template also supplies Command.create lines WITHOUT a leading Command.clear(), they append. | Invoice and credit note distribution should each contain exactly one line for the base. | `raise at addons/account/models/account_tax.py:551 (_check_repartition_lines 546-551); constraint _validate_repartition_lines account_tax.py:553-588 (calls _check_repartition_lines at 564-565). Related companion errors: 'same number of lines' 568, 'at least one tax repartition line' 572, 'should match (same percentages, in the same order)' 579, 'total factor (+) equals to 100' 585, 'total factor (-) equals to 100' 588.` | To update an EXISTING tax's repartition without duplicating, you have two correct mechanisms. (1) On FRESH creation in the same batch, rely on delay(): chart_template.py:653-663 prepends Command.clear() to repartition_line_ids when the tax does not yet exist, removing the auto-defaults before your lines are written. (2) On RELOAD of an existing tax, do NOT append. _pre_reload_data converts in-place: chart_template.py:480-482 rewrites each Command.CREATE in an x2many of an existing record into Command.update(existing_line.id, vals) by zipping rec[fname] with values[fname] positionally — so line N in your template updates line N already on the tax (count must match). Additionally, if the tax is judged 'changed' (tax_template_changed: amount_type/amount differ, or template provides a different NON-ZERO count of lines than the tax currently has — chart_template.py:337-344), the old tax is renamed '[old] ...' (392-397) and obsolete xmlid unlinked (457-461) so a brand-new tax is created cleanly instead of mutating. If unchanged, repartition_line_ids are kept but reduced to ONLY tag_ids updates (chart_template.py:421-427: each line's vals are cleared except tag_ids, which become [Command.clear()] when empty). Practical rule: to merely retag/adjust an existing tax, keep the SAME number of repartition lines in the template (positional update), or change amount/amount_type/line-count to force a full recreate. Never hand-write Command.create repartition lines expecting them to replace — they append and trip the base-line constraint. |
| _parse_csv structure & evaluation rules. CSV files are read from {module}/data/template/{model}[-{template}].csv. The module name is asserted to match ^[a-z0-9_]+$. Parent-template CSVs are layered (parents reversed, so the most specific template overrides). 'id' column keys the record; columns named 'field' must exist in the model's _fields (or contain '@' for translations) else they are ignored; boolean/int/float fields are ast.literal_eval'd, char fields are .strip()'d; columns containing '/' define nested one2many records via Command.create({}). Missing file is silently ignored (debug log only). | _get_account_account / _get_account_group / _get_account_tax_group / _get_account_tax / _get_account_fiscal_position all call _parse_csv (chart_template.py:1114-1134). Runs at data-build time. | AssertionError (no message) if module name fails the regex; ValueError/SyntaxError from ast.literal_eval if a boolean/int/float column holds a non-literal; FileNotFoundError is caught and ignored (debug log 'No file %s found for template') | `_parse_csv chart_template.py:1277-1329; module assert at 1283; evaluate() literal_eval at 1294 / char strip at 1296; nested '/' handling 1312-1325; FileNotFoundError swallow 1327-1328; unknown-field filtering happens later in _pre_load_data chart_template.py:531-543 (deletes keys whose fname.split('@')[0] not in model._fields, unless context l10n_check_fields_complete is set)` | Keep CSV column headers matching real field names (unknown columns are dropped unless context l10n_check_fields_complete=True, which would instead surface them). For boolean/int/float fields the cell must be a valid Python literal (True/False/1/0/1.5) — a bad value raises during ast.literal_eval. Use 'fieldname@lang' headers for translations (skipped from field-existence check). Use 'parent/child_field' columns to build nested Command.create lines (each new model_path segment appends one Command.create({}); see the create_added set logic 1318-1323). The empty-string template fallback (['' ] at 1300) reads {model}.csv when there is no parent chain. Note: code_digits normalization of account.account 'code' happens in _pre_load_data (chart_template.py:521-524), padding to template_data code_digits (default 6); and account.account 'prefix'+'code_digits' (no explicit code) is resolved to a free code by account.account.create via _search_new_account_code (account_account.py:1013-1017), which raises UserError 'Cannot generate an unused account code.' (account_account.py:544) if it cannot find one. |
| Account code uniqueness per company tree. account.account._ensure_code_is_unique requires a code set for every company the account belongs to, and no parent/child company may already have an account with that code. | account.account write/create touching {company_ids, code, code_mapping_ids} unless context defer_account_code_checks is set (account_account.py:1057). Fires during _load_records of accounts. | The code must be set for every company to which this account belongs. / Account codes must be unique. You can't create accounts with these duplicate codes: %s | `addons/account/models/account_account.py:1081 (code-not-set) and 1114-1116 (duplicate codes); guard at 1057-1062; _ensure_code_is_unique 1066-1116` | Ensure each template account has a unique code within the company (and across parent/child companies in a multi-company tree). On reload, _pre_reload_data re-points an existing-code account's xmlid to the existing record instead of creating a duplicate (chart_template.py:428-444 matches by code SIMILAR TO pattern). Padding to code_digits (chart_template.py:521-524) can make two short codes collide after padding — keep distinct codes. |
| Receivable/Payable accounts must be reconcilable; the template often loads such accounts. Also off_balance accounts cannot be reconcilable or carry taxes. | @api.constrains('account_type','reconcile') and @api.constrains('reconcile','account_type','tax_ids') on account.account create/write during load. | You cannot have a receivable/payable account that is not reconcilable. (account code: %s) / An Off-Balance account can not be reconcilable / An Off-Balance account can not have taxes | `addons/account/models/account_account.py:31 (_check_reconcile 27-31); 192 and 194 (_constrains_reconcile 187-194)` | In the account CSV, set reconcile=True for any account with account_type asset_receivable or liability_payable. Do not set reconcile=True or tax_ids on off_balance accounts. Note _pre_reload_data pops 'reconcile' from values on existing accounts (chart_template.py:447) to avoid overriding the user's setting and a partial-reconcile error — so this mainly bites on first creation. |
| Tax name uniqueness within the company tree. account.tax names must be unique per (company tree, name, type_tax_use, tax_scope, country_id) for taxes whose type_tax_use != 'none'. | @api.constrains('company_id','name','type_tax_use','tax_scope','country_id') on account.tax create/write during _load_records. | Tax names must be unique!\n%(taxes)s (each line: - %(name)s in %(company)s) | `addons/account/models/account_tax.py:226-238 (_constrains_name 211-238)` | On reload, _pre_reload_data proactively renames colliding pre-existing taxes to '[old] <name>' / '[old1] <name>' (chart_template.py:392-397) before creating the new one, and unlinks obsolete xmlids, to dodge this constraint. For new templates, ensure your tax CSV does not define two taxes with identical name+type_tax_use+tax_scope+country in the same company tree. |
| Tax group country must match the tax's country. A tax's tax_group_id, if it has a country_id, must equal the tax's country_id. | @api.constrains('tax_group_id') on account.tax during load. | The tax group must have the same country_id as the tax using it. | `addons/account/models/account_tax.py:244 (validate_tax_group_id 240-244)` | Assign each tax a tax.group whose country_id is either empty or equals the tax's country. account.tax.group.country_id is computed from company.account_fiscal_country_id by default (account_tax.py:65-68), and account.tax.country_id likewise (account_tax.py:268-271), so leaving both to default usually aligns; only explicit cross-country group assignment breaks it. |
| Cash-basis transition account must be reconcilable — but this check is SKIPPED during chart template load. | @api.constrains('tax_exigibility','cash_basis_transition_account_id'): only raises if tax_exigibility='on_payment' AND the account is not reconcilable AND context flag chart_template_load is NOT set. | The cash basis transition account needs to allow reconciliation. | `addons/account/models/account_tax.py:254 (guarded by 'not self.env.context.get("chart_template_load")' at 252); _load sets context chart_template_load=True at chart_template.py:205` | During normal template load this is bypassed because _load runs with chart_template_load=True (chart_template.py:199-206). If you create/modify such a tax OUTSIDE the load flow, set the cash_basis_transition_account_id to a reconcilable account. _instantiate_foreign_taxes creates these accounts reconcilable=True explicitly (chart_template.py:1049-1057). |
| Children-tax scope/recursion rules for group taxes. A group tax's children must share its type_tax_use (or 'none') and tax_scope (or False); no recursion; no nested groups. | @api.constrains('children_tax_ids','type_tax_use') on account.tax load (children_tax_ids is a self-referencing m2m, deferred by delay() then applied). | Recursion found for tax “%s”. / The application scope of taxes in a group must be either the same as the group or left empty. / Nested group of taxes are not allowed. | `addons/account/models/account_tax.py:594, 600, 605 (_check_children_scope 590-605)` | When defining amount_type='group' taxes via children_tax_ids (comma-separated xmlids in CSV), ensure each child has the same type_tax_use (sale/purchase) or 'none', matching or empty tax_scope, no cycles, and no child is itself a group. children_tax_ids is resolved via deref of the comma string (chart_template.py:611-616) and applied in delay()'s deferred pass. |
| Tax repartition line account domain (check_company + type exclusion). account.tax.repartition.line.account_id must belong to the tax's company tree (check_company=True) and the field domain excludes asset_receivable/liability_payable/off_balance account types. Base lines must have no account (onchange nulls it). | Writing repartition_line_ids on account.tax during _load_records; company consistency enforced by _check_company_auto / check_company_domain_parent_of. | (company mismatch -> standard '... must be assigned to ... company' / check_company error from base; domain is a UI/onchange constraint, account nulled for base lines) | `addons/account/models/account_tax.py:4923-4946 (AccountTaxRepartitionLine: _check_company_auto=True 4927, _check_company_domain 4928, account_id domain 4942, _onchange_repartition_type nulls account for base 4977-4980)` | Set account_id on repartition lines only for repartition_type='tax' lines, never 'base'. Use accounts that exist in the same company (or parent) and are not receivable/payable/off_balance. In templates, account references are local xmlids resolved per-company. |
| Fiscal-position foreign-VAT consistency. account.fiscal.position with foreign_vat enforces: a country must be derivable; within your own fiscal country a foreign_vat requires a state_ids; country must be inside the chosen country_group; only one foreign-VAT fiscal position per country. | @api.constrains('country_id','country_group_id','state_ids','foreign_vat') on account.fiscal.position create/write during load (fiscal positions are 3rd in TEMPLATE_MODELS order). | The country of the foreign VAT number could not be detected. Please assign a country to the fiscal position. / You cannot create a fiscal position with a foreign VAT within your fiscal country without assigning it a state. / You cannot create a fiscal position with a country outside of the selected country group. / A fiscal position with a foreign VAT already exists in this country. | `addons/account/models/partner.py:123, 127, 130, 139 (constrains block 118-139); fiscal position model is account.fiscal.position in partner.py:26-27; zip range check at partner.py:116` | Standard domestic template fiscal positions usually omit foreign_vat and are unaffected. If a template defines a foreign-VAT fiscal position, give it a country (and a state if it is within the fiscal country), keep the country inside any country_group, and only one per country. Also for zip-range positions set both zip_from and zip_to with to>=from (partner.py:112-116: 'Invalid "Zip Range"...'). |
| Account tag unique constraint and protected master tags. account.account.tag has unique(name, applicability, country_id); and the three master tags (account_tag_operating/financing/investing) cannot be deleted/deactivated. | Creating account.account.tag rows (e.g. tax grids) during/around load; ondelete on those master tags. | A tag with the same name and applicability already exists in this country. / You cannot delete this account tag (%s), it is used on the chart of account definition. | `addons/account/models/account_account_tag.py:22-25 (_name_uniq) and 120 (_unlink_except_master_tags 110-120)` | Do not redefine an existing (name, applicability, country) tag. The mapper relies on these tags existing per-country; create them once via the localization/report layer. Never attempt to remove the master operating/financing/investing tags referenced by default cash-difference accounts (chart_template.py:862-869). |
| Account-group prefix range constraints. account.group code_prefix_start/end must be equal length; same-granularity groups can't overlap; no recursive parent groups. | @api.constrains on account.group (first model created in TEMPLATE_MODELS); on reload account.group is skipped entirely if any group already exists (chart_template.py:307-309). | The length of the starting and the ending code prefix must be the same / Account Groups with the same granularity can't overlap / You cannot create recursive groups. | `addons/account/models/account_account.py:1497-1499 (_check_length_prefix Constraint), 1555 (overlap, _check_prefix_overlap 1536-1555), 1567 (recursion, 1564-1567)` | Define account.group prefixes with equal-length start/end, non-overlapping ranges at the same depth, and no parent cycles. On reload these are not re-evaluated because the whole account.group dataset is dropped when groups already exist. |
| x2many positional update on reload (count must align). In _pre_reload_data, for any existing record, each Command.CREATE in an x2many is converted to Command.update of the existing line at the SAME index via zip(rec[fname], values[fname]). If your template supplies a different number of lines than already exist, zip truncates to the shorter -> extra template lines are silently ignored or extra existing lines untouched, which can leave repartition counts mismatched and then trip _validate_repartition_lines ('same number of lines'). | _pre_reload_data x2many conversion (only on reload_template, i.e. loading the same chart_template already on the company). | (indirect) Invoice and credit note distribution should have the same number of lines. (account_tax.py:568) | `chart_template.py:463-482 (zip+Command.update at 480-482); reload entry _load chart_template.py:232-234` | When editing an existing tax via reload, keep the template's repartition line list the SAME length and order as the live tax's lines (positional mapping). If you need a structurally different repartition (different count), force a recreate by changing amount/amount_type/line-count so tax_template_changed() (chart_template.py:337-344) returns True — then the old tax is renamed and a fresh one created instead of being patched in place. |

### account.journal / account.payment / account.payment.term / account.pay

| Rule | Triggers on | Error message | Source | How to avoid |
|---|---|---|---|---|
| A 'bank' journal's bank_account_id must (a) belong to the same company and (b) be held by the company's own partner (company_id.partner_id). | Creating/writing a bank journal with a bank_account_id, or chart setup assigning bank accounts to journals. | The bank account of a bank journal must belong to the same company (%s). / The holder of a journal's bank account must be the company (%s). | `addons/account/models/account_journal.py:604 (_check_bank_account, raises at :609 and :613)` | When wiring journals to res.partner.bank in our localization, set the bank account's company_id to the journal's company and partner_id to company.partner_id; never point a bank journal at a customer/supplier bank account. |
| A sale/purchase journal's default_account_id must NOT be of type asset_receivable or liability_payable. | Setting/changing default_account_id on a sale or purchase journal (e.g. when our chart template assigns default income/expense accounts). | The type of the journal's default credit/debit account shouldn't be 'receivable' or 'payable'. | `addons/account/models/account_journal.py:624 (_check_type_default_account_id_type, raise at :628)` | Map sale journals to an income account and purchase journals to an expense account; keep 1310/3xxx receivable/payable accounts off the journal default. Our gec_localization journal defaults must avoid asset_receivable/liability_payable account_types. |
| Payment-method-line multiplicity: 'unique'-mode methods may exist on only one journal per company; 'electronic'-mode one per journal per company per provider; and no two lines of the same payment_method+name on one journal. | Adding/editing inbound/outbound payment_method_line_ids on a journal (also triggered indirectly when a payment.method line name is written, via account_payment_method.py:145). | You can't have two payment method lines of the same payment type (%(payment_type)s) and with the same name (%(name)s) on a single journal. / Some payment methods supposed to be unique already exists somewhere else.\n(%s) | `addons/account/models/account_journal.py:630 (_check_payment_method_line_ids_multiplicity, raises at :661 and :690)` | When seeding journals via chart_template, attach each 'unique' payment method (e.g. manual) to exactly one journal per company and give each line a distinct name. |
| A journal cannot be archived (active=False) while it has draft journal entries. | Archiving/deactivating a journal that still has state='draft' moves. | You can not archive a journal containing draft journal entries.\n\nTo proceed:\n1/ ... 2/ ... 3/ ... | `addons/account/models/account_journal.py:695 (_check_auto_post_draft_entries, raise at :705)` | Post or delete all draft moves on a journal before archiving it in migration/cleanup scripts. |
| A journal's company_id cannot be changed once any journal entry is linked to it. | Writing company_id on a journal that already has account.move records. | You can't change the company of your journal since there are some journal entries linked to it. | `addons/account/models/account_journal.py:615 (_check_company_consistency, raise at :622)` | Set journal company at creation; never reassign company on journals after posting (relevant for multi-company chart re-org scripts). |
| restrict_mode_hash_table cannot be disabled on a journal that already has hashed accounting entries (write-time guard, not @api.constrains). | Writing restrict_mode_hash_table=False on a journal that has moves with inalterable_hash set. | You cannot modify the field %s of a journal that already has accounting entries. | `addons/account/models/account_journal.py:write override, raise at :811 (also bank-account partner mismatch raise at :803)` | Decide hash/inalterability per journal at setup; don't toggle restrict_mode_hash_table off after posting hashed entries. |
| Every account.payment must have a payment_method_line_id, and that line's journal_id (if set) must equal the payment's journal_id (computed-stored field, enforced via constrains since required=True can't be used). | Confirming/creating a payment whose computed payment_method_line_id is empty or belongs to a different journal. | Please define a payment method line on your payment. / The selected payment method is not available for this payment, please select the payment method again. | `addons/account/models/account_payment.py:808 (_check_payment_method_line_id, raises at :815 and :817)` | Ensure each bank/cash journal used for payments has at least one inbound/outbound payment method line so the computed line resolves; keep the line on the same journal as the payment. |
| A non-draft/non-canceled payment that has an outstanding_account_id cannot exist without a journal entry (move_id). | Posting a payment that resolves an outstanding account but has no move_id. | A payment with an outstanding account cannot be confirmed without having a journal entry. | `addons/account/models/account_payment.py:819 (_check_move_id, raise at :827)` | Don't bypass move generation when confirming payments; let standard flow create move_id whenever an outstanding/transfer account is configured. |
| You cannot REDUCE a currency's decimal places (increase rounding, or set rounding=0) once that currency has been used in accounting move lines (write-time guard). | Writing res.currency.rounding to a coarser value (or 0) on a currency already used as currency_id/company_currency_id on any move line. | You cannot reduce the number of decimal places of a currency which has already been used to make accounting entries. | `addons/account/models/res_currency.py:26 (write override, raise at :31)` | Set GEL/foreign currency rounding precisely before posting; never coarsen an in-use currency's rounding in setup/migration scripts. |
| A company's currency_id cannot be changed if the company (root) already has accounting entries (write-time guard on res.company). | Writing res.company.currency_id to a different value when root_id._existing_accounting() is true. | You cannot change the currency of the company since some journal items already exist | `addons/account/models/company.py:write, raise at :748` | Set company currency at company creation / before first posting; do not flip company currency after the chart is in use. |
| account_price_include (company-level tax price-include default) cannot be changed once the company has started invoicing. | Writing res.company.account_price_include on a company where _existing_accounting() is true. | Cannot change Price Tax computation method on a company that has already started invoicing. | `addons/account/models/company.py:334 (_check_set_account_price_include, raise at :337)` | Fix the tax price-include policy in res.config.settings before the first invoice; treat it as immutable post-go-live in our tax setup. |
| restrictive_audit_trail cannot be turned off if force_restrictive_audit_trail is set by a localization. | Writing res.company.restrictive_audit_trail=False while a localization forces it on. | Can't disable restricted audit trail: forced by localization. | `addons/account/models/company.py:328 (_check_audit_trail_restriction, raise at :332)` | If gec_localization sets force_restrictive_audit_trail, do not expose/allow disabling restrictive_audit_trail; keep the two consistent. |
| Company fiscal year last day must be a valid day for the configured last month (1..monthrange max), with a special allowance for Feb 29. | Setting fiscalyear_last_day / fiscalyear_last_month (or account_opening_move_id) to an out-of-range day. | Invalid fiscal year last day | `addons/account/models/company.py:339 (_check_fiscalyear_last_day, raise at :354)` | Use valid day/month combos for the GE fiscal year (e.g. 31/12); avoid day 0 or >max for the chosen month. |
| account.cash.rounding.rounding must be strictly positive. | Creating/writing a cash rounding record with rounding <= 0. | Please set a strictly positive rounding value. | `addons/account/models/account_cash_rounding.py:45 (validate_rounding, raise at :49)` | When defining cash-rounding presets for invoices, always give a positive rounding step (e.g. 0.01). |
| On a bank statement line, foreign_currency_id must differ from the line's currency_id; amount_currency requires a foreign_currency_id and vice-versa (mutual consistency). | Creating/writing a statement line with foreign_currency_id == currency_id, or amount_currency set without foreign_currency_id (or the reverse). | The foreign currency must be different than the journal one: %s / You can't provide an amount in foreign currency without specifying a foreign currency. / You can't provide a foreign currency without specifying an amount in 'Amount in Currency' field. | `addons/account/models/account_bank_statement_line.py:320 (_check_amounts_currencies, raises at :326/:329/:332)` | In bank import flows, only set foreign_currency_id+amount_currency together and only when it differs from the journal currency. |
| A bank statement may only contain lines from a single journal, and statements cannot be created with missing/out-of-order transactions. | Grouping statement lines from >1 journal into one statement, or creating a statement over a gap in transactions. | A statement should only contain lines from the same journal. / Unable to create a statement due to missing transactions. You may want to reorder the transactions before proceeding. | `addons/account/models/account_bank_statement.py:321 and :331` | Build statements per journal and over a contiguous transaction range. |
| account.partial.reconcile requires both debit_currency_id and credit_currency_id to be set (computed); a partial cannot have a missing foreign currency. | Creating/writing a partial reconcile whose debit_/credit_currency_id resolves empty. | Missing foreign currencies on partials having ids: %s | `addons/account/models/account_partial_reconcile.py:73 (_check_required_computed_currencies, raise at :77)` | Ensure both reconciled move lines carry a currency_id (company currency at minimum) before creating partials in custom reconciliation code. |
| account.analytic.line.general_account_id must match its move_line_id.account_id. | Creating/writing an analytic line linked to a journal item with a mismatched general account. | The journal item is not linked to the correct financial account | `addons/account/models/account_analytic_line.py:52 (_check_general_account_id, raise at :56)` | Let the analytic line's general_account_id stay computed from move_line_id.account_id; don't override it independently. |
| account.payment.term: the sum of 'percent' lines must equal 100% (Payment Terms precision), early-discount terms must be single-line with strictly positive discount % and days; per-line percent values must be 0..100. | Editing payment term line_ids / early_discount / line value_amount. | The Payment Term must have at least one percent line and the sum of the percent must be 100%. / The Early Payment Discount functionality can only be used with payment terms using a single 100% line. / The Early Payment Discount must be strictly positive. / The Early Payment Discount days must be strictly positive. / Percentages on the Payment Terms lines must be between 0 and 100. | `addons/account/models/account_payment_term.py:156 (_check_lines, raises :162-169) and :343 (_check_percent, raise :347); days_next_month range guard at :329 (raise :334/:336)` | Define payment terms whose percent lines sum to 100; for early discount use one 100% line with positive discount %/days; keep days_next_month 0..31. |
| account.reconcile.model(.line) regexes (match_label_param when match_regex, and amount_string when amount_type=regex) must compile; fixed/percentage amount_string must not be 0. | Saving a reconcile model with an invalid regex or a zero fixed/percentage amount. | The regex is not valid / The amount is not a number / Balance percentage can't be 0 / Statement line percentage can't be 0 | `addons/account/models/account_reconcile_model.py:138 (_check_match_label_param, raise :145) and :64 (_validate_amount on account.reconcile.model.line, raises :68/:70/:72/:77)` | Validate regex patterns and use non-zero amounts when seeding reconcile models in a localization. |
| A withholding-on-payment tax may not use amount_type 'group' or 'division'. | Setting is_withholding_tax_on_payment with amount_type in ('group','division'). | Withholding On Payment taxes cannot use the 'Group of Taxes' or the 'Percentage Tax Included' computations. | `addons/l10n_account_withholding_tax/models/account_tax.py:42 (_check_amount_type, raise at :47)` | Define GE withholding taxes as 'percent' or 'fixed' amount_type, never as group/division. |
| A withholding line's account_id may not be a liquidity (bank/cash) account nor the company transfer_account_id; and base_amount must be > 0. | Creating/writing an account.withholding.line with a liquidity/transfer account, or base_amount <= 0. | The account "%(account_name)s" is not valid to use on withholding lines. / The base amount of a withholding tax line must be above 0. | `addons/l10n_account_withholding_tax/models/account_withholding_line.py:297 (_constrains_account_id, raise :302) and :290 (_constrains_base_amount, raise :295)` | Route withholding to a dedicated liability/asset withholding account (never 12xx bank/cash or the transfer account), and only register withholding lines with a positive base. |
